# 하네스 변경 이력

> 하네스(CLAUDE.md·hook·skill·센서·메트릭)를 바꾸면 여기에 기록한다.
> 목적: 진화 방향 추적 + 퇴행(regression) 방지.
> **정렬: 최신이 위.** 같은 날짜 안에서는 나중 변경이 위다.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-12 | **ADR-0015 결정 3 폐기·개정**(앞뒤 대칭 → 방향 표현) + `requirements.md` **v2.8**(RQ-74~77 신설, RQ-73·43·53 개정) + 골든 **GA-101~111** 신설 | `harness/adr/0015-player-model-geometry.md`, `harness/adr/README.md`, `harness/specs/requirements.md`, `harness/evals/golden/track-a-product.jsonl`, `harness/progress.md` | 원장 24ck·24cb·24cl, PR #82. ⚠️ **이 저장소에서 ADR 결정이 「폐기」된 첫 사례다** — 신설·개정은 있었으나 조항 자체를 무효로 돌린 적은 없었다. 원문은 `<details>`로 보존했다(이력이 사라지면 「왜 대칭이었나」를 다음 사람이 재발명한다). ⚠️ **무효 조건이 예고대로 작동한 첫 사례이기도 하다** — 조건을 산문이 아니라 **전용 ⬜ 행**(24cb)으로 두고 참조 파일에 `GameState.ts`를 넣어 검출기에 연결한 덕에 `yaw` 라운드가 그 결정을 **찾아왔다**. ADR-0014 결정 1이 같은 형태를 산문에만 적어 PR #73 리뷰 major를 받은 것(원장 28t)과 대비된다. ⚠️ **그러나 그 성과를 자평하며 예고 장치를 제거할 뻔했다**(1차 리뷰 blocker B2) — 스펙만 쓰고 스키마를 안 건드린 채 24cb·24ck를 둘 다 ✅로 닫아 **`GameState.ts` 트리거가 죽고 RQ-74~77을 소유하는 열린 행이 0개**가 됐었다. **교훈: 무효 조건을 소진 처리할 때는 「그 행이 소유하던 트리거를 누가 인수하는가」를 함께 적는다.** |
| 2026-08-11 | **ADR-0016 신설 + 골든 GA-96~100** — 명중 피드백의 권위를 정한다. **서버가 명중 이벤트를 보내고 클라는 그리기만** 한다 (⚠️ **승인 ADR-0014도 함께 고쳤다** — 그 결정 1이 RQ-70·71을 대표 예시로 **이름 지어** 쓰고 있어 갱신하지 않으면 두 승인 ADR이 같은 대상에 대해 갈린 채 머지된다. 델타 재리뷰 major M3·minor D5) | harness/adr/0014-audio-authority.md, harness/adr/0016-hit-feedback-authority.md, harness/evals/golden/track-a-product.jsonl, harness/specs/requirements.md, harness/progress.md, harness/adr/README.md | **RQ-70·71 개정과 같은 PR이다**(스펙 항목 = PR 1개). ⚠️ **ADR-0014와 반대 방향을 택한 것이 이 ADR의 요점**이다 — 발소리는 클라가 서버와 **같은 답**을 낼 수 있어 로컬 판정을 허용했지만, 명중은 서버가 **RQ-64로 되감아** 판정하므로 클라 재현이 **구조적으로 갈린다**(상한 200ms·6 m/s면 최대 1.2m). ⚠️ **GA-100을 처음부터 넣었다** — RQ-73 라운드가 순수 함수를 만들고 **그 함수를 부르는 배선에 테스트를 0건**으로 둬서 계약 위반 변이 4건이 1039/1039 통과한 채 살아남았다. 같은 공백을 선제로 막는다. ⚠️ **정보 누출 필터를 두지 않은 것은 선택**이고(RQ-31·RQ-56이 경계해 온 축) 그 무효 조건을 원장 **24cc**(전용 ⬜ 행)의 참조 파일 열에 연결했다 — ⚠️ 초안은 24ca(완료 행)였는데 **그 행이 닫히면 조건도 죽는다**(리뷰 major M2) |
| 2026-08-11 | **ADR-0015 신설 + 골든 GA-90~94** — 캐릭터 모델의 형상·권위를 정한다. 결정 5건: ①**절차적 지오메트리**(모델 에셋 파일 0건 — ADR-0014 결정 2와 같은 계열) ②**히트박스가 정본**(모델이 히트박스를 따르며 그 역이 아니다) ③**앞뒤 대칭** + **무효 조건**(yaw를 스키마에 올리는 날 개정 — 원장 **24cb**가 소유하고 참조 파일 열에 `GameState.ts`를 연결해 검출기가 보게 했다 — ⚠️ **초안은 24bz였고 그 행이 같은 날 ✅로 닫히며 실제로 죽어 있었다**(PR #80 리뷰 major M2, 실측 확인)) ④**걸음 위상은 별도 누적기**(RQ-72와 값을 공유하지 않는다) ⑤**검증 2층 분리**(배치·위상 계산은 순수 함수로 면제 없음, 렌더 배선만 면제하고 스크린샷으로 확인). 골든 GA-90~94는 `status: todo` — 구현 라운드가 `done`으로 승격한다 | harness/adr/0015-player-model-geometry.md, harness/evals/golden/track-a-product.jsonl, harness/specs/requirements.md, harness/progress.md | **RQ-73 신설과 같은 PR이다**(스펙 항목 1개 = PR 1개). ⚠️ **결정 3의 무효 조건을 산문에 두지 않고 원장 참조 파일 열에 연결한 것이 이번 설계의 요점**이다 — ADR-0014 결정 1이 같은 형태의 조건을 **ADR 산문 안에만** 적어 PR #73 리뷰 major를 받았고(원장 28t가 인수), 이 저장소는 같은 형태로 **세 번 조용히 미발화**했다(26af·26x·26ae/26ak). ⚠️ **결정 5에서 오디오와 갈린 지점**: ADR-0014는 `fe.md`의 수동 게이트가 「**스크린샷** — 레이아웃·스타일 회귀」라 오디오에 안 맞아 **새 칸을 만들어야** 했는데, 모델은 시각이라 **그 게이트가 그대로 맞는다**. 같은 3층 분리를 쓰되 증거 형태만 다르다. ⚠️ **부수 정정**: `requirements.md` 제목의 버전이 **v2.4에 멈춰 있었다**(최신 행은 v2.5) — 제목은 `e01c3c8`까지 갱신돼 왔으므로 **v2.5 라운드가 빠뜨린 드리프트**다. v2.6으로 함께 고쳤다 |
| 2026-08-10 | **`main`에 브랜치 보호를 걸고, 머지 절차를 실행체에 박았다** — **① 브랜치 보호 설정**(사용자 결정 「gate만 필수 체크로」): `gate` 필수 검사 · `enforce_admins` 켬 · force-push·삭제 차단 · **리뷰 승인 요구 없음**. **② 머지 절차를 `.claude/skills/review-gate/SKILL.md`(실행체)로** — `gh pr view --json headRefOid`로 머지 대상 SHA를 얻고 `gh run list -q select(.headSha==$SHA)`로 `completed·success`를 **기계가 대조**한다. `--auto`·`--watch` 금지. `review-gate.md`에는 규범 한 줄만 남겼다(그 문서가 스스로 「절차의 정본은 SKILL」이라 정해 놨다). **③ 사실 오진술 정정** — `review-gate.md:15`가 브랜치 보호를 **있다고 단언**하고 있었고, 「미완」 표기가 **다섯 곳**(17b·sensor-catalog·README·ONBOARDING·changelog)에 흩어져 3주간 아무도 집지 않았다. 살아 있는 네 곳을 전환했다. 원장 **28af** 신설·완료 | .claude/skills/review-gate/SKILL.md, harness/workflow/review-gate.md, harness/{progress,sensor-catalog,README}.md, docs/ONBOARDING.md | **PR #76에서 실제로 뚫렸다.** 실측 `404 Branch not protected` + 저장소 설정 **`allow_auto_merge=false`** — 두 조건이 겹쳐 `--auto`가 auto-merge로 등록되지 못하고 **즉시 머지로 퇴화**해 **CI 완료 35초 전에** 머지됐다(머지 13:09:07Z, run 완료 13:09:42Z. 사후 success라 피해 0). ADR-0012의 「CI 선행」은 RQ-18에서 **리뷰 APPROVE 후 CI가 두 번 뒤집은** 실측 위에 세워진 규칙인데 낱말 하나로 빠졌다. ⚠️ **`--watch`도 같은 라운드에 뚫렸다** — 새 커밋 push 직후 **이미 끝나 있던 옛 run**을 보고 즉시 종료한다(옛 run 12:48:39Z 완료, 새 run 13:07:18Z 생성). ⚠️ **PR #77 리뷰가 처방 자신의 결함 셋을 잡았다**: 초안이 절차를 **명세에만** 넣어 실행 경로가 안 바뀌었고(PR #25와 같은 형태), 처방한 `gh pr checks`는 **headSha를 출력하지 않아** 대조가 불가능했으며, 「어느 문서에도 없다」는 서술이 **거짓**이었다. ⚠️ **남는 비대칭을 명시한다**: 이제 **CI는 기계가 강제하지만 reviewer APPROVE는 여전히 규율**이다 — 승인 요구를 일부러 걸지 않았다(솔로 체제). 2026-07-21 「리뷰 게이트 스캐폴딩」 행이 「⚠️ **미완**: GitHub 브랜치 보호(status check 필수)는 아직 설정하지 않아, 지금 이 게이트는 규율로만 지켜진다」고 적어 뒀고 **3주 뒤 그 미완이 실제 사고로 나타났다** |
| 2026-08-10 | **ADR-0014 결정 5 신설 — 오디오 계층의 검증 방식** | `harness/adr/0014-audio-authority.md`, `src/client/audio/`, `src/client/scene/PlayerControls.tsx`, `tests/unit/rq-72-*.test.ts`, `src/shared/config/audio-tuning.ts`(신규) | ⚠️ **ADR-0008 §6은 오디오를 면제한 적이 없다** — 문면이 「렌더링(R3F) — WebGL 픽셀·씬 그래프」뿐이고 `harness/`의 어느 문서에도 「오디오」가 **0건**이다(실측: `0008`·`fe.md`·`tdd.md`·`review-gate.md`). 그런데 RQ-72 2/2-b 라운드(원장 24bv)가 「§6 **렌더·오디오** 계층 면제」를 소스·테스트 docblock **일곱 곳**에 계약처럼 적고 **그것을 근거로 재생 배선 테스트를 0건**으로 뒀다 — **없는 면제를 인용한 것**이다(PR #76 리뷰 major 1). 결정 5가 그 공백을 메운다: 오디오를 **판정·합성·재생 배선** 세 층으로 갈라 앞의 둘은 **순수 함수 + 값 단언(면제 없음)**, **재생 배선만** 단위 테스트를 면제하되 **그 대가로 청취 증거 5항목**을 요구한다. `fe.md`의 수동 확인 칸이 「**스크린샷** — 레이아웃·스타일 회귀」라 오디오에 맞지 않으므로 「확인 안 함」으로 갈음할 수 없음도 명시했다. ⚠️ **인용 정정이 한 번에 안 끝났다** — 1차에 7곳을 고쳤다고 보고했으나 델타 재평가가 **2곳 누락**을 잡았다(오케스트레이터의 grep이 「§6」과 「오디오」를 **같은 줄**에서 찾아 줄바꿈으로 갈린 인용을 못 봤다). 함께: 스펙 근거 없는 튜닝값을 `constants.ts`에서 `config/audio-tuning.ts`로 이전(선례 `combat-tuning.ts` — 위임된 튜닝 슬롯) |
| 2026-08-09 | **스펙 미러 등록부에 `AUDIO` 2건 추가 + `WORLD.SIZE_M` 패턴 과탐 수정** | `.claude/hooks/gate_spec_mirror.py`, `harness/sensor-catalog.md` | RQ-72 구현 라운드(원장 24bm)가 `AUDIO.AUDIBLE_RANGE_M`·`FOOTSTEP_STRIDE_M`을 신설했는데, **ADR-0014 확정값 표**가 그 수치를 인용하면서 등록부에 없어 어긋나도 못 잡는 상태였다(원장 28l·28r·28s가 같은 형태로 세 번 발생). 등록부 5→**7**. ⚠️ **초안이 「RQ-72 본문도 같은 수치를 적으니」를 근거에 넣었으나 틀렸다** — 이 게이트는 `requirements.md`를 **읽지 않는다**(제외 목록). 등재가 실제로 덮는 것은 ADR-0014 확정값 표 **두 줄뿐**이고, `done` 골든의 산문 인용은 패턴이 파이프를 요구해 **여전히 무방비**다(원장 **28w**). ⚠️ **골든 12건을 `done`으로 전환하자 기존 패턴의 과탐이 드러났다** — `WORLD.SIZE_M`의 `A×Bm`이 GA-85의 「19×0.2m」(체공 19틱 × 틱당 0.2m)를 플레이 면적 주장으로 오해해 **하드 실패**시켰다. 역참조 `(\d+)×\1m`로 좁혔고(플레이 면적은 정사각형), 정본을 70으로 변이시켜 **게이트 대상 2곳**(`0007`의 확정값 표 · 골든 GA-50)에서 **2건 전부** 불일치로 보고되는 것을 확인했다. ⚠️ **초안이 「ADR 3곳 실측」이라 적었으나 그중 둘**(`0013:139` 본문 산문 · `adr/README.md:17` 인덱스 표)**은 확정값 블록 밖이라 원래부터 스캔 대상이 아니다** — 축소 때문에 놓친 것이 아니라 내가 게이트의 스캔 범위를 넘겨 세었다(PR #74 리뷰 major). 같은 과탐 축을 `SAFE_ZONE_RADIUS_M`의 「반경 <수>m」도 갖는다는 것을 코드 주석에 남겼다. 카탈로그의 「상수 30개 중 5개」는 실측 32/7로 낡아 **수치를 걷어냈다**(28r·28s와 같은 처방) |
| 2026-08-09 | **ADR-0014 신설 — 오디오 스택과 발소리의 권위** + `sensor-catalog`에서 **휘발성 수치 2개 제거**(원장 28r 회수) | `harness/adr/0014-audio-authority.md`(신규), `harness/adr/README.md`, `harness/specs/requirements.md`(RQ-72 신설, v2.5), `harness/evals/golden/track-a-product.jsonl`(RQ-72 골든 신규 등재, 사용자 확정 — ⚠️ **건수를 여기 적지 않는다**: 「GA-77~84」→「GA-77~87 11건」으로 두 번 고쳤는데 두 번 다 같은 커밋 안에서 다시 늘어 낡았다. **대상 열은 어떤 파일인지를 적는 자리이고 몇 건인지는 그 파일이 답한다**(원장 28r·28s와 같은 처방)), `harness/sensor-catalog.md`, `.claude/hooks/gate_golden_backref.py`(docblock 주석만 — 낡은 92% 근거 정정, 실행 코드 무변경) | 저장소에 오디오 축이 **0건**인 상태에서 발소리를 넣으려면 권위·에셋·수치 세 축을 먼저 정해야 한다. 결정 — **클라 로컬 판정·합성**(⚠️ 초안이 「RQ-61의 명시적 예외」로 적었으나 **틀렸다** — CLAUDE.md 위계상 ADR이 RQ에 예외를 줄 수 없고, 애초에 발소리는 게임 상태가 아니라 **표현**이라 RQ-63·70·71과 같은 층이다. 가청 판정을 클라에 두는 근거는 `@filter` 0건이라 정보 누출이 새로 생기지 않는다는 것 하나이며, 위치 필터링 도입 시 **무효**임을 원장 **28t**가 검출기 경로와 함께 소유한다) · **절차적 합성**(에셋 파일 0개 — ⚠️ 출처 게이트의 `ASSET_SUFFIXES`가 오디오를 안 봐서 커밋해도 무검사인데, 파일을 안 두면 그 공백을 건드리지 않는다) · **이동 거리 기준**(ADR-0008 결정론). 카탈로그 정리는 원장 **28r**의 트리거(`harness/**/*.md`에 표 증감)가 **이 PR로 도래**해 함께 처리했다 — 낡은 수치를 고치는 대신 **수치 자체를 걷어냈다**. 한 라운드 만에 표 45→46→49로 두 번 움직인 값이라 고정하면 또 낡는다 |
| 2026-08-09 | **골든 역참조 게이트 — `todo` 승격 + 경고 0건 달성** — ⚠️ **직전 라운드가 세운 게이트가 자기 동기(GA-52)를 못 잡았다**(원장 28g 독립 평가 blocker). GA-52는 존재한 적 없는 파일을 가리킨 채 `status: todo`로 **약 10일 체류**했는데 `todo`가 스캔 대상이 아니었다. **사용자 결정으로 `todo` + 깨진 `verify`를 경고로 승격**해 그 축을 닫는다(하드 실패 아님 — 「실패하는 소음 게이트는 꺼진다」). ⚠️ **`verify` 키 부재·빈 문자열은 스킵 불변** — 작성 중 골든의 정상 상태이고 건드리면 **트랙 B 7건이 전부 경고로 튄다**. **승격의 현재 영향은 0**(`todo` 골든 0건 실측)이고 정하는 것은 앞으로의 관행이다. 함께: **역참조 경고 6건 → 0건**(`it()` 이름에 `GA-NN:` 접두 — ⚠️ **GA-52는 `then`이 「사다리 2개」와 「클러스터 3곳」의 연언이라 두 곳에 붙였다**, 하나만 붙이면 절반의 역참조가 거짓이 된다) · **원장 28l**(`sensor-catalog`의 스펙 미러 게이트 행이 상수를 29로 적었으나 실측 30). | .claude/hooks/gate_golden_backref.py, tests/gates/test_golden_backref_gate.py, tests/unit/22b-aim-camera-binding.test.ts, tests/unit/map-box-dimensions.test.ts, tests/integration/rq-41-slot-promotion.test.ts, tests/integration/rq-92-crouch-stance.test.ts, scripts/check.sh, harness/sensor-catalog.md | 원장 28i·28l. ⚠️ **직전 행(28g)이 「승격은 미결이다」로 끝나 있었다** — append-only라 그 문장을 고치지 않고 이 행이 결론을 잇는다 |
| 2026-08-09 | **골든 역참조 게이트 신설 + ADR-0008 결정 3 개정** — `status: done` 골든의 `verify` **경로 실재**(하드 실패)·**자기 GA-ID 포함**(경고)·**스캔 건수 0**(하드 실패)을 검사한다. ⚠️ **ADR-0008 결정 3에 예외 절을 신설했다** — 하네스 도구(게이트) 테스트는 **pytest·`tests/gates/`**, RQ-ID 명명 비적용. **이 저장소 최초의 두 번째 러너이자 파이썬 서드파티 의존**이다(CI에 `pip install pytest`). 근거: 게이트의 `--selftest`는 **구현자가 쓰므로 자기 채점**이고, 파이썬 대상에 Vitest를 붙일 수 없어 독립 세션의 검증 층이 필요하다(CLAUDE.md 검증 격리). ⚠️ **예외는 하네스 도구에만** — 제품 코드(`src/`)는 여전히 Vitest 하나다. `docs/ONBOARDING.md` 2절에 `pip install pytest`를 넣고 `check.sh`에 **없으면 시끄럽게 죽는 런타임 가드**를 뒀다(조용히 건너뛰면 검증 층이 통째로 빠진 채 초록이 난다). | .claude/hooks/gate_golden_backref.py, .github/workflows/ci.yml, `.claude/hooks/gate_golden_backref.py`, `.github/workflows/ci.yml`, `harness/sensor-catalog.md`, `scripts/check.sh`, `tests/gates/test_golden_backref_gate.py`, docs/ONBOARDING.md, harness/adr/0008-test-strategy.md, scripts/check.sh, tests/gates/ | 원장 28g. ⚠️ **이 게이트는 그 동기가 된 GA-52를 못 잡는다** — GA-52는 줄곧 `status: todo`였고 `todo`는 스캔 대상이 아니다(독립 평가가 blocker로 잡았다). 게이트가 막는 것은 **`done` 전환 시점**의 깨진 경로다. `todo`를 경고로 승격할지는 **미결**이다 |
| 2026-08-08 | **하네스 후속 3건 — 장부↔현실 드리프트 정정** — ① **GA-52** `verify`가 **존재하지 않는 파일**을 가리켰다(`tests/integration/rq-32-map-volumes.test.ts`) → 실제 담당 `tests/unit/map-box-dimensions.test.ts`로 정정 + `status: done`. `given`/`when`도 테스트가 실제로 하는 일에 맞게 개정했다(파싱 경로 공백은 **원장 28f**로 분리). ② **`check.sh` 안내** — 「`nvm use` 또는 `fnm use`」까지만 적혀 있어 **셸 통합이 없는 비대화형 세션(에이전트)에서 통하지 않았다**(`fnm exec -- npm`은 Windows에서 스폰 실패). PATH 우회·`fnm install`·OS 한정을 추가했고 **리뷰어가 직접 따라 해 둘 다 통한다를 확인**했다. ③ **원장 28a 보강** — 원인은 규명돼 있었으나 「그래서 어떻게 22로 바꾸는가」가 비어 있어 **최소 세 에이전트 세션이 각자 재발견**했다. | harness/evals/golden/track-a-product.jsonl, scripts/check.sh, harness/progress.md | 원장 28e. ⚠️ **이 세 건은 파이프라인이 구조적으로 못 잡는 종류**다 — Red→Green→평가→리뷰는 **코드의 옳음**을 보지 **원장이 현실을 반영하는지**는 안 본다. 다음 라운드 후보를 조사하다 **우연히** 발견했고, 그 우연에 기대지 않기 위한 기계 대응을 **원장 28g**(골든 역참조 게이트 — 리뷰어 실측상 76건 중 역참조 부재 6건이라 소음이 낮다)로 등재했다 |
| 2026-08-07 | **골든 쓰기 권한 개방 + 낡은 서술 2건 정정** — 사용자 결정으로 `.claude/settings.json`의 `ask: Edit(./harness/evals/golden/**)` 규칙을 **제거**했다(에이전트가 골든 파일을 묻지 않고 쓴다). ⚠️ **열린 것은 쓰기 권한이지 저자 자격이 아니다** — 상태 전환·`verify` 채우기·형식·**사용자가 결정한 수치의 전사**는 묻지 않고 하되, **새 판정 기준을 창작할 때는 given/when/then을 사용자에게 보인 뒤** 진행한다(막지 않고 보이게만 둔다). 골든이 평가의 정답지라 정답과 구현을 같은 주체가 쓰면 평가가 자기 채점이 되기 때문이다. 함께 정정한 낡은 서술 2건: ① `tdd-workflow` 스킬이 "ask 게이트는 **아직 없다**"고 적었으나 **2026-07-26부터 있었다**(원장 17g) ② `evals/README.md`의 강제 수단 절을 이력으로 남기고 제거 사실을 명시했다. ⚠️ **Bash 우회는 그대로다** — `Edit`/`Write` 규칙은 셸 리다이렉트·python heredoc을 보지 못하고, 실제로 이 세션이 GA-64~68을 그 경로로 등재해 게이트가 발화하지 않았다(README가 이미 정직한 한계로 문서화해 둔 것과 일치). | .claude/settings.json, harness/evals/README.md, .claude/skills/tdd-workflow/SKILL.md, harness/progress.md | 원장 24ax. 게이트가 있던 기간의 실측 서술은 **삭제하지 않고 이력으로 보존**했다 — 되돌릴 때 다시 유효하다 |
| 2026-08-04 | **정션 관통 삭제 3회차 — `tdd.md` Phase 3에 "해제 검증" 단계 신설 + 기본 경로를 `npm ci`로 전환** — RQ-90 라운드에서 메인 트리 `node_modules`가 다시 비었다(항목 0개, `npm ci`로 293개 복구, 추적 파일 무손상). **더미 타깃으로 메커니즘을 재현해 증명했다**: `victim/KEEPME.txt`를 넣고 정션을 건 뒤 ① `Remove-Item <정션> -Force`(`-Recurse` **없이**) → NonInteractive 프롬프트로 **실패**, 정션 생존 ② 이어서 `git worktree remove --force` → **타깃 디렉터리는 남고 내용물만 0개**. 사고 현장에서 관측한 `node_modules` 상태와 정확히 일치했다. 해제 방법별 안전성도 실측표로 등재(`-Recurse` 있는 `Remove-Item`·`[IO.Directory]::Delete(p,$false)`·`(Get-Item).Delete()`는 링크만 제거하고 타깃 보존) | `harness/workflow/tdd.md` Phase 3 | **이전 두 번의 수정이 빗나간 이유가 드러났다.** 1·2회차 수정은 모두 *어떤 명령을 쓰는가*를 고쳤는데(→ `rm -rf` 금지 → `cygpath -w`), 3회차는 **문서의 순서 규칙을 지켰는데도** 났다 — 해제(③)와 제거(④) 사이에 **"③이 성공했는가"를 묻는 단계가 없어** 조용한 실패가 그대로 통과했다. 그래서 이번엔 명령 이름이 아니라 **검증 단계**를 넣고, 기본 경로 자체를 정션 없는 `npm ci`로 바꿨다(실측표의 어느 위험 칸도 밟지 않는다). 사고 세션의 원문 명령을 그대로 받아 재현한 뒤 고쳤다 — 추정으로 고친 두 번이 모두 재발했기 때문이다 |
| 2026-08-04 | **정션 사고 4회차 — 이번엔 “남의 워크트리를 지웠다”** | `harness/workflow/tdd.md` Phase 3 격리 워크트리 절 | **PR #54 라운드 중 발생, 원장 25a-9.** 앞선 두 2026-08-04 행이 정션 사고를 “고쳤다”고 적었으나 **네 번째가 났고 이번엔 유형이 다르다.** 1~3회차는 전부 “**어떻게** 지우는가”의 문제였다 — `rm -rf`(1회차) · `git worktree remove --force`(2회차) · 그 수정 자체가 슬래시 경로 파싱 실패로 실행 안 됨(3회차). **4회차는 “무엇을 지우는가”의 문제다**: 한 세션이 `git worktree list`에서 본 낯선 경로를 “하네스가 내 경로를 별칭 처리한 것”으로 오판해 지웠는데 **다른 세션의 워크트리**였고, 그 안의 `node_modules` 정션이 메인 트리를 가리켜 메인이 비었다(`npm ci` 복구, 추적 파일 무손상, `npm ls` 결손 0으로 오케스트레이터 재확인). ⚠️ **순서 규정(`cygpath -w` 선해제 → 워크트리 제거)을 완벽히 지켜도 대상이 남의 것이면 그대로 사고다** — 기존 수정은 이 실패 모드를 원리적으로 막지 못한다. **수정**: ① 표준 절차 경로를 `wt-<에이전트이름>-<라운드>`로 바꿔 **경로에 소유자를 새기고** ② 삭제 전 `git worktree list`로 **소유 확인** 단계를 절차에 넣고 ③ “자기가 `add`한 경로에만 `remove`를 쓴다, 모르는 경로는 지우지 말고 두어라 — 남이 실험 중일 수 있다”를 명문화했다. 당사자 두 세션이 원인을 스스로 규명·인정하고 같은 재발 방지책을 서로 합의했으며, 그 내용이 이 수정의 입력이다 |
| 2026-08-04 | **정션 해제 명령 정정 — 앞 행의 수정이 실행되지 않았다(같은 결함 3회차)** | `harness/workflow/tdd.md` Phase 3 코드 블록 | **PR #50 델타 리뷰 major 2.** 바로 아래 2026-08-04 행이 기록한 수정(슬래시 경로를 쓰는 `cmd //c rmdir` 형태)이 **실행되지 않는다** — `cmd.exe`의 빌트인 `rmdir`은 슬래시를 **스위치 접두사**로 파싱해 “매개 변수가 틀립니다”로 실패하고 **정션이 그대로 남는다**. 즉 “읽히지 않던 안전 절차”가 “**읽히지만 실행되지 않는** 안전 절차”가 됐다. 실패 모드가 더 나쁠 수 있다 — 구문 에러를 본 사람이 “이미 지워졌나 보다” 하고 `git worktree remove --force`로 넘어가면 그게 정확히 2회차 사고 경로다. 앞 행의 “경로를 슬래시로 바꿔 재발을 막았다”는 **사실이 아니었다**(append-only 원칙에 따라 그 행은 고치지 않고 여기서 정정한다). **수정**: 경로를 `cygpath -w`로 변환해 넘긴다 — 그러면 ⓐ 문서에 리터럴 역슬래시를 두지 않아 역슬래시-n이 개행으로 먹히는 저작 사고가 **구조적으로 재발 불가**하고 ⓑ cmd가 정상 파싱한다. 워크트리 경로를 변수로 한 곳에서만 정의하게 바꿔 치환 실수도 줄였다. **검증**: 리뷰어가 실제 정션을 만들어 3형태를 실행 대조했다(슬래시 exit 1·정션 잔존 / 혼합 exit 1 / 전부 역슬래시 exit 0·대상 무손상). 오케스트레이터는 **파싱 실패만 독립 재현**했다 — 슬래시 형태는 “매개 변수가 틀립니다 - workspace”(스위치로 파싱), `cygpath` 형태는 “지정된 경로를 찾을 수 없습니다”(정상 파싱)로 **오류 종류가 다르다**. 셸에서 정션 생성이 실패해 종단 확인은 하지 못했고 그 사실을 숨기지 않는다. ⚠️ **같은 결함의 3회차다** — 1회차 `rm -rf`(2026-07-28), 2회차 `git worktree remove --force`(2026-08-04), 3회차는 **그 수정 자체**. 매번 “고쳤다”고 적었고 매번 다음 사람이 걸렸다. 정션을 아예 만들지 않고 `npm ci`로 재설치(약 10초)하는 쪽이 구조적으로 더 안전하다는 것을 문서에 병기했다 |
| 2026-08-04 | **정션 경고를 명령 이름이 아니라 위험 자체로 다시 씀 — 같은 사고 2회차** | `harness/workflow/tdd.md` Phase 3 격리 워크트리 절 | **PR #50 리뷰 major 2.** 경고가 “정션을 **rm -rf로** 지우지 마라”로 **금지 명령 하나를 지목**하고 있어, 이번에 `git worktree remove --force`가 같은 일을 했을 때 빗나갔다(메인 트리 `node_modules`가 통째로 비워짐, `npm ci`로 복구, 추적 파일 무손상). 2026-07-28 RQ-61 라운드의 `rm -rf` 사고에 이은 **2회차**다. 더 나빴던 것은 **표준 절차 코드 블록 자체가 정션 해제 없이 `git worktree remove --force`만 담고 있었다**는 점 — 문서가 권장하는 경로를 그대로 따르면 사고가 났다. 안전 절차는 있었으나 “rm -rf를 쓸 때만” 읽히는 위치였다. **수정**: ① 표준 절차 블록에 정션 해제 줄을 넣어 **기본 경로 자체를 안전하게** 만들고 ② 경고 표제를 **“순서를 바꾸지 마라 — 정션을 먼저 해제한 뒤에 워크트리를 지운다”**로 바꿔 위험을 명령이 아니라 **행위**로 규정했다. ⚠️ **부수 발견**: 기존 안전 절차 코드 블록이 **실제로 깨져 있었다** — 경로의 역슬래시-n이 개행으로 들어가 블록이 두 줄로 갈라져 있었고, 읽히지 않는 안전 절차였다. 경로를 슬래시로 바꿔 재발을 막았다. ⚠️ **이 행을 쓰다 같은 버그를 재현했다** — 초안이 역슬래시 이스케이프 때문에 두 줄로 쪼개졌고 **원장 표 게이트가 즉시 잡았다**(열 수 부족 + 선행 파이프 없는 행). 게이트가 없었다면 changelog가 조용히 깨진 채 머지됐을 것이다. **이월로 두지 않은 이유(리뷰어 논거)**: 이 항목의 착수 트리거는 “다음에 누가 워크트리를 지울 때”인데 그 시점은 예고 없이 오고 **이미 두 번 왔다** — 트리거를 적을 수 없는 이월은 이월이 아니라 3회차 예약이다 |
| 2026-08-03 | **ADR-0012 조건 2 개정 — 임계 2 폐기 · 임계 3 면제 확대 · 배수 기전 미도입** | `harness/adr/0012-review-tail-slim.md` | **원장 26an·26au·26aw 회수(사용자 결정).** ① **임계 2 폐기** — 협소화(PR #40) 이후 **#41·#42·#43·#44 네 라운드 연속 미측정**이었다. 도래 시점이 행에 기록돼 있지 않아 매 라운드 산문 전수 독해가 필요한데 그 비용을 지불하지 않기로 했다(원리적 불가가 **아니다** — 26aw 수정안 ②에 전환 경로가 있다). 매 라운드 "미측정"만 적히는 것은 신호가 아니라 소음이고 **없는 임계가 있는 척하는 임계보다 낫다**. 남는 구멍을 ADR에 명시했다 — 근사를 제공하는 `gate_trigger_due.py`는 **행위형 트리거를 못 본다**. ② **임계 3 면제 확대** — 판정을 **정정하는** 행위도 면제(원장을 정확하게 만드는 행위가 지표를 악화시키던 역설 제거). 회피는 **판정 절 경계를 명문화**해 막았다(시작=굵은 표지, 끝=다음 굵은 표지 직전). ③ **배수 기전 미도입** — ⚠️ 초안이 "부채는 PR #39 한 라운드(+81)에서 왔다"고 적었으나 **거짓이었고 리뷰가 실측 반증**했다(`+81`은 14라운드 누적, #39 자체는 −1). 정정된 실측은 **라운드당 평균 +4.5·최대 +14의 지속 누적**이다. 그럼에도 유지한 이유: 순증이 큰 라운드가 **전부 기능·스펙 라운드**라 **검증이 제 일을 한 결과**이고, 기전으로 억제하면 검증을 줄이는 압력이 생긴다 |
| 2026-08-02 | **스펙 미러 게이트·트리거 발화 검출기 신설 + 원장 표 게이트 커버리지 30배 + ADR-0012 개정 2건** | `.claude/hooks/{gate_spec_mirror,gate_trigger_due}.py`(신설), `.claude/hooks/gate_ledger_table.py`, `harness/adr/0012-review-tail-slim.md`, `scripts/check.sh`, `.github/workflows/ci.yml` | **원장 26af·26x·26al·26ae·26ak·25m 회수 + 26am 신설(7행), 26an 등재.** 25m은 **트리거 검출기가 자기 첫 실전에서 잡아낸 것**이다 — 그 행이 경고한 피해(메인 트리 변이 실험 → 복원이 정당한 미커밋 편집을 삭제)가 같은 라운드에 실제로 났고, `tdd.md`에 단일 세션 사고를 두 번째 이유로 명문화했다. ① **스펙 미러 게이트** — 문서가 인용한 수치를 정본과 대조한다. 정본이 세 종류(`constants.ts` 값 · 게이트 `--selftest` 실행 출력 · `sensor-catalog.md`의 ✅ 계수)라는 것이 설계의 핵심이며, 뒤 둘은 코드를 돌리거나 세어야 알 수 있어 상수만 훑는 검사로는 안 잡힌다. **배선 첫 실행에서 즉시 1건**(같은 라운드가 바꾼 selftest 수치의 낡은 인용)을 잡았다. ② **트리거 발화 검출기** — 이월 트리거가 **세 번 조용히 미발화한** 근본 원인에 대응한다. ⬜ 행의 참조 파일과 변경 경로를 대조해 알리며 **항상 exit 0**(실패하는 소음 게이트는 꺼진다). ③ **원장 표 게이트 커버리지** `progress.md` 1파일 → `harness/**/*.md` **30파일**. 확장 즉시 승인 ADR-0004의 RQ-92 확정값 표 실파손 1건 검출·수정. 정밀도 잔여 5건(선행 파이프 없는 행·코드 펜스·6열 부록 표 오탐 문서화·최소 표 수·구분선 파손 시 진단 폭주 138건→1건) 전부 처리. 코드 스팬 검사를 표 행 문맥으로 한정(확장 직후 블록쿼트 산문에서 낸 오탐 제거). ④ **ADR-0012 결정 3** — 심각도 축과 수정 등급 축이 `major+(b)`에서 정반대를 지시하던 모순을 **조합표**로 해소(등급 축이 재평가를, 심각도 축이 재리뷰를 정한다). 미러 3곳 동행 갱신. ⑤ **ADR-0012 조건 1·2** — 「트리거는 등재된 행 ID를 가리켜야 한다」 신설, 「원장 정정 커밋」 세는 규칙 신설(사용자 결정), 임계 2를 "트리거 도래 후"로 좁힘. **정직한 기록**: `changelog.md`를 미러 게이트 대상에서 **뺐다** — append-only 기록이라 옛 수치가 그 시점의 사실이고 오늘 값으로 고치면 이력 위조다. 대신 시점 병기 규칙을 세웠다 |
| 2026-07-30 | **원장 표 무결성 게이트 신설 + 파손 7행 복구 + 오진 3건 정정** | `.claude/hooks/gate_ledger_table.py`(신설), `harness/progress.md`(7행 복구 + 26l·26ag 정정), `scripts/check.sh`·`.github/workflows/ci.yml`·`.claude/settings.json`(배선), `harness/sensor-catalog.md` | **표가 깨져도 아무것도 실패하지 않았다** — 7행이 깨진 채 CI를 통과했고, `28a`·`22f`는 GitHub 렌더에서 비고가 **잘려 사라졌으며**, `26l`은 이스케이프 파이프가 상태 열 **앞**에 있어 ⬜ 집계를 97로 만들었다(참값 98). 그 수치가 ADR-0012 조건 2의 재검토 임계를 가르므로 표시 문제가 아니라 집계 문제다. **규율로는 안 막힌다는 증거**: 이 결함을 고치고 있던 2026-07-29 라운드에서 오케스트레이터가 절댓값 기호를 코드 스팬에 넣어 `26s` 행을 새로 깨뜨렸다 — 새 훅은 그 패턴을 잡는다. ⚠️ **앞선 진단 3건이 틀렸고 함께 정정했다**(GitHub 실제 렌더러 실측): ① "12행 깨짐"은 과대 계상, 실제는 7행이며 4행은 **naive 파이프 분할의 오탐**이었다 ② "`21a-2`·`22u`·`22z5`가 같은 원인"은 정반대(이미 이스케이프돼 있었다) ③ **"`&#124;`로 치환한다"는 거짓** — 백슬래시 하나가 코드 스팬 안에서도 정상 동작하고, 이중 백슬래시는 백슬래시가 노출되며 `&#124;`는 코드 스팬 안에서 디코딩되지 않는다. 게이트가 **이스케이프-인식 분할**을 쓰는 이유가 바로 그 오탐이다. 함께 `sensor-catalog.md`의 등재 누락(맵 에셋 출처 게이트)과 낡은 개수("✅ 8행/막는 것 3개" → **2026-07-30 실측** 11행/5개)를 정정했다 — 그 문서가 스스로 "Sensor 현황의 정본"이라 선언한다 |
| 2026-07-29 | **맵 에셋 출처 게이트 신설 + 골든 GA-50~55 + RQ-21 개정(v1.4)** | `.claude/hooks/gate_map_asset_provenance.py`(신설), `.claude/settings.json`(배선), `harness/evals/golden/track-a-product.jsonl`, `harness/specs/requirements.md` | 로드맵 23·24·25가 **골든 0건으로 막혀** 파이프라인이 Phase 0에서 멈추던 병목을 맵 쪽에서 뚫었다. **Dust2 미반입은 골든이 아니라 게이트로** 갔다 — 부재의 증명이라 `when`이 비어 버리므로 커밋 시점 차단이 맞다. **두 층으로 배선했다** — PreToolUse 훅(쓰기 전 경로) + CI `--check-paths`(커밋된 파일 내용). 훅만으로는 파일이 아직 없어 내용 검사가 성립하지 않는다. ⚠️ 실수·부주의만 막으며 리토폴로지·트레이싱은 검출 불가라는 한계를 훅 docblock에 명시했다. selftest 내장(차단 5·허용 5)이며 **작성 중 selftest가 정규식 결함을 실제로 잡았다**(`\b`가 `_` 뒤에서 안 걸림). RQ-21은 원문이 검증 명제를 세울 수 없을 만큼 짧아 개정했고 속도는 기존 상수에서 유도했다(값 발명 금지) |
| 2026-07-29 | **ADR-0013 신설 — 이동 정본과 충돌 계층의 분리** | `harness/adr/0013-movement-authority-layering.md`, `harness/adr/0004-physics-collision.md`(**결정 1·3 대체 + 결정 2 부분 대체** 표시), `harness/adr/0003-netcode-authority.md`(결과 절 대체 부기), `harness/adr/0007-map-asset-pipeline.md`(결정 3 잠정 표시), `harness/adr/README.md` | ADR-0004가 **자기모순**이었다 — 결정 절은 "서버가 **Rapier로** 이동을 매 틱 계산"인데 RQ-20 착수 시 붙은 인라인 노트는 "정본은 **순수 틱 함수**, Rapier는 충돌 해결 계층"이었다. 평지에서는 구별되지 않지만 **충돌이 들어오는 순간 갈라진다**. 원장 21a-1이 "인라인 노트 누적 대신 새 ADR로 승격"을 로드맵 8단계(맵) 착수의 선행 조건으로 등재해 뒀고, 그 트리거가 발화했다. **사용자 결정(2026-07-29): 순수 함수 정본 + Rapier는 질의만.** 근거 — 이동 관련 done 골든 15건과 `stepMovement` 의존 13파일을 살리고, 결정론을 엔진 내부가 아니라 구조로 보장하며, ADR-0004가 스스로 "미검증"이라 적은 성능 가정(Node에서 Rapier headless가 33.3ms 틱 예산 안에 10인 처리)에 대한 노출을 줄인다 |
| 2026-07-29 | **골든 GA-11의 `when` 확장 — 낙하 데미지 포함** | `harness/evals/golden/track-a-product.jsonl` | RQ-31 Safe Zone이 무기 피해만 막던 구현이 **스펙이 아니라 소스 주석에만** 근거를 두고 있었고, RQ-16("받는 **모든** 피해")과 자기모순이었다 — 스폰 3.000초까지는 세이프존 안 낙하 무피해, 3.001초부터는 같은 자리에서 피해. **사용자 결정(2026-07-28)으로 낙하도 막는 쪽으로 확정**하고 GA-11의 `when`을 "다른 플레이어의 공격" → "피해를 받음(무기 피해·낙하 데미지 포함)"으로 넓혔다. `given`·`then`·`spec`은 불변이며 **스펙 개정은 불요**했다(RQ-31 문면에 원래 한정어가 없다). 골든 문면 변경은 사람 승인 사안이라 여기 남긴다 — 원장 25f·25g |
| 2026-07-28 | **변이 실험 격리 워크트리 규약 신설 + 통합 실행 명령 정정.** 검출력 변이는 `git worktree add --detach` 위에서만 하고 끝나면 제거한다. 복원은 CRLF/LF에 따라 방법이 갈리며 sha256 동일 확인이 증거다. 아울러 `vitest.integration.config.ts`는 **존재하지 않는다**는 사실을 명시 — 이 저장소는 `vitest.config.ts` 하나로 단위·통합을 돌린다 | `harness/workflow/tdd.md` Phase 3 | RQ-61 라운드(원장 20e)에서 작업자와 평가자가 **메인 트리의 같은 파일에 동시에 변이**를 심어, 서로의 `git checkout --` 복원이 상대 실험을 무성으로 지웠다. 평가자가 파일 크기 변화를 실측해 자기 스위트 결과를 폐기하고 격리 워크트리에서 다시 돌렸다. 오케스트레이션 실수였고, 재발하면 조용히 잘못된 결론이 나온다. 실행 명령 오류도 같은 라운드에서 오케스트레이터가 반복해서 잘못된 config 경로를 지시한 데서 드러났다 |
| 2026-07-28 | **통합 워커 크래시 근본 원인 규명 — Node v24.** `engines: "^20.19.0 \|\| ^22.13.0"` + `.nvmrc`(22.23.1) 신설, CI를 `node-version-file: .nvmrc`로 전환. `scripts/check.sh`·ADR-0008 §5의 "근본 원인 미규명" 서술과 거짓 확률 주장을 정정 | scripts/check.sh, harness/adr/0008-test-strategy.md, package.json, .nvmrc, .github/workflows/ci.yml, harness/progress.md, harness/sensor-catalog.md, docs/ONBOARDING.md, **harness/infra/worker-crash-rca.md(신규 404줄 — 조사 스냅샷 승격)** | **세 라운드 연속 재시도 한도 소진**으로 착수. 격리 조사(런 116회·워커 3,514개·워커 9,300초·abort 29건)가 **네이티브 abort**(`0xC0000409` STATUS_STACK_BUFFER_OVERRUN)임을 확정하고 uncaught exception·unhandled rejection·`process.exit()`·`process.abort()`를 전부 배제했다. **살아 있는 Colyseus 클라이언트 세션이 필요조건**이며 (클라 없는 워크로드 1,204워커에서 abort 0), 풀 계층 조정은 전부 무효였다 — `no-file-parallelism`은 소요 4배인데도 남았고 `no-isolate`는 워커당 발생률을 2.8%→5.1%로 **악화**시켰다. 위험이 프로세스 토폴로지가 아니라 **한 프로세스의 Colyseus 작업량**에 비례한다. **문서가 위험을 약 2만 배 과소평가**하고 있었다: "~2%"는 런이 아니라 **워커 단위**였고, 파일 30개 기준 런 실패율은 **52.5%**(실측 21/40), 3연속 소진 확률은 14.5%다(**오늘 기준**). 그 문장이 쓰인 시점의 파일은 2개였고 당시 실제 3연속 확률은 0.0172%로 구주장 대비 21.5배 과소평가였다 — **파일이 2→30으로 늘면서 어긋난 것**이지 처음부터 크게 틀린 것은 아니다(독립 평가 정정). 런 실패율이 파일 수에 지수적이라 한도 초과 세 번은 이상 현상이 아니다. **런타임 A/B가 원인을 확정**했다(같은 머신·같은 코드): 실 통합 스위트에서 **v24.15.0은 누적 12런 중 6런 실패, v22.23.1은 8런 전부 통과**(매번 111/111). CI가 거의 항상 통과한 이유도 **CI가 Node 20을 썼기 때문**이며, 조사가 의심한 "Windows 특성"은 OS와 런타임이 교란된 관측이었다 — 다만 **CI 대조만으로는 둘을 분리하지 못한다**. 분리한 것은 같은 Windows 머신의 A/B이므로 **결론의 범위도 Windows 실측 기준**이며 `Linux + v24`는 미검증이다. **교훈**: 재시도 예산 상향(최후 수단)을 먼저 썼다면 원인을 영영 못 찾았을 것이다 — 파일 수가 늘수록 악화되는 구조라 은폐책은 시간이 갈수록 더 자주 뚫린다. 경위: `harness/progress.md` 28a, `harness/infra/worker-crash-rca.md` **함께: `check.sh` 런타임 가드 신설**(`harness/sensor-catalog.md` 등재) — npm `engines`는 경고일 뿐 강제가 아니고 이번 통증은 설치가 아니라 **테스트 시점**에 났으므로 전체 검증 진입점에서 Node major ≥ 24를 직접 차단한다. `--fast`는 제외 — 파일 저장마다 도는 5초 예산이고 통합을 돌리지 않아 이 위험에 노출되지 않는다(거기서 막으면 편집 흐름만 끊는 과차단). |
| 2026-07-26 | **ADR-0012 신설 — 검증 꼬리 간소화(CI 선행 · minor 기본 이월 · 델타 재검증 축소).** ① PR 생성 → **CI 통과 확인 → 리뷰 의뢰** 순서로 고정(CI가 빨간 상태에서 리뷰 시작 금지) ② **minor는 기본 이월** — in-PR 수정은 (a) 계약·스펙 위반 (b) 1줄급+회귀 위험 0일 때만. 리뷰어가 "즉시 조치 불요"로 판정한 항목은 **원장 행 초안(근거·수정안·착수 트리거)을 낸 경우에만** 자동 이월 ③ **§2(b)(1줄급·회귀 위험 0)만 반영한 경우에만 델타 재검증을 생략한다**(`npm run check` + CI로 종결) — 계약·스펙 위반 수정과 CI 실패 대응 수정은 **면제되지 않는다**. 반영 대상: `review-gate.md`(규칙 복제 대신 **ADR 포인터로 축약**)·`tdd.md`·`.claude/skills/{review-gate,tdd-workflow}/SKILL.md`·`.claude/agents/reviewer.md`·`harness/adr/README.md`. **분류 주체는 reviewer**이고 오케스트레이터는 더 가벼운 쪽으로 바꿀 수 없으며, 델타 재검증은 blocker·major **또는 (a) 또는 CI 실패 대응 수정**에 필요하다 | harness/adr/0012-review-tail-slim.md, harness/workflow/{review-gate,tdd}.md | **사용자 지시(2026-07-26) — "RQ 단계별 리뷰를 간소화해달라, 구현 기간이 오래 걸린다".** RQ-18 실측이 근거다: 검증 패스 9회 중 **핵심 파이프라인은 4회뿐이고 꼬리가 5회**였다. 리뷰 minor 8건을 분해하면 실질 결함 2건(minor 1 낙차 계약 위반, minor 5 RQ-16 가드 부재), **처방이 틀린 것 2건**(minor 3은 구현했다가 폐기 — 처방 자체가 minor 6과 같은 오류를 담고 있었다), 코드 변경 불요 4건. 즉 **minor 3은 일을 줄인 게 아니라 만들었다**. 결정적으로 **CI가 잡은 2건(run 30185861428·30188488491)은 로컬 게이트·독립 평가·독립 리뷰가 모두 통과시킨 것** — 비싼 추론 게이트가 놓친 것을 값싼 결정론 게이트가 잡았는데 순서는 반대였다(리뷰 APPROVE 후 CI가 두 번 뒤집어 그 리뷰가 헛일이 됐다). **격리 원칙·blocker/major 게이트·변이 테스트는 그대로 유지** — 줄인 것은 리뷰의 깊이가 아니라 횟수다. **감수하는 비용을 ADR에 명시**: minor로 잘못 분류된 실질 결함은 이 정책 아래 이월된다(minor 1이 실제로 그런 경우였다). 회수 경로는 원장이며, 이월만 쌓이고 회수되지 않으면 ADR을 재검토한다. **리뷰 REQUEST_CHANGES(blocker 5/major 6) 전부 반영** — 독립 리뷰가 이 정책 PR 자체에서 자기모순 3건을 잡았다: ① **실행체(SKILL) 미갱신**으로 정책이 발효되지 않는 상태였다(명세 `harness/workflow/`만 고치고 오케스트레이터가 실제로 읽는 SKILL은 그대로 — 이 저장소가 이미 여러 번 치른 패턴) ② **결정 2(a) ∩ 결정 3 자기모순** — 결정 3은 "minor는 회귀 위험이 낮은 것만 남는다"를 전제로 재검증을 면제했는데, (a)에는 회귀 위험 조건이 없어 **프로덕션 로직 변경이 무검증 머지**될 수 있었다(RQ-18 minor 1이 바로 그 형태 — 원장이 "오늘 결과 동일"로 기록한, 기존 테스트가 구분 못 하는 변경) → (a)·CI 대응 수정을 재검증 대상으로 복원 ③ **결정 4의 추론이 근거와 반대 방향** — "리뷰어 판정이 틀렸던 사례"를 근거로 "그러니 재심하지 말자"로 갔었다 → 리뷰어가 **원장 행 초안(착수 트리거 포함)**을 낸 경우로 자동 이월을 한정. 또 **꼬리의 최대 산출이 결산에서 빠졌던 것**도 지적도었다 — 최종 리뷰가 발견한 **제품 결함(22f 클라 점프 입력 유실)**이 "꼬리의 75%가 낭비"라는 그림을 만드는 표에서 생략돼 있었다. 그리고 **이월 회수 실적이 0**임이 실측돼(미해소 ⬜ 9건, 17e-1은 나흘째 미회수) 완화책을 가정이 아니라 **수치 임계로 박은 재검토 조건**으로 바꿈. **보류한 대안**: 평가·리뷰 통합(RQ 등급제) — 단축 효과는 가장 크나 22b에서 평가가 잡은 "가드 무용지물"을 리뷰는 못 잡았고 그 반대도 있어 두 시각이 실제로 보완 중이다 |
| 2026-07-26 | **하네스 부채 4건 정리 — 델타 리뷰 신규 blocker(4) + major(2) + minor(4~6) 대응, 3차.** 2차 라운드가 만든 `gate_coder_test_write.py` 자체가 새 blocker였다 — `agent_type=="coder" and is_test_path(...)` 한 줄이 coder의 `tests/` 쓰기를 **무조건** 차단해, ADR-0011 결정 3(test-after 영역에서 coder는 tests/ 순증(신규 파일·신규 it)을 만들 수 있음)과 정면 충돌했다(원장 22b가 실제로 이 경로를 탐). **[blocker 4] 판정 로직을 "coder의 tests/ 쓰기"에서 "coder가 tests/의 기존 단언에 접촉"으로 좁힘**: `Write`+신규 파일=허용, `Write`+기존 파일 덮어쓰기=차단, `Edit`/`MultiEdit`은 `old_string`이 `new_string`에 그대로 포함되는 순수 삽입만 허용, 그 외는 차단. 라이브 6케이스로 재실증(저장소 밖 임시 프로젝트, 실제 coder/test-writer/evaluator probe 에이전트로 진짜 Write/Edit 호출) — coder 신규 파일 Write 허용·coder 기존 파일 Write 차단(Read 선행으로 native 전제조건까지 충족시킨 뒤 확인)·coder 순수 삽입 Edit 허용·coder 치환 Edit 차단·test-writer 통과·evaluator 통과. `--selftest`도 같은 판정을 재현하되 실제 저장소 `tests/`는 건드리지 않도록 `tempfile.TemporaryDirectory()`로 ROOT를 일시 교체(이 과정에서 초기 버전이 `.resolve()` 누락으로 Windows 임시경로 별칭 때문에 실패하는 것을 실측으로 발견·수정). **[major 1] `.claude/agents/coder.md` 개정**: "테스트 파일을 절대 수정하지 않는다"를 Red-first 영역 한정으로 좁히고 test-after 영역의 순증 허용을 ADR-0011 원문대로 반영 — hook의 차단 메시지도 그 낡은 문장이 아니라 ADR-0011 결정 3을 직접 인용하도록 정정(진실 공급원 순서 ADR>에이전트 정의>hook 메시지 복원). **[major 2] `--selftest`를 CI(`ci.yml` 게이트 자체 검증 스텝)와 `scripts/check.sh`에 배선** — 이전엔 아무도 호출하지 않았다. **[minor 4]** 두 hook 스크립트(`gate_spec_freeze.py`·`gate_coder_test_write.py`)의 경로 정규화 로직 중복에 상호 참조 주석 추가("한쪽을 고치면 반대쪽도"). **[minor 5]** selftest의 evaluator 케이스 주석을 "실제로는 안 씀"에서 "tests/에 프로브를 만들 수 있으므로 반드시 통과해야 한다"로 정정. **[minor 6]** 차단 메시지에 "무엇이 허용되는지"(신규 파일·순수 삽입)까지 안내하도록 보강 | `.claude/hooks/gate_coder_test_write.py`, `.claude/hooks/gate_spec_freeze.py`(상호참조 주석만), `.claude/agents/coder.md`, `.github/workflows/ci.yml`, `scripts/check.sh`, `harness/progress.md`, `harness/sensor-catalog.md` | **정정 없이 만든 게이트가 파이프라인을 막을 뻔했다.** 2차 라운드에서 "역할 식별 가능"이라는 사실 확인까지는 옳았지만, 그 사실 위에 얹은 판정 로직의 **범위**를 승인 ADR과 대조하지 않았다 — "할 수 있다"와 "무엇을 막아야 하는가"는 다른 질문인데 후자를 검증 없이 전자로 치환했다. 이번엔 수정 전에 실제 coder 에이전트 정의로 6케이스를 라이브 재현해서만 반영했다(이전 라운드에서도 같은 패턴이 반복돼 왔다는 것 자체가 "라이브 재검증을 매 라운드 다시 하라"는 규칙의 근거) |
| 2026-07-26 | **하네스 부채 4건 정리 1차 — 리뷰 REQUEST_CHANGES로 뒤집힘(정정 이력 보존용, 아래 최신 항목이 실제 상태)** — 코드 로직 변경 없음. ~~① 17f: coder `tests/` 쓰기 차단 hook — 구현하지 않기로 결정. PreToolUse hook payload에 호출 주체를 식별하는 필드가 없다고 단정~~ **→ 거짓으로 판명(아래 리뷰 대응 항목 참고, `agent_type` 필드가 실제로 존재).** ~~② 17g: ask 게이트 3건(`Edit`·`Write`·`MultiEdit`) 추가, 검증은 JSON 문법 통과만~~ **→ 2건(`Write`·`MultiEdit`)은 Claude Code 2.1.220에서 무효인 죽은 설정으로 판명.** ③ 17h 주석 2건, ④ 21a-2 stale 정정은 그대로 유효(리뷰에서 "문제 없음" 확인). 상세 대응은 아래 최신 항목 참고 | `.claude/settings.json`, `src/server/index.ts`, `scripts/check.sh`, `harness/progress.md` | **이 항목 자체가 반면교사다.** "없는 강제 장치를 있다고 쓰지 마라"의 정확한 거울상("있는 능력을 없다고 확정 기록하기")을 저지르고, 스스로 "핵심 교훈을 남겼다"고 자평까지 했다 — 그 자평 자체가 틀린 실측 위에 서 있었다. 검증하지 않은 것을 단정한 결과이며, 격리 리뷰 세션이 라이브 실증으로 잡았다(리뷰 세션의 존재 이유가 실증된 사례) |
| 2026-07-26 | **하네스 부채 4건 정리 — 리뷰 REQUEST_CHANGES(blocker 3) 대응, 최종.** 위 1차 항목의 두 핵심 단정이 리뷰의 라이브 실증(저장소 밖 임시 프로젝트 + 중첩 `claude -p`)으로 뒤집혔고, 이 세션에서도 동일 방법으로 독립 재실증했다. **[blocker 1] 17f를 실제로 구현함**: PreToolUse hook stdin JSON에 서브에이전트 호출 시 `agent_type`(값=에이전트 frontmatter `name:`과 일치)이 실린다는 것을 4케이스 라이브로 확인(coder 차단·test-writer 통과·메인세션 통과·coder의 Bash 경유는 미차단) → `.claude/hooks/gate_coder_test_write.py` 신설(`gate_spec_freeze.py`와 동일 구조, `--selftest` 내장)하고 기존 PreToolUse 매처에 등록. **(이 판정 로직 자체 — `agent_type=="coder"`이면 tests/ 쓰기를 무조건 차단 — 은 다음 델타 라운드에서 ADR-0011과 충돌하는 blocker로 재차 뒤집혔다. 아래 최신 항목 참고.)** **[blocker 2] 17g의 `Write`·`MultiEdit` 규칙 2건 제거**: Claude Code 2.1.220이 이 두 규칙을 세션 시작마다 "무효" 경고하고(`MultiEdit`은 존재하지 않는 툴명, `Write`는 file permission 검사에 안 걸림), `Edit(경로)` 규칙 1건이 Write를 포함한 모든 파일 편집 툴을 커버함을 대조 실험(다른 경로 Write 통과 vs golden 경로 Write 차단)으로 확인 → `.claude/settings.json`을 `"ask": ["Edit(./harness/evals/golden/**)"]` 한 줄로 축소. **[blocker 3] 문서 모순 해소**: `harness/evals/README.md`·`sensor-catalog.md`가 여전히 "ask 게이트는 없다"고 단언하던 것을 실제 상태(Edit 1건 존재, Bash 경유는 못 막음)로 갱신, sensor-catalog 상태를 ✅가 아니라 🟡(부분)로 표기. **[minor 1~3]** check.sh 주석에 실제 grep 패턴 5개 전부 열거+삼중/이중 비대칭과 안전 실패 방향 명시, index.ts 주석의 RQ-80 인용을 스펙 원문("Redis 세션" 소실)으로 정정하고 RQ-81(통계 보존) 충돌 가능성 추가, 17f 행을 새 구현 내용으로 갱신(닫히는 게 아니라 잔존 리스크가 Bash 경유로 재정의됨). 후속 등재(17i, ⬜): golden 파일 Bash 경유 쓰기 차단은 이 PR 범위 밖 | `.claude/hooks/gate_coder_test_write.py`(신설), `.claude/settings.json`, `harness/evals/README.md`, `harness/sensor-catalog.md`, `scripts/check.sh`, `src/server/index.ts`, `harness/progress.md` | **검증 없이 단정하지 말라는 규칙을, 그 규칙을 어긴 바로 다음 라운드에서 다시 확인했다.** 1차 라운드가 "없는 걸 있다고 쓰지 마라"를 지키려다 반대 방향("있는 걸 없다고 쓰기")으로 넘어졌다면, 이번엔 리뷰의 반증 자체도 그대로 믿지 않고 같은 방법(저장소 밖 임시 프로젝트 + 중첩 `claude -p`)으로 4~5개 케이스를 이 세션에서 직접 재현한 뒤에만 코드·문서에 반영했다 — 남의 실측도 자기 실측으로 재확인해야 "검증됨"이라 쓸 수 있다는 것이 이번 라운드의 실무 교훈 |
| 2026-07-25 | **RQ-80 골든 GA-41~43 신설 + `scripts/**/*.mjs` lint override 추가** — Docker/Nginx 배포(ADR-0009) 검증용 골든 3건(헬스체크·WS 업그레이드 프록시·재시작 복구) 신설, 사용자 승인(원장 27a). `verify`가 테스트 파일이 아니라 **셸 스크립트**(`scripts/smoke-deploy.sh`)인 이 저장소 최초 사례 — 배포 인프라(빌드된 Docker 이미지·컨테이너 재시작) 검증은 vitest 통합 테스트 경계(ADR-0008, 프로세스 내부 서버 기동) 밖이라 같은 방식으로 테스트화할 수 없다. `scripts/smoke-ws-check.mjs`(GA-42·43 검증 헬퍼, host node에서 colyseus.js로 접속)가 Node 전역(`process`·`console`·`setTimeout` 등)을 쓰므로 `eslint.config.js`에 `src/server`와 동일한 `globals.node` override를 `scripts/**/*.mjs`에 추가(규칙 완화 아님 — 새 파일 글롭에 환경만 부여) | `harness/evals/golden/track-a-product.jsonl`, `eslint.config.js` | 이 스모크는 `scripts/check.sh`·CI 어디에도 연결돼 있지 않다(ADR-0008 §7의 3분 예산상 실 Docker 빌드·기동을 CI에 넣는 건 부적절 — 제외가 의도적 설계) — **사람이 배포 전 수동으로 실행**해야 하는 게이트라는 사실을 `harness/evals/README.md`(트랙 A 예외 절)에 명시했다(리뷰 minor 7). 배운 것: 인프라 검증 골든은 "verify = 테스트 파일"이라는 기존 관례를 깨므로, 그 사실 자체를 하네스 문서에 남기지 않으면 다음 세션이 "왜 CI가 이걸 안 잡지?"를 다시 조사해야 한다 |
| 2026-07-25 | **발사 이벤트 타임스탬프/시퀀스의 RQ-64 유예 명문화 + FE 레이어 표에 input·config 행 추가** | `harness/adr/0005-hit-registration.md`(결과 절), `harness/workflow/fe.md`(레이어 표) | 22b 리뷰 major 1·minor 6 대응 — ADR 결정 1항과 구현의 간극을 '누락'이 아니라 '유예'로 확정해 RQ-64 세션의 오독을 막고(원장 22b 행과 상호참조), 클라 디렉토리 구조(input·config)를 문서화 |
| 2026-07-24 | **선별 Red 채택 (ADR-0011 신설)** — Red-first를 src/shared·서버 판정 로직·결함 재현으로 한정, 그 외는 test-after 허용(같은 PR 동반·coder tests/ 순증만). tdd.md Phase 1 게이트·CLAUDE.md TDD 규칙 개정 | `harness/adr/0011-selective-red.md`, `harness/workflow/tdd.md`, `CLAUDE.md` | 결정권자 판정("매 RQ Red-first는 과하다") + 7 RQ 실측(Red의 실증 가치가 공유 시뮬 계약·결함 재현에 집중). 평가·리뷰 게이트와 검증 꼬리는 불변 — Red 순서만 자유화 |
| 2026-07-22 | **RQ-04 PR #5 리뷰 major 대응 — 재시도를 크래시 시그니처에 게이팅** — reviewer가 짚음: 기존 재시도는 `nonzero면 무조건`이라 단언 flaky(예: GA-29 락 레이스)를 은폐할 수 있었다(false-negative). `check.sh`를 실패 출력 분류로 고침: 단언 실패(`Failed Tests`/`AssertionError`)는 즉시 하드 실패, 알려진 워커 크래시(`Worker exited unexpectedly`/`Unhandled Error`)만 재시도, 미지의 실패도 하드 실패(보수적). 실측: 단언 주입 시 재시도 0회 즉시 하드 실패(5초). ADR-0008 §5에 재시도 방침 포인터 추가, changelog threads 항목에 철회 마커 | scripts/check.sh, harness/adr/0008-test-strategy.md, harness/changelog.md | **evaluator는 결정적 실패 방향만 실증했고 reviewer가 단언-flaky 방향(사각지대)을 잡았다** — 두 게이트의 관점 차이가 또 값을 했다. 재시도의 정당성이 '진짜 실패는 결정적'이라는 전제에서 '재시도 대상을 크래시로 한정'으로 강화됐다 |
| 2026-07-22 | **RQ-04 flaky 최종 해결 — 통합 테스트 스크립트 레벨 재시도** — pool 계층 수정(threads·maxWorkers) 실패·철회 후, `scripts/check.sh`를 단위(하드 게이트, 재시도 없음)/통합(최대 3회 재시도)으로 분리. **정당성 = 결정론 구분**: flaky는 vitest/Node teardown 계층의 비결정적 워커 크래시(독립 ~2%, exit≠0, 테스트 본문 전 사망)라 재시도가 복구하고, 진짜 assertion 실패는 결정적이라 3회 전부 실패 → 하드 실패로 그대로 잡힌다. assertion·테스트 파일 무변경 → **테스트 약화 아님**. **~~"~2% 독립 크래시가 3회 연속일 확률 ≈0.0008% — 게이트가 실질적으로 안정된다"~~** **→ 2026-07-28 거짓으로 판명**(원장 28a): 그 2%는 런이 아니라 **워커 단위**였고, 런 실패율은 1-(1-p)^n으로 통합 파일 수에 지수적이다. 작성 시점(파일 2개)엔 3연속 0.0172%로 21.5배 과소평가였고 오늘(30개) 기준으로는 14.5%다. 근본 원인은 **Node v24의 네이티브 abort**이며 `.nvmrc`·`engines`로 대응했다. 40회 실측: 1회 crash→재시도 복구, 3연속실패 0 | scripts/check.sh, harness/progress.md | **"검증 없는 강제 X, flaky 게이트는 무시된다" 원칙과 재시도가 충돌하지 않는 이유를 명문화**: 재시도는 검증(assertion)을 약화하지 않고 인프라 스폰 크래시만 흡수한다. 근본 원인(vitest/Node black-box)은 3개 세션이 규명 실패해 미해결로 남으나, 게이트 신뢰성은 회복됐다 |
| 2026-07-22 | **RQ-04 flaky 대응 — 서버 종료 버그 2개 수정 + 통합 테스트 pool 격리** — 독립 평가가 실 네트워크 통합의 콜드스타트 ~2.75% 워커 크래시를 검출(102회 반복, 5회 요구를 넘어 자발적으로). coder가 실제 버그 2개 수정: ① `new ColyseusServer()`가 프로세스 전역에 등록하는 자동 종료 리스너가 `app.close()`로 해제 안 돼 누적 → `gracefullyShutdown:false`로 비활성화(**다중 인스턴스 통합 테스트 전반을 오염시킬 버그**), ② `gracefullyShutdown`의 fire-and-forget 소켓 close → 열린 연결 직접 추적·drain await. 크래시 6.25%→2.75%. 잔여를 vitest fork 워커 IPC 부트스트랩(앱 아래 계층)으로 추적 확인 → `vitest.config.ts` pool을 forks→threads로 변경 시도(worker_threads엔 crash 나던 child_process IPC 채널이 없다는 가설, 30/30 스모크) **→ 다음 항목에서 철회됨: 재평가 270회로 flaky 미해소 + 게이트로 전파(악화) 확인, forks로 되돌림. 30/30은 과소표본이었다** | src/server/index.ts, vitest.config.ts, harness/progress.md | **사용자 결정: 근본 수정 후 잔여는 테스트 하네스 계층에서 해결.** (단 이 항목의 pool:threads 수단은 철회됐고, 최종 수단은 다음 항목의 스크립트 레벨 재시도다.) **102회 반복이 5회로는 못 잡을 콜드스타트 flaky를 잡았다** — 실 네트워크 테스트는 반복 횟수가 곧 검출력이다 |
| 2026-07-22 | **RQ-04 착수 — 파이프라인 첫 RQ 실전 + Colyseus 버전 정렬** — 로드맵 2단계 첫 RQ. `tdd-workflow` 스킬을 RQ에 처음 정식 적용(하네스 3건은 Phase 0 순환이라 스킬 우회였다). Phase 0에서 두 전제 막힘 실측: ① RQ-04 매핑 GA 케이스 0건 — **사용자가 검증 기준을 결정**해 GA-26~29 신설(정답은 사람이 쓴다). ② `colyseus.js` 미설치 + **schema 메이저 불일치 발견** — 서버 colyseus 0.17(schema 4)인데 클라 최신이 0.16(schema 3)이라 와이어 포맷 비호환. 0.16 라인으로 정렬(colyseus 0.16.5 + @colyseus/schema 3.0.76 + colyseus.js 0.16.22, 셋 다 schema 3). ADR-0002에 버전 제약 명문화 | harness/evals/golden/track-a-product.jsonl, package.json, harness/adr/0002-realtime-transport.md, harness/progress.md | **스캐폴딩(PR #1)의 잠복 결함이 첫 RQ에서 드러났다** — 클라이언트 없이 서버만 최신으로 깔아 schema 불일치가 숨어 있었다. Phase 0의 전제조건 실측이 이걸 착수 전에 잡았다(코드 쓰기 전). GA 케이스 부재로 Phase 0이 멈춘 것은 **정상 동작** — 온보딩 문서가 예고한 대로 RQ 44건 중 GA 있는 건 소수라 자주 멈춘다 |
| 2026-07-21 | **PR #4 재리뷰 지적 반영 (major 2 + minor)** — 재리뷰 판정 APPROVE(blocker 0). ① 원장 17e-1이 이미 고친 「무제한 루프」를 여전히 "미수정"으로 나열하던 것 정정(수정 2건/미수정 2건으로). ② changelog에 누락됐던 연쇄 폭주 상한 항목 추가(위 항목). ③ 원장 17e 테스트 수치 갱신(56/70 → 59/73). ④ 계약을 gitignore된 `_workspace/`에서 `harness/sim/README.md`로 **영구 승격** — `src/shared/sim`은 영구 런타임 코드인데 그 동작 계약이 저장소에 안 남아 있었다 | harness/{progress,changelog,sim/README}.md | **"이미 고친 것을 미수정으로 남겨두는" 정합 실수.** 상한 구현 후 원장 행을 안 고쳐, 하필 reviewer가 후속 이월을 승인한 근거 행이 모순됐다. 게이트가 잡았다 — 문서 정합도 게이트 대상이라는 확인 |
| 2026-07-21 | **PR #4 major 대응 — 연쇄 폭주 상한 (`SchedulerOptions` 공개 API 추가)** — 재평가에서 실측된 `advanceTo` 무제한 루프(콜백이 `scheduleIn(0)`으로 자기 재예약 → 200,000회+, 상한 없으면 무한, 서버 틱 hang) 대응. 계약 §3에 「연쇄 폭주 상한」 신설, `createScheduler(clock, { maxCallbacksPerAdvance })` 추가(기본 100,000). 상한 초과 시 원인을 특정하는 `RangeError`(상한값+재예약 진단), 콜백 예외의 `AggregateError`와 분리(try/catch 밖). test-writer 실패 테스트 3건(`c876709`) → coder 구현(`d18c11e`) → 재평가 PASS(변이 테스트로 검출력 확인) | src/shared/sim/scheduler.ts, tests/unit/sim-scheduler.test.ts | **서버 틱 경로(RQ-60)의 hang을 원인이 특정되는 에러로 바꾸는 안전망.** 정상 코드는 상한에 닿지 않는다(호출자 버그 방어). 값을 `@shared/constants`가 아니라 스케줄러 옵션에 둔 이유: 스펙 상수(게임 규칙)와 안전망 파라미터(구현 방어선)는 성격이 다르다 |
| 2026-07-21 | **PR #4 blocker 대응 — 전진 단위 desync 수정 + 회귀 테스트** — `advanceTicks(n)`이 시계를 벌크로 옮긴 뒤 `advanceTo`를 1회만 불러, 마감 틱 3의 콜백이 실행될 때 `clock.tick`이 이미 10이었다. 벌크 경로와 틱별 경로가 다른 결과를 냈다(연쇄 예약 마감이 70 vs 63). 틱 단위 루프로 수정(`dcd4f87`) + 계약 §4에 「전진 단위 불변식」 명시 + 회귀 테스트 3건(`f79b7c4`). 재평가 PASS — evaluator가 **변이 테스트**로 "구현을 되돌리면 회귀 테스트가 실제로 실패하는지"까지 확인 | tests/support/harness.ts, src/shared/sim/scheduler.ts, tests/unit/sim-harness.test.ts, harness/progress.md | **평가 게이트가 샜다.** evaluator가 PASS를 낸 뒤 리뷰 게이트가 blocker를 찾았다. 원인은 evaluator의 태만이 아니라 **계약 §4의 누락**이다 — 계약이 "시계를 전진시키고 만료 콜백을 실행한다"까지만 적고 "전진 단위에 무관해야 한다"를 쓰지 않아 문면 충족으로 판정됐다. **두 게이트의 관점이 다르다는 것이 실증된 사건**이다: evaluator는 계약 충족을, reviewer는 머지 적합성을 본다. 하나로 합쳤다면 통과했을 버그다. 교훈: 계약을 쓸 때 "이 동작이 다른 경로로 호출돼도 같은 결과인가"를 명시적으로 묻는다 |
| 2026-07-21 | **결정론 시뮬레이션 하네스 구축 (원장 17e) + `setTimeout` lint 강제** — `src/shared/sim/{clock,rng,scheduler}.ts` + `tests/support/harness.ts` 신설. TDD 파이프라인(test-writer→coder→evaluator)을 **처음으로 실제 가동**했다(스킬은 우회 — Phase 0이 이 하네스를 전제하므로 순환). 테스트 52건 신규, evaluator PASS. 평가에서 검출된 lint 구멍을 즉시 수정: `eslint.config.js`의 `src/shared` 블록에 `setTimeout`·`setInterval`·`requestAnimationFrame` 추가 | src/shared/sim/, tests/support/, tests/unit/sim-*.test.ts, eslint.config.js, harness/progress.md | `tdd.md` Phase 0이 요구하는 전제조건이었으나 실체가 없었다(PR #2 blocker 1에서 검출). **설계 핵심**: 시간의 정본 단위를 틱(정수)으로 — test-writer가 `ticksToMs(60) = 2000.0000000000002`를 실측해 ms 누적 금지의 근거가 됐다. **"규칙은 있는데 강제가 없는" 패턴이 네 번째다** — 계약이 `setTimeout` 금지를 정했지만 lint에 없었고, 방어는 테스트 스파이 하나뿐이라 `src/shared`의 다른 파일은 무방비였다. evaluator가 잡았다 |
| 2026-07-21 | **PR #3 R2 지적 반영 (major 3)** — R2 판정 APPROVE(blocker 0). ① **§8 OS 버그 행이 정정 후에도 틀렸다**: 실측(커밋 `06cb0a2`, CI 실패 이력)으로 확인하니 각 방향이 **한 번씩** 있었다 — cp949 인코딩은 로컬만, 백슬래시 경로는 CI만. "CI만 실패는 관측된 적 없음"이라고 없는 것을 단정했었다. ② `harness/README.md`가 "지금 강제되는 것"에 독립 리뷰를 넣고 13줄 뒤에 스스로 부정 — "기계가 막는 것"으로 좁힘. ③ `sensor-catalog.md` 잔여 3건(Guides ADR 행 0001~0009·제안 표기, 게이트 행의 완료된 후속 문구, 헤더의 근거 없는 개수) 정정. `progress.md` 항목 2의 "9건 전부 승인"도 함께 | docs/ONBOARDING.md, harness/{README,sensor-catalog,progress,changelog}.md | **1차 리뷰어의 권고가 틀렸고 내가 검증 없이 반영했다.** 1차는 게이트의 *차단 동작* 방향(Windows 차단/Linux 통과)을 개발자가 겪는 *실패* 방향으로 옮겨 적었다. R2가 git 이력으로 반박했다 — 리뷰어 판정도 사실 주장이면 출처로 검증해야 한다는 뜻이다. 다중 라운드 리뷰가 그 교정 장치로 작동했다 |
| 2026-07-21 | **PR #3 리뷰 지적 반영 (blocker 1 + major 4 + minor 6)** — ① **blocker**: 온보딩 문서가 "evaluator가 커밋 순서를 확인한다"고 단정했으나 evaluator 검증 항목 6건 어디에도 없다. 실제로 커밋 순서를 검사하는 게이트는 저장소 전체에 없다 — "자동으로 잡히지 않는다, 규율로만 지켜진다"로 정정. ② `sensor-catalog.md`(Sensor 현황의 정본)의 Sensors 표 5행 + Guides ADR 행 + 헤더를 실측 상태로 갱신 — lint/typecheck·테스트·동행검사를 ⬜→✅로, 골든 ask 게이트와 PostToolUse 빠른 검사의 "부재 사유"를 실제와 맞춤. `harness/README.md`의 중복 Sensor 표는 삭제하고 위임. ③ 온보딩 §6 격리 표에 강제 수단 열 추가(프롬프트 강제 vs 구조적). ④ §8의 OS 버그 행 정정 — 실측 결과 **각 방향이 한 번씩** 있었다(cp949는 로컬만, 백슬래시 경로는 CI만 — 커밋 `06cb0a2`). ⑤ 온보딩 최상단에 "이 문서는 요약이며 원본이 이긴다" 서열 명시, §9 문서 지도를 위임으로 축소. ⑥ 브랜치 컨벤션을 `<타입>/<설명>`으로 확장(정본 `CLAUDE.md` 먼저 수정) | docs/ONBOARDING.md, CLAUDE.md, harness/{README,sensor-catalog,changelog}.md | **"없는 강제 장치를 있다고 쓰는" 오류가 세 번째다** — PR #2 골든 ask 게이트, PR #2 SKILL 단정, 그리고 이번. 이번엔 신입용 문서에서 났다는 점이 특히 나쁘다(잘못된 안전감을 준다). 또 이 PR은 "stale 서술 교체"가 목적이었는데 정작 가장 stale한 `sensor-catalog.md`를 두고 그 옆에 두 번째 사본을 세웠다 — PR #1·#2에서 두 번 겪은 복제-갈라짐 패턴의 세 번째 재발 |
| 2026-07-21 | **신입 온보딩 문서 신설 (`docs/ONBOARDING.md`)** — 하네스 문서 25개(`harness/**/*.md` 실측)에 사람용 진입점이 없었다. `CLAUDE.md`는 에이전트용 포인터, `progress.md`는 원장이라 신입에게 "이거부터 읽어"라고 줄 문서가 없었다. 여섯 가지 규칙 + **각각의 이유와 위반 시 결과**, 진실 공급원 순위, 격리 파이프라인이 왜 필요한지, 자주 막히는 곳, 문서 지도. `CLAUDE.md` 참조 맵과 `harness/README.md` 문서 지도에서 링크. 함께: stale해진 `harness/README.md`의 "문서만 존재한다"·"ADR 9건"·"다음 단계 로드맵 1단계" 서술을 현재 상태(Sensor 3개 가동, ADR 10건, 다음은 결정론 하네스)로 교체 | docs/ONBOARDING.md, CLAUDE.md, harness/README.md, harness/progress.md | **규칙에 이유를 붙인 이유**: 이유를 모르는 규칙은 번거로움으로만 읽히고 결국 우회된다. 예로 "스펙에 없으면 만들지 않는다"의 근거를 스코프 크리프 방지가 아니라 "검증 기준 없는 코드를 막기 위해"로 적었다. 규칙 ⑥(내가 만든 것을 내가 판정하지 않는다)은 사람 신입에게 특히 중요하다 — 사람은 본능적으로 자기 작업을 자기가 검토한다. **미완**: 7절 단계별 실제 링크와 10절 워크드 예제는 RQ-04 실행 후 채운다 |
| 2026-07-21 | **PR #2 3차 확인 리뷰(R3) 지적 반영 (major 3 + minor 1)** — R3 판정 APPROVE(blocker 0), R2 major 3건 해소 확인됨. ① **헌법의 새 규칙이 자기모순이었다**: "테스트·비판적 점검은 별도 세션"이 바로 다음 줄("완료 주장에는 테스트 실행 출력을 증거로")과 coder의 Green 출력 의무를 부정했다 — 격리 대상을 **판정 행위**로 좁히고 "증거를 만드는 것은 작업자, 합격을 판정하는 것은 격리 세션"으로 명문화. ② `agent-roster.md` 개요 표의 입력 칸을 상세 절 위임으로 교체(복제 제거). ③ `tdd.md` Phase 1 프롬프트에 GA 케이스 전문 추가. ④ SKILL 에러 핸들링 표를 `tdd.md` 위임 + 오케스트레이션 고유 분기만으로 축소(R2·R3 이월 minor 해소) | CLAUDE.md, harness/{agent-roster,workflow/tdd}.md, .claude/skills/tdd-workflow/SKILL.md | **규칙을 헌법에 넣자마자 그 규칙이 헌법 안의 다른 줄과 충돌했다.** 격리 에이전트가 아니었으면 못 잡았을 종류다 — 내가 방금 쓴 문장이라 스스로는 의도대로 읽힌다. 이번 PR에서 "수정이 새 문제를 만든" 사례가 세 번째다(1차→R2→R3) |
| 2026-07-21 | **헌법에 "검증은 격리 세션에서" 규칙 추가** — `CLAUDE.md` 최상위 규칙에 "테스트·비판적 점검은 반드시 별도 에이전트 세션에서 한다"를 명문화. 데이터는 파일로만 전달하고 대화·의도 설명은 넘기지 않는다. 단순 실행(설치·빌드·git)은 제외 | CLAUDE.md | **사용자 지시(2026-07-21).** 그동안 evaluator·reviewer 정의에는 격리 원칙이 있었지만 헌법에는 없어 "파이프라인 밖에서의 검증"에는 적용되지 않았다. 실제로 이 규칙이 없던 탓에, R2 APPROVE 이후 내가 7개 파일을 고치고 **그 수정을 내가 직접 검증한 뒤 머지하려던 참**이었다 — 자기 채점의 전형이다. 규칙이 그 시도를 막았다 |
| 2026-07-21 | **PR #2 재리뷰(R2) 지적 반영 (major 3 + minor 5)** — blocker 2건은 R2에서 해소 확인됨. 남은 것은 전부 "실행체는 올렸는데 명세가 못 따라온" 같은 패턴이었다: ① SHA 기준점을 `tdd.md` Phase 3·`agent-roster.md`(test-writer 출력, evaluator 입력)에 반영 — 사슬 3고리를 명세에도 명문화. ② roster coder 절에 불변식 6종 추가(evaluator 절과 형식 통일). ③ roster test-writer 입력의 GA 케이스 "(있다면)" 삭제. ④ SKILL의 "골든 파일 수정은 사람 승인 게이트다" 단정을 정정 — `settings.json`에 ask 항목이 실제로 없다. `evals/README.md`의 낡은 서술도 함께 정정하고 후속 작업 17g로 등재. ⑤ 시나리오의 GA 매핑 오류(RQ-03은 GA-02·GA-21, GA-03은 RQ-10) 수정, test-writer에 `tools:` 한계 단서 추가 | .claude/{agents,skills}/, harness/{workflow/tdd,agent-roster,evals/README,progress}.md | **"없는 강제 장치를 있다고 쓰는" 오류를 이번엔 내가 냈다.** 골든 파일 ask 게이트는 계획일 뿐인데 SKILL이 단정했다 — 같은 PR이 sensor-catalog에서는 정직하게 ⚠️로 적었으면서 여기서는 반대로 갔다. 실측(`settings.json` permissions에 deny 3건뿐)으로 검출됐다 |
| 2026-07-21 | **PR #2 리뷰 지적 반영 (blocker 2 + major 4 + minor 4)** — 리뷰 게이트가 처음으로 머지를 차단했다. ① SKILL Phase 0의 전제조건 복제를 `tdd.md` 위임으로 교체(복제가 blocker 1·major 5의 공통 원인). ② 테스트 약화 감지의 diff 기준점을 3파일에 연동 — test-writer가 테스트 커밋 SHA 기록 → SKILL Phase 3이 전달 → evaluator가 `git diff <SHA>..HEAD -- tests/` 사용, 빈 출력은 "기준점 확인 필요"로 처리. ③ test-writer 산출물에 `tsc --noEmit` Red 증거 추가(ADR-0008 §4). ④ 골든 커버리지 검사를 존재 확인 → `then`↔assertion 내용 대조로 강화, PASS 시 `verify`/`status` 갱신 요청 추가. ⑤ GA 케이스 전제조건을 `tdd.md`(정본)에 추가하고 3문서 정합. ⑥ `agent-roster.md`를 실행체 수준으로 상향(결정론 직접호출 검사·불변식 절). ⑦ 3개 에이전트에 `tools:` 제한의 한계를 명시 | .claude/agents/, .claude/skills/tdd-workflow/, harness/{workflow/tdd,agent-roster,sensor-catalog,progress}.md | **게이트가 실제로 무력한 검사 2건을 잡았다.** blocker 2가 특히 중요: coder 커밋 후 워킹트리가 깨끗하므로 인자 없는 `git diff`는 항상 빈 출력이고, 파이프라인의 주 실패 모드(테스트 약화)를 잡는 유일한 검사가 언제나 통과했을 것이다. blocker 1은 명세의 전제조건 하나가 실행체에서 누락된 것 — 결정론 하네스 부재가 드러나 RQ-04 선행 작업으로 원장에 기록(17e) |
| 2026-07-21 | **TDD 파이프라인 스캐폴딩 — Red→Green→독립평가 3종 에이전트 + 오케스트레이터.** `.claude/agents/{test-writer,coder,evaluator}.md` + `.claude/skills/tdd-workflow/SKILL.md` 신설. `harness/workflow/tdd.md`·`agent-roster.md`의 구현 상태를 ✅로, `sensor-catalog.md` evaluator 행 🟡→✅ | .claude/agents/, .claude/skills/tdd-workflow/, harness/workflow/tdd.md, harness/agent-roster.md, harness/sensor-catalog.md | 로드맵 2단계(서버)가 TDD로 진행되어야 하는데 파이프라인 실행체가 없었다. **격리가 목적**: 테스트 작성자가 구현을 보면 구현에 맞춘 테스트가 되고, 평가자가 구현 세션 맥락을 공유하면 자기 채점이 된다. 데이터는 `_workspace/{RQ-ID}/` 파일로만 전달하며 대화는 넘기지 않는다. reviewer까지 포함해 **4개 에이전트 전부 실행 가능** 상태가 됐다 |
| 2026-07-21 | **PR #1 리뷰 지적 반영 (major 5 + minor 5)** — reviewer가 격리 세션에서 검출한 문서·실행체 불일치를 같은 PR에서 해소. (1) `CLAUDE.md`·`agent-roster.md`·`tdd.md`의 "코드가 없다/미스캐폴딩" 서술을 실제 상태로 갱신. (2) `review-gate.md` 검토 항목을 6건→10건, ADR 범위 0001~0010으로 확장(실행체에 맞춰 명세를 갱신 — 근거 있는 항목을 줄이지 않았다). (3) `fe.md` 레이어 경로를 `src/net/`→`src/client/net/` 등 ADR-0010 레이아웃에 정합. (4) 리뷰 보고서 경로에 브랜치명 `/`→`-` 치환 규칙을 3곳(명세·SKILL·에이전트)에 명시. (5) `eslint.config.js`에 `no-restricted-imports`(node:*·fs·path 등)와 `performance.now` 추가, ADR-0010 §결과를 실제 lint 상태로 정정 | CLAUDE.md, harness/{agent-roster,workflow/tdd,workflow/fe,workflow/review-gate,specs/requirements,adr/0010,progress,changelog}.md, .claude/{agents/reviewer,skills/review-gate/SKILL}.md, eslint.config.js, vite.config.ts | **리뷰 게이트의 첫 실전 가동이 실제로 결함을 잡았다.** 특히 두 건이 "조용히 고장 나는" 부류였다: 브랜치명에 슬래시가 있어 재리뷰가 1회차로 처리되면 이전 blocker 해소 확인이 통째로 건너뛰어지고, `no-restricted-globals`만으로는 `import fs from 'node:fs'`가 무사통과해 shared 환경중립이 뚫린다(실측 확인). 또 이 PR이 추가한 "차이가 생기면 실행체를 명세에 맞춘다"는 규칙을 같은 PR이 스스로 어긴 것도 검출됐다 — 이 경우엔 실행체 4개 항목에 근거가 있어 명세를 갱신하는 쪽이 옳았다 |
| 2026-07-21 | **리뷰 게이트 스캐폴딩 — 두 번째 실동작 Sensor(추론적).** `.claude/agents/reviewer.md`(opus) + `.claude/skills/review-gate/SKILL.md` 신설. `harness/workflow/review-gate.md`의 명세를 실행체로 옮기고, 해당 문서의 "구현 상태" 주석을 ✅로 갱신. `sensor-catalog.md` PR 리뷰 게이트 행 ⬜→✅ | .claude/agents/, .claude/skills/, harness/workflow/review-gate.md, harness/sensor-catalog.md | CI(결정론적)와 reviewer(추론적)의 이중 게이트를 완성한다. CI는 lint·typecheck·test처럼 "기계가 판정 가능한 것"만 잡고, 스코프 이탈·ADR 모순·서버 권위 위반처럼 문서를 읽고 판단해야 하는 것은 잡지 못한다. **격리가 핵심**: 구현 세션의 대화·의도 설명을 reviewer에 넘기지 않는다 — 작성자의 논리를 들으면 작성자의 맹점을 물려받는다. ⚠️ **미완**: GitHub 브랜치 보호(status check 필수)는 아직 설정하지 않아, 지금 이 게이트는 규율로만 지켜진다 |
| 2026-07-21 | **프로젝트 스캐폴딩 (로드맵 1단계)** — ADR-0010(프로젝트 레이아웃) 신설: 단일 package.json + `src/{client,server,shared}`. `vite.alias.ts`로 경로 별칭을 한 곳에 정의하고 vite·vite.server·vitest가 임포트(tsconfig `paths`와는 도구 체계가 달라 이중 선언 잔존 — 주석으로 명시). `src/shared/constants.ts`에 스펙 확정값(RQ-03/60/64/90/92 등)을 근거 주석과 함께 인코딩. eslint에 `src/shared` 환경중립 규칙(window·document·process 금지)과 결정론 규칙(`Math.random`·`Date.now` 금지) 추가 — ADR-0008/0010을 lint로 승격. `scripts/check.sh` 실질화(게이트→lint→typecheck→test), CI에 검증 일괄·빌드 단계 추가 | package.json, tsconfig.json, vite*.config.ts, eslint.config.js, src/, tests/, scripts/check.sh, .github/workflows/ci.yml, harness/adr/0010 | 로드맵 1단계. **설계 함정 회피**: 서버를 `tsc`로 컴파일하면 `@shared/*` 별칭이 재작성되지 않아 타입검사·빌드는 통과하고 `node dist/server/index.js`만 런타임에 죽는다 — Vite SSR 빌드로 전환해 빌드 타임에 해석시키고, 빌드 산출물을 실제 실행해 `/health` 응답까지 확인했다. 검증 실측: 테스트 14건 통과, check.sh 4.2초. **범위 준수**: 게임 로직은 넣지 않았다(2단계부터 TDD 파이프라인). ADR-0001에 없는 라이브러리(drei 등) 미도입 |
| 2026-07-21 | **스펙 동결 게이트 구축 — 하네스 최초의 실동작 Sensor.** `.claude/hooks/gate_spec_freeze.py` 신설: requirements.md에 🟡 PENDING이 남은 동안 구현 디렉토리(`BLOCKED_TOP_DIRS`: src/tests/test/\_\_tests\_\_/client/server/shared/packages) 수정을 PreToolUse에서 exit 2로 차단. `.claude/settings.json`으로 hook 등록 + 시크릿 파일 읽기 deny. `.github/workflows/ci.yml` 신설: 게이트 자체 검증(`--selftest`) → 스펙 동결 게이트(하드) → M2·M3 동행 검사(경고). `sensor-catalog.md` 해당 행 ⬜→✅ | .claude/hooks/, .claude/settings.json, .github/workflows/ci.yml, harness/sensor-catalog.md | CLAUDE.md·requirements.md가 이 게이트의 존재를 전제로 서술해왔으나 실체가 없었다. 코드가 없는 지금이 게이트를 세우기 가장 싼 시점 — 코드가 생긴 뒤에 붙이면 이미 드리프트한 코드가 기준선이 된다. **설계 결정**: 판정 로직을 스크립트 1개에 두고 hook·CI가 같은 코드를 호출한다(bvwebchat은 hook=Python, CI=grep 정규식으로 이중 정의 — 어긋나면 로컬과 CI 판정이 갈려 게이트 신뢰가 죽는다). **구축 중 발견·수정**: cp949 stdout에서 em-dash 출력 시 UnicodeEncodeError로 게이트가 스펙과 무관하게 죽는 버그 — `_force_utf8()`로 stdout·stderr 모두 고정하고 selftest에 해당 경로 검증 추가 |
| 2026-07-21 | Deep Interview 완료 반영 — `requirements.md` v0.1→v1.0(RQ-90~95 🟡→✅ 확정, RQ-03·RQ-04·RQ-81 개정). `sensor-catalog.md` Guides 4건을 ✅로 갱신(CLAUDE.md·harness/adr/·plan mode 승인 규칙이 실재 확인됨), 결정론 위반 감지 센서 우선순위 1위로 상향(RQ-90 탄퍼짐 랜덤 콘이 첫 실제 감시 대상), 프레임 예산 회귀 센서를 60fps 가정에서 RQ-01 확정치(내장 GPU Iris Xe급 30fps)로 정정. `metrics-baseline.md` M7 베이스라인 23→32, M8에 20연결 부하 조건 명시, M9 목표를 RQ-01 30fps=33.3ms 기준으로 채움. `evals/golden/track-a-product.jsonl`에 GA-17~25(9건) 추가, GA-07·08·16을 확정 수치(헤드샷 50, 4타 킬, 되감기 200ms)로 정밀화 | harness/specs/requirements.md, harness/sensor-catalog.md, harness/metrics-baseline.md, harness/evals/golden/track-a-product.jsonl | 37개 질문 전부 결정권자가 답해 무기·이동·관전·맵·통계 수치가 확정됨(`harness/specs/interview/answers.md`). 확인 결과 ADR-0001·0002·0003·0006·0008·0009는 이미 승인, ADR-0004(물리·충돌)·0005(히트판정·랙보상)·0007(맵 에셋 파이프라인)은 관련 RQ가 확정되어 승인 검토가 가능해졌으나 harness/adr/ 상의 실제 상태는 아직 제안 — 이 문서는 그 미승인 상태를 그대로 기록한다 |
| 2026-07-21 | 하네스 초기 구성 — `sensor-catalog.md`(Guide/Sensor 카탈로그 + 게임 특화 센서 4종), `metrics-baseline.md`(M1~M9), `evals/README.md`, `evals/golden/track-a-product.jsonl`(GA-01~16), `evals/golden/track-b-harness.jsonl`(GB-01~07), 본 변경 이력 파일 신설 | harness/ | `harness/specs/requirements.md`(RQ-01~95, v0.1) 확정 이후 가드레일·측정 체계의 뼈대를 마련. 프로젝트에 코드가 전무해 대부분의 센서는 ⬜미구축/🟡계획으로 정직하게 기록 — CLAUDE.md·ADR(harness/adr/, 템플릿만 존재)는 병행 작성 중 |
