# 하네스 변경 이력

> 하네스(CLAUDE.md·hook·skill·센서·메트릭)를 바꾸면 여기에 기록한다.
> 목적: 진화 방향 추적 + 퇴행(regression) 방지.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-21 | 하네스 초기 구성 — `sensor-catalog.md`(Guide/Sensor 카탈로그 + 게임 특화 센서 4종), `metrics-baseline.md`(M1~M9), `evals/README.md`, `evals/golden/track-a-product.jsonl`(GA-01~16), `evals/golden/track-b-harness.jsonl`(GB-01~07), 본 변경 이력 파일 신설 | harness/ | `harness/specs/requirements.md`(RQ-01~95, v0.1) 확정 이후 가드레일·측정 체계의 뼈대를 마련. 프로젝트에 코드가 전무해 대부분의 센서는 ⬜미구축/🟡계획으로 정직하게 기록 — CLAUDE.md·ADR(harness/adr/, 템플릿만 존재)는 병행 작성 중 |
| 2026-07-21 | **스펙 동결 게이트 구축 — 하네스 최초의 실동작 Sensor.** `.claude/hooks/gate_spec_freeze.py` 신설: requirements.md에 🟡 PENDING이 남은 동안 구현 디렉토리(`BLOCKED_TOP_DIRS`: src/tests/test/\_\_tests\_\_/client/server/shared/packages) 수정을 PreToolUse에서 exit 2로 차단. `.claude/settings.json`으로 hook 등록 + 시크릿 파일 읽기 deny. `.github/workflows/ci.yml` 신설: 게이트 자체 검증(`--selftest`) → 스펙 동결 게이트(하드) → M2·M3 동행 검사(경고). `sensor-catalog.md` 해당 행 ⬜→✅ | .claude/hooks/, .claude/settings.json, .github/workflows/ci.yml, harness/sensor-catalog.md | CLAUDE.md·requirements.md가 이 게이트의 존재를 전제로 서술해왔으나 실체가 없었다. 코드가 없는 지금이 게이트를 세우기 가장 싼 시점 — 코드가 생긴 뒤에 붙이면 이미 드리프트한 코드가 기준선이 된다. **설계 결정**: 판정 로직을 스크립트 1개에 두고 hook·CI가 같은 코드를 호출한다(bvwebchat은 hook=Python, CI=grep 정규식으로 이중 정의 — 어긋나면 로컬과 CI 판정이 갈려 게이트 신뢰가 죽는다). **구축 중 발견·수정**: cp949 stdout에서 em-dash 출력 시 UnicodeEncodeError로 게이트가 스펙과 무관하게 죽는 버그 — `_force_utf8()`로 stdout·stderr 모두 고정하고 selftest에 해당 경로 검증 추가 |
| 2026-07-21 | Deep Interview 완료 반영 — `requirements.md` v0.1→v1.0(RQ-90~95 🟡→✅ 확정, RQ-03·RQ-04·RQ-81 개정). `sensor-catalog.md` Guides 4건을 ✅로 갱신(CLAUDE.md·harness/adr/·plan mode 승인 규칙이 실재 확인됨), 결정론 위반 감지 센서 우선순위 1위로 상향(RQ-90 탄퍼짐 랜덤 콘이 첫 실제 감시 대상), 프레임 예산 회귀 센서를 60fps 가정에서 RQ-01 확정치(내장 GPU Iris Xe급 30fps)로 정정. `metrics-baseline.md` M7 베이스라인 23→32, M8에 20연결 부하 조건 명시, M9 목표를 RQ-01 30fps=33.3ms 기준으로 채움. `evals/golden/track-a-product.jsonl`에 GA-17~25(9건) 추가, GA-07·08·16을 확정 수치(헤드샷 50, 4타 킬, 되감기 200ms)로 정밀화 | harness/specs/requirements.md, harness/sensor-catalog.md, harness/metrics-baseline.md, harness/evals/golden/track-a-product.jsonl | 37개 질문 전부 결정권자가 답해 무기·이동·관전·맵·통계 수치가 확정됨(`harness/specs/interview/answers.md`). 확인 결과 ADR-0001·0002·0003·0006·0008·0009는 이미 승인, ADR-0004(물리·충돌)·0005(히트판정·랙보상)·0007(맵 에셋 파이프라인)은 관련 RQ가 확정되어 승인 검토가 가능해졌으나 harness/adr/ 상의 실제 상태는 아직 제안 — 이 문서는 그 미승인 상태를 그대로 기록한다 |
