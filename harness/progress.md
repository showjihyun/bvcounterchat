# 진행 원장 (Progress Ledger) — ChatStrike

> **규칙(최상위)**: 모든 작업은 시작 전 이 원장에 행을 만들고 상태를 🔄로
> 바꾼 뒤 진행한다. 완료 시 ✅ + 참조 파일/산출물을 기록한다. 원장에 없는
> 작업은 먼저 행을 추가한다 — 원장 밖 작업 금지.
> 상태: ⬜ 대기 · 🔄 진행중 · ✅ 완료 · ⛔ 차단(사유 병기)

---

## 1. 하네스 구축 (2026-07-21)

| 순번 | 작업 | 관련 RQ/ADR | 상태 | 참조 파일 | 비고 |
|---|---|---|---|---|---|
| 1 | 요구사항 EARS 정규화 (`docs/req/` → RQ-01~95) | 전체 RQ | ✅ | `harness/specs/requirements.md` | **v1.0으로 갱신(2026-07-21)** — Deep Interview 완료로 RQ-90~95 🟡→✅ 확정, RQ-03·RQ-04·RQ-81 개정(항목 15 참고). 🟡 잔존 0건 |
| 2 | ADR 초안 0000~0009 | ADR-0001~0009 | ✅ | `harness/adr/`, `harness/adr/README.md` | 초안 9건 + 템플릿 작성 완료. 0004·0005·0007은 RQ-90/92/94 확정 후 항목 16에서 승인 전환. **현재 ADR은 0010(레이아웃) 포함 10건 전부 승인** — 상태의 정본은 `harness/adr/README.md` 상태표다 |
| 3 | TDD 워크플로우 문서 | — | ✅ | `harness/workflow/tdd.md` | Red→Green→독립평가 파이프라인 명세. 실행 스킬·에이전트 정의는 미스캐폴딩 |
| 4 | 리뷰 게이트 워크플로우 문서 | — | ✅ | `harness/workflow/review-gate.md` | reviewer(Opus) APPROVE를 머지 필요조건으로 명세. 실행 스킬 미스캐폴딩 |
| 5 | FE(클라이언트·HUD·3D) 워크플로우 문서 | RQ-50~55, RQ-42, RQ-70/71 | ✅ | `harness/workflow/fe.md` | netcode→game state→scene→HUD 레이어 분리 규칙. ADR-0001/0003/0007 참조 |
| 6 | 에이전트 로스터 명세 (test-writer/coder/evaluator/reviewer) | — | ✅ | `harness/agent-roster.md` | 4개 에이전트 격리 파이프라인 명세. 실행 스킬(`.claude/agents/*.md`)은 미스캐폴딩 |
| 7 | 센서 카탈로그 | — | ✅ | `harness/sensor-catalog.md` | Guide/Sensor 현황표 + 게임 특화 센서 4종. 항목 대부분 ⬜미구축/🟡계획으로 정직하게 기록(코드 전무 상태 반영) |
| 8 | 메트릭 베이스라인 | — | ✅ | `harness/metrics-baseline.md` | M1~M9 정의. M7(골든 케이스 수)만 실측값 보유(32), 나머지는 "미측정"이 정직한 값 |
| 9 | 골든 evals | 전 RQ | ✅ | `harness/evals/golden/track-a-product.jsonl`(GA-01~25), `track-b-harness.jsonl`(GB-01~07) | **총 32건(GA 25 + GB 7), 전부 `status: todo`.** Deep Interview 확정값으로 GA-17~25 신규 추가(탄퍼짐 시드 결정론·공중가속 미허용·세이프존 사격불가·스폰 로테이션·관전자 정원초과 거부·UUID 통계 2건·금칙어 필터·낙하 데미지)하고 GA-07/08/16을 확정 수치로 정밀화. `blocked_on_spec` 0건. JSONL 라인별 파싱·ID 중복 검증 통과. ※ 과거 원장에 있던 "파일이 실재하지 않는다"는 플래그는 **오탐이었음** — 두 파일 모두 처음부터 존재했다 |
| 10 | 질문 은행(Deep Interview) | RQ-90~95 | ✅ | `harness/specs/interview/question-bank.md` | 37문항(A~G). 모순 플래그 2건(질문 13 관전자 상한, 질문 25 통계-닉네임 식별) 포함. **완료 배너·ADR-0007/0009 staleness 수정 반영(2026-07-21)** |
| 11 | 답변 기록(Deep Interview 결과) | RQ-90~95 | ✅ | `harness/specs/interview/answers.md` | 질문 1~37 전부 답변·근거 기록 완료(2026-07-21). 뱅크가 권장안을 유보했던 질문 4·8과 ★를 기각한 질문 15·30·31은 결정권자의 채택/기각 사유를 원문 그대로 보존 |
| 12 | 하네스 변경 이력 | — | ✅ | `harness/changelog.md` | 2026-07-21 항목 1건 |
| 13 | 하네스 개요(README) | — | ✅ | `harness/README.md` | Guide/Sensor 모델, 문서 지도. **갱신 완료(2026-07-21)** — "지금 상태" 섹션으로 교체, 스펙 동결 게이트 구축 반영 |
| 14 | 진행 원장 | — | ✅ | `harness/progress.md` | 본 파일 |

---

## 2. Deep Interview 및 후속 조치

| 순번 | 작업 | 관련 RQ/ADR | 상태 | 참조 파일 | 비고 |
|---|---|---|---|---|---|
| 15 | Deep Interview 진행 (RQ-90~95 해소) | RQ-90~95, RQ-03/04/81 개정 | ✅ | `harness/specs/interview/{question-bank,answers}.md`, `harness/specs/requirements.md`(v1.0) | **완료(2026-07-21)**. 37문항 전부 답변 확보 → `answers.md` 기록 → `requirements.md`를 v1.0으로 개정(RQ-90~95 확정 + RQ-03·04·81 정합화). 모순 2건(관전자 상한, 통계-닉네임 식별) 모두 RQ 문구 개정으로 해소. 스펙 동결 게이트가 막던 🟡는 0건. hook·CI 실체는 항목 17에서 구축 완료 |
| 16 | ADR-0004·0005·0007을 "제안"→"승인"으로 전환 | ADR-0004, ADR-0005, ADR-0007 | ✅ | `harness/adr/` | **완료(2026-07-21)**. 세 ADR 모두 승인 전환 + 확정값 본문 반영(0004 이동 파라미터표·공중가속 미허용, 0005 무기 수치·되감기 200ms·`headshot` 플래그, 0007 맵 수치·스프레이 아틀라스). `adr/README.md` 상태표 9건 전부 승인으로 갱신. 단서: 승인 ≠ 전면 검증 — Rapier 30Hz 실측(0004), 히트박스 치수(0005)는 잔존 사항으로 각 `## 결과`에 명시 |
| 17 | **스펙 동결 게이트 구축 (hook + CI)** | 전 RQ (드리프트 방지) | ✅ | `.claude/hooks/gate_spec_freeze.py`, `.claude/settings.json`, `.github/workflows/ci.yml` | **완료(2026-07-21) — 하네스 최초의 실동작 Sensor.** 🟡 존재 시 구현 디렉토리 수정을 PreToolUse exit 2로 차단, CI에서 동일 스크립트로 PR 차단. 판정 로직 단일화(hook·CI가 같은 코드 호출). `--selftest` 내장, CI 첫 스텝으로 실행. 4개 시나리오 실측 검증(구현 차단 / 문서 통과 / CI 실패 / 문서전용 PR 통과). 구축 중 cp949 인코딩 크래시 버그 1건 발견·수정. **후속 필수**: 스캐폴딩에서 레이아웃 확정 시 `BLOCKED_TOP_DIRS` 갱신, git init 후 CI 활성 |
| 17b | **리뷰 게이트 스캐폴딩 (skill + reviewer 에이전트)** | 전 PR (추론적 게이트) | ✅ | `.claude/skills/review-gate/SKILL.md`, `.claude/agents/reviewer.md` | **완료(2026-07-21)**. reviewer=opus, 검토 항목 10건. **첫 실전 가동 결과**: PR #1에서 1차 major 5·minor 8, 재리뷰 major 1(수정이 새로 만든 것)·minor 6 검출 — blocker 0. 실제로 잡은 것: lint 구멍(`import fs from 'node:fs'` 무사통과), 재리뷰 경로 미정규화(재리뷰가 조용히 1회차로 처리됨), 헌법의 저장소 상태 오진술. **미완**: GitHub 브랜치 보호 status check 미설정 — 현재는 규율로만 강제 |
| 17c | **TDD 파이프라인 스캐폴딩 (test-writer·coder·evaluator + skill)** | 전 RQ 구현 | ✅ | `.claude/agents/{test-writer,coder,evaluator}.md`, `.claude/skills/tdd-workflow/SKILL.md` | **완료(2026-07-21)**. 모델: test-writer·coder=sonnet, evaluator=opus. 전제조건은 `tdd.md` Phase 0이 정본이며 SKILL은 위임한다(복제 금지). **PR #2 리뷰에서 blocker 2건 검출·수정**: ① 전제조건 복제 중 '결정론 하네스 존재' 누락(→17e 선행 작업으로 등재) ② 테스트 약화 감지가 기준점 없는 `git diff`라 항상 빈 출력 → SHA 사슬(test-writer 기록 → SKILL 전달 → evaluator 사용)로 수정. reviewer 포함 4개 에이전트 전부 실행 가능. **미검증**: 파이프라인 자체는 아직 실행된 적 없다 — RQ-04가 첫 실전 |
| 17d | **온보딩 워크플로우 문서** | — | ✅ | `docs/ONBOARDING.md` | **완료(2026-07-22)**. 골격(여섯 규칙+이유·진실공급원·격리 설명·문서지도)에 이어 RQ-04 완료 후 7절(단계별 실제 커밋·PR 링크)과 10절(워크드 예제 — Phase 0 멈춤부터 머지까지 11단계 표 + 교훈 4가지)을 실제 사례로 채움. 게이트 이력(평가 3회·리뷰 2회·오케스트레이터 오판 2회 포함)을 미화 없이 기록 — "작성자의 확신은 증거가 아니다"가 사람에게만 적용되는 게 아니라는 것이 예제의 핵심 |
| 17e | **결정론 시뮬레이션 하네스 (고정 틱 + 시드 RNG + 틱 스케줄러)** | RQ-11/15/16/43/60/90, ADR-0008 | 🔄 | `src/shared/sim/{clock,rng,scheduler}.ts`, `tests/support/harness.ts` | 테스트 **59건 신규(총 73건 통과)**. **핵심 판단**: 시간의 정본 단위는 틱(정수) — `ticksToMs(60)`이 `2000.0000000000002`인 것이 실측 확인돼 ms 누적 금지의 근거가 됐다. RNG는 32비트 정수 연산, 스케줄러는 이진 최소 힙. **⚠️ 게이트 이력(기록 가치 최상)**: evaluator가 1차 **PASS**를 냈으나 그 뒤 리뷰 게이트가 **blocker**를 검출했다 — `advanceTicks(n)`이 시계를 벌크로 옮겨 벌크 경로와 틱별 경로가 다른 결과를 냈다(서버는 틱별, 테스트는 벌크 → 하네스로 통과한 테스트가 서버 동작을 보장하지 못함). **evaluator 잘못이 아니라 계약 §4의 누락이 원인**이었고, 계약에 「전진 단위 불변식」을 추가한 뒤 `dcd4f87`(수정)·`f79b7c4`(회귀 테스트 3건)로 대응, 재평가 PASS(변이 테스트로 회귀 검출력까지 확인). **평가 게이트가 샜다는 사실 자체가 이 항목의 핵심 기록이다** |
| 17e-1 | 하네스 후속 — PR #4 리뷰 major 미수정 2건 | ADR-0008/0010, RQ-90(fork)/RQ-62(예측) | ⬜ | `src/shared/sim/`, `_workspace/review/chore-determinism-harness.md` | **PR #4 리뷰 major 4건 중 2건은 이 PR에서 수정됨**(17f 병합 복구 `23e9f60`, `advanceTo` 무제한 루프 → 연쇄 폭주 상한 `d18c11e`). **나머지 2건이 여기 남는다** — reviewer 재리뷰(APPROVE)에서 후속 이월 타당 판정: ① **`createRng(seed)`·`fork(salt)`의 `seed >>> 0` 조용한 강제** — `createRng(1.5)`와 `createRng(1)`이 **같은 스트림**이다. 시드를 계산식으로 만들 때 무증상 충돌. **RQ-90 탄퍼짐의 fork 실호출자가 생기기 전 필수 수정**(reviewer 지정). ② **`createClock(startTick)` 인자 무검증** — `advance`·`scheduleAt`에는 "조용한 반올림 금지"를 적용해놓고 생성자만 비대칭. 함께: `assertNonNegativeInteger`가 `clock.ts`·`scheduler.ts`·`harness.ts`에 3중 중복. ③ **계약 미규정(재평가 r3 관측)**: 연쇄 폭주 `RangeError`로 즉시 중단할 때 이미 `errors`에 쌓인 콜백 예외가 버려진다 — 계약 보강 후보 |
| 17e-2 | 하네스 플랫폼 간 RNG 동일성 검증 | RQ-62 | ⬜ | — | **미확인 이월.** 32비트 정수 연산만 쓴다는 건 소스로 확인됐으나 다른 엔진(브라우저 JS, ARM Node)에서 같은 수열이 나오는지는 단일 환경(Node/win32)에서 검증 불가. 기준값이 `03_evaluator_report.md` §4-2 B1에 남아 있다 — **클라이언트 코드가 생기면 그때 대조한다.** 갈라지면 클라이언트 예측(RQ-62)이 서버와 어긋난다 |
| 17f | coder 세션의 `tests/` 쓰기 차단 hook (후속) | 파이프라인 무결성 | ⬜ | `.claude/hooks/` (미생성) | **PR #2 리뷰 blocker 2 후속 권고.** coder는 `Edit`·`Bash` 권한이 있어 테스트 파일 수정이 도구 수준에서 막히지 않는다. 현재 방어는 evaluator의 사후 diff 검출 하나뿐 — `gate_spec_freeze.py`가 PreToolUse 경로 차단의 선례이므로 같은 방식으로 프롬프트 강제를 결정론적 게이트로 승격 가능 |
| 17g | 골든 파일 ask 게이트 (`settings.json`) | 평가 무결성 | ⬜ | `.claude/settings.json` | **PR #2 리뷰 minor d 후속.** `harness/evals/README.md`와 `sensor-catalog.md`가 `harness/evals/golden/**` 수정을 사람 승인 게이트로 전제하지만 `permissions.ask` 항목이 없다 — 규율로만 지켜진다. 에이전트가 자기 정답을 쓰는 것을 막는 장치이므로 파이프라인 실전 전에 넣는 편이 낫다 |
| 17h | RQ-04 리뷰 minor 3건 (후속) | RQ-04, RQ-80, ADR-0008 | ⬜ | `src/server/index.ts` | **PR #5 리뷰 minor 이월.** ① `gracefullyShutdown:false`가 프로덕션 SIGTERM 그레이스풀 종료를 제거 — RQ-80이 재시작 소실 허용해 v1 수용 가능하나 `src/server/index.ts`에 트레이드오프 주석 한 줄 권고(reviewer). ② 재시도 방침의 ADR-0008 반영은 major 대응 시 완료. ③ changelog threads 철회 마커 완료. ④ **델타 리뷰 minor**: check.sh 게이팅이 vitest 기본 리포터 문자열("Failed Tests" 등)에 결합 — 리포터/메이저 업그레이드 시 어긋날 수 있음(현재 삼중 중복이라 견고). vitest 업그레이드 시 시그니처 재검증 필요 |
---

## 3. 로드맵 (`docs/req/07_Roadmap.md` 10단계, v1 = **전체 10단계** — 질문 34 확정)

Deep Interview 완료로 RQ 단위 스펙 동결 게이트가 전부 해제됐다. 아래 표의
⬜는 이제 순수하게 **작업 순서 대기**이며, 이전 버전에 있던 ⛔(스펙 미확정
차단)는 더 이상 없다. 마감일은 없음(질문 33) — 순서는 로드맵 순서를 따른다.

| 순번 | 로드맵 단계 | 관련 RQ | 상태 | 참조 파일 | 비고 |
|---|---|---|---|---|---|
| 18 | 1. 프로젝트 초기화 | RQ-01, ADR-0001~0003, ADR-0010 | ✅ | `package.json`, `tsconfig.json`, `vite*.config.ts`, `src/{client,server,shared}`, `tests/{unit,integration}`, `scripts/check.sh` | **완료(2026-07-21)**. ADR-0010 신설(단일 패키지 + src 하위분할). 검증 실측: typecheck 0에러, lint 0에러, 테스트 14건 통과, `check.sh` 4.2초(예산 3분), 빌드 성공, **빌드된 서버 실행 후 `/health`가 공유 상수를 반환하는 것까지 확인**. 게이트 `BLOCKED_TOP_DIRS`는 src/tests를 이미 포함해 갱신 불요(주석만 보강). CI에 lint·typecheck·test·build 추가 |
| 19 | 2. 서버 | RQ-01~04 | 🔄 | `docs/req/07_Roadmap.md` §2 | ADR-0002(실시간 전송)·ADR-0006(신원·영속성) 기반. RQ-03(정원 10+10)·RQ-04(세션 수명 무제한)·RQ-02(닉네임 자동 접미사) 모두 확정. **PR #1 리뷰 이월(minor)**: ① `colyseus.js` 설치·정렬 완료(2026-07-22, RQ-04 착수 시) — **schema 메이저 불일치 발견·해소**: 서버가 0.17(schema 4)인데 클라 최신이 0.16(schema 3)이라 0.16 라인으로 정렬(ADR-0002 버전 제약). ② `vite.config.ts` 개발 프록시가 `/matchmake`만 잡음 — Colyseus WS 업그레이드용 `ws: true` 추가 필요. ③ `colyseus`·`@colyseus/schema`·`rapier3d-compat`이 1단계에서 코드 없이 설치돼 현재 어떤 빌드·테스트도 검증하지 않음 |
| 19a | **RQ-04 상설 세션 (파이프라인 첫 RQ 실전)** | RQ-04, ADR-0002 | ✅ | `feat/RQ-04-persistent-session` 브랜치 | **착수(2026-07-22)**. `tdd-workflow` 스킬의 첫 정식 RQ 적용(하네스 3건은 스킬 우회였음). Phase 0에서 두 전제 막힘 실측·해소: ① RQ-04 매핑 GA 케이스 0건 → **사용자가 검증 기준 결정**(GA-26~29 신설, 정답은 사람이 씀). ② colyseus.js 미설치 + schema 메이저 불일치 → 0.16 라인 정렬. GA-26(0명이어도 유지)·GA-27(라운드 종료 없이 재입장)·GA-28(재시작 시 소실)·GA-29(단일 세션) **평가 1차 FAIL(flaky)**: 실 네트워크 통합이 콜드스타트 ~2.75% 워커 크래시. coder가 실제 버그 2개 수정(Colyseus 프로세스 전역 리스너 누적 → `gracefullyShutdown:false`, fire-and-forget 소켓 close → drain await, 커밋 `4bccf45`) → 6.25%→2.75%. 잔여는 vitest fork 워커 IPC 계층(앱 아래)으로 확인 → 하네스 계층 수정 시도가 **실패**: `pool:'threads'`는 flaky를 못 낮추고(270회 ~1.5%) 오히려 crash를 게이트 전체로 전파(악화), `maxWorkers:1`은 13%로 악화 — 둘 다 철회하고 forks(기본) 유지. **재평가 2차 FAIL**(flaky 미해소). 잔여 exit-127 크래시의 근본 원인은 vitest/Node teardown 계층 black-box로 2개 세션+오케스트레이터 모두 규명 실패. 실-WS+preClose 종료 경로 고유. **해결: 스크립트 레벨 재시도**(사용자 결정). `check.sh`가 단위=하드 게이트(재시도 없음)/통합=최대 3회 재시도로 분리. 정당성=결정론 구분: flaky는 비결정적 인프라 크래시(독립 ~2%)라 재시도가 복구하고, 진짜 실패는 결정적이라 3회 전부 실패 → 하드 실패. assertion 무변경(테스트 약화 아님). 40회 실측: 1회 crash→재시도 복구, 3회연속실패 0. **완료(2026-07-22, PR #5 머지 `da5a2f1`)**. 게이트 최종: 평가 3차 PASS(check.sh 47/47 + 단언 주입 실증) → reviewer APPROVE(major 1: 재시도가 단언 flaky 은폐 가능 → 크래시 시그니처 게이팅으로 차단 `3e7ddcf`) → 델타 확인 APPROVE(게이팅 구멍 0, 삼중 시그널 확인). **RQ-04 구현 게이트 통과 — 첫 제품 RQ 완료** |
| 19b | **RQ-02 닉네임 식별 + 자동 접미사** | RQ-02, ADR-0002/0006 | ✅ | `feat/RQ-02-nickname-identity` 브랜치 | **착수(2026-07-22)**. tdd-workflow 정식 적용 2번째. Phase 0 전부 충족(GA-01 매핑·하네스 실재·check Green). **완료(2026-07-22, PR #7 머지 `25ddf19`)**. 한 사이클에 Red(`db5498c` 3건)→Green(`9f1632a`)→평가 PASS(8항목)→리뷰 APPROVE(blocker 0). 스키마 `src/shared/schema/GameState.ts` 신설(ADR-0010), 접미사는 결정론적 카운터. 평가 §2에서 **재시도 게이팅의 첫 live 실증** — 알려진 워커 크래시 1회를 단언 실패와 정확히 구분해 재시도·흡수. 리뷰 major 1건(닉네임 새니타이즈)은 19b-1로 이월(reviewer가 RQ-40/HUD 전 처리 지정) |
| 19b-1 | 닉네임 새니타이즈 (RQ-02 리뷰 major 이월) | RQ-02 v1.2, RQ-40/51/55 표시 계층 | ✅ | `fix/19b-1-nickname-sanitize` 브랜치 | **완료(2026-07-24, PR #13)**. 스펙 v1.2(`4954bb8`)→Red(`55ab3a4`, 11건 — 서로게이트 경계 3케이스·관전자 경로·접미사 순서)→Green(`40f70e2` — 순수 함수 `src/shared/identity/nickname.ts` 분리+`IDENTITY.NICKNAME_MAX_CODEPOINTS` 상수+onJoin 공유 지점 적용)→평가 PASS(naive UTF-16 절단 대조 프로브·관전자 실측·lint 스코프 포함 확인)→리뷰 APPROVE(blocker 0/major 0/minor 3 — 전부 스펙 확장 여부라 이월, 19b-2 참조). **착수(2026-07-24)**. 값 사용자 결정: 최대 16자(유니코드 코드포인트, 초과 절단)·제어문자(Cc)만 제거(최소 개입 — 이모지·특수문자 허용)·양끝 트림·빈 결과는 기본 닉네임 폴백·접미사는 제한 적용 후 부착. RQ-02 v1.2 개정 동반(코드와 같은 PR). **PR #7 리뷰 major — reviewer가 후속 타이밍 지정(RQ-40/HUD 착수 전 필수).** 닉네임이 길이·문자 제한 없이 권위 상태에 저장·브로드캐스트된다: ① 길이 상한 없음(ws 기본 100MB까지 통과 → 전원 브로드캐스트 증폭) ② 제어문자·개행 통과 → Top Scorer/킬피드/채팅 표시를 깨거나 시각적 사칭 가능. 객체/배열 주입은 `typeof==='string'` 필터로 이미 차단됨. 수정 방향: `uniqueNickname` 호출 전 길이 클램프+제어문자 제거+트림 폴백. **관전자(RQ-03)도 동일 무검증 경로를 탄다** — 수정 지점이 플레이어/관전자 분기 위 공유 지점이므로 구조적으로 동시 커버되지만, 구현 시 관전자 케이스 테스트를 빠뜨리지 말 것(PR #8 리뷰 minor). 스펙이 길이·문자에 침묵하므로 값 결정 시 스펙 개정 동반(코드와 같은 PR) |
| 19c | **RQ-03 정원 10+10 (초과 시 관전자/거부)** | RQ-03, RQ-41 일부, ADR-0002 | ✅ | `feat/RQ-03-capacity` 브랜치 | **착수(2026-07-22)**. GA-02(11번째→관전자)·GA-21(21번째→거부). 스코프 주의: GA-02의 "월드에 물리적으로 존재하지 않는다"는 물리(4단계) 도입 전이라 현 시점 관측 가능 범위는 "players 컬렉션 비포함 + 관전자 구분"까지 — 물리적 부재 검증은 4단계로 이월. **완료(2026-07-22, PR #8 머지)**: 한 사이클 Red(`9c40607`)→Green(`143003a`)→평가 PASS(8항목, GA-29 회귀·거부 무오염까지 프로브 실측)→리뷰 APPROVE(blocker 0/major 0). 핵심 판단: `maxClients` 대신 onJoin throw — 네이티브 방식은 룸 lock→둘째 룸 생성으로 GA-29를 깬다(코드 주석에 문서화됨) |
| 20a | **RQ-60 30Hz 고정 틱 (3단계 착수)** | RQ-60, ADR-0003/0008 | ✅ | `feat/RQ-60-fixed-tick` 브랜치 | **착수(2026-07-22)**. GA-14. 결정론 하네스(clock·scheduler)가 실 서버 30Hz 루프에 처음 결합되는 지점 — 하네스 계약의 "서버는 실 루프로, 테스트는 수동으로 같은 인터페이스" 설계가 여기서 검증된다 — **하네스 재평가(r2)의 미확인 항목 "서버 30Hz 루프와의 실제 결합"이 이 RQ로 해소됨**(17e-2 RNG 동일성과는 별개 — PR #9 리뷰가 오귀속 정정). 테스트 전략: 결정론 단위(주력) + 관대한 통합(최소). **완료(2026-07-22, PR #9 머지)**: 한 사이클 Red(`1d1d395`)→Green(`c8a5ce6`)→평가 PASS(9항목: 엡실론 경계 스캔 1e6지점 off-by-one 0, 31년 정밀도, 12회 반복 클린)→리뷰 APPROVE(blocker 0). test-writer가 Red에서 뺄셈 누적 드리프트 함정(100ms→2틱)을 계산으로 폭로, 테스트 수치가 그 버그를 정확히 잡도록 설계됨. 후속: 20a-1(catch-up clamp) |
| 20a-1 | tickDriver catch-up clamp (후속) | RQ-60, RQ-10/20 선행 조건 | ✅ | `src/shared/sim/tickDriver.ts` | **PR #9 리뷰 major — reviewer가 타이밍 지정(게임 로직이 틱에 실리기 전 필수).** `advanceByElapsed`가 긴 정지(GC·OS 서스펜드) 후 수백~수만 틱을 한 콜백에서 동기로 따라잡는다(spiral-of-death). 지금은 틱이 비어 실 비용 없음. reviewer 분석: 스케줄러 `maxCallbacksPerAdvance`는 advanceTo 1회당 상한이라 catch-up과 **직교** — 오탐도 없지만 보호도 없다. **완료(2026-07-24, PR #10)**: 하이브리드 clamp(사용자 결정 — 한 호출 15틱, 밀림 30틱 초과 시 **전량** 버림+0틱+onOverflow, `==`는 정상). RQ-60 v1.1 개정 동행(코드와 같은 PR). 평가 PASS(변이 3종 검출력·경계 프로브·3자 정합)·리뷰 APPROVE(blocker 0). 기존 테스트 1건 개정은 약화 아님(clamp 15 신규 단언 추가 — evaluator·reviewer 독립 판정 일치). 후속: 20a-2 |
| 19b-2 | 닉네임 유니코드 사칭 벡터 — 스펙 확장 여부 (후속) | RQ-02 v1.2, RQ-40/51/55 표시 계층 | ⬜ | `harness/specs/requirements.md`, `src/shared/identity/nickname.ts` | **PR #13 리뷰 minor 3건 이월 — 전부 코드 결함이 아니라 스펙 확장 여부(사용자 결정 영역).** ① v1.2는 Cc만 제거(사용자 결정 "최소 개입") — Cf(RTL override U+202E·제로폭)·동음이의 문자는 통과해 시각적 사칭 여지 잔존. 차단하려면 스펙 확장이 먼저. ② 절단이 새니타이즈 마지막이라 거대 페이로드는 접속 시 O(n) 처리 — 1차 위협(브로드캐스트 증폭)은 해소됐고 잔여는 전송 계층 페이로드 상한(별개 사안). ③ trim→truncate 순서상 16번째 코드포인트에 후행 공백 잔존 가능(표시상 사소). **HUD/채팅(RQ-40/51/55) 착수 전 ①의 결정 권고** |
| 20a-2 | onOverflow 배선 — 조용한 유실 갭 (후속) | RQ-60 v1.1 "경고" 규범 | ✅ | `fix/20a-2-onoverflow-wiring` 브랜치 | **완료(2026-07-24, PR #12)**. Red(`3657977` — 이벤트 루프 1.2초 실블록으로 배선 갭 재현)→Green(`cf53f89` — 배선 15줄)→평가 PASS→리뷰 APPROVE(blocker 0, major 1·minor 2는 **같은 PR에서 전량 반영**: `0351864` droppedTicks 단언 탈-tautology("틱" 인접 8자 창 — 리터럴 60 배제 실측)+대조군 기준선 방식 de-flake, `3d880d0` 주석 사실 정정(ADR-0009 오인용 제거·console.warn=stderr 명시))→델타 재평가 PASS(OLD 헬퍼 거짓 통과 프로브 실증)→델타 재리뷰 APPROVE 유지. 부기(20a-1 stale 주석 정리)도 이 PR에서 완료. 잔여 참고: ① 경고 하위규범 **전용 골든 부재** — 신설 여부는 사용자 결정 대기(evaluator 권고) ② 메시지 포맷 변경 시 "틱" 인접 창의 오탐 여지(reviewer — 그 PR이 볼 참고). **착수(2026-07-24, RQ-20 머지 직후 — 이동이 틱에 실려 조용한 유실이 실 게임플레이 위험이 됨).** 로깅 정책: stdout 경고(console.warn) — GameRoom은 Fastify(pino)와 분리된 Colyseus 계층, ADR-0009 도커 로그 수집이 운영 관측 경로. 부기(20a-1 리뷰 minor)도 이 라운드에서 정리: `tests/unit/sim-tick-driver.test.ts`의 "스펙 질문 별도 보고" 프레이밍 stale(v1.1로 해소됨). **PR #10 리뷰 major-2.** RQ-60 v1.1이 "유실 시 경고를 남긴다"를 규범화했는데 GameRoom이 onOverflow를 배선하지 않아 실 서버의 긴 정지 유실이 **조용히** 일어난다. 훅 자체는 tickDriver에 옳게 노출됨 — 서버 로깅 정책 결정과 함께 배선 필요. 부기(리뷰 minor): 20a-1 테스트 describe 주석의 "스펙 질문 별도 보고" 프레이밍 stale — 다음 test-writer 라운드에서 겸사 정리 |
| 20b | 클라이언트 기본 1차 — 접속·씬·상태 표시 | RQ-01/02 클라 측, RQ-61(표현만), ADR-0001/0003/0010 | ✅ | `feat/client-basic-connect` 브랜치 | **완료(2026-07-24, PR #14)**. Red(`88ba712` 10건: store 단위 6+net 통합 4)→구현(`6aa0a56` — vanilla store·connection·R3F 씬·JoinScreen·vite ws:true)→**coder가 tw의 lint 결함(prefer-const 1건)을 규칙대로 안 고치고 차단 보고 → tw 정정(`620f60b`)** — 격리 원칙이 실전에서 유지된 사례. 평가 PASS(이원 게이트: 로직 TDD+렌더 정독)→리뷰 APPROVE(blocker 0/major 0/minor 3)→minor 1·2 in-PR 반영(`9e24dbe` 청크 임계 1250 리베이스라인·forEach→for...of)→델타 재평가 PASS·델타 재리뷰 APPROVE 유지. 신규 의존성 0. 후속: ① disconnect의 onStateChange detach 미보관 — **RQ-62/63(재접속/leave UI) 착수 시** 정리(리뷰 minor 3) ② PlayerMeshes 주석 "for...of는 할당 없이 순회" 과장 — 다음 FE 라운드 겸사 정정(trivial). **착수(2026-07-24, 사용자 결정 — 19b-1 머지 후 선택지에서 채택).** 범위: 닉네임 입장 화면(캔버스 밖 DOM, 기능 최소 — 시각 디자인은 DESIGN.md 🟡 이후로 명시 유예)→`joinOrCreate`→`src/client/net/` 스냅샷 수신→`src/client/store/`(Zustand — 서버가 진실, 캐시일 뿐)→`src/client/scene/`(R3F 바닥+플레이어 표시, fe.md 프레임 예산 규칙). PR #1 이월 ②(vite 프록시 `ws:true`)도 이 PR에서 정리. **예측(RQ-62)·보간(RQ-63)·입력 전송·HUD는 후속 PR** — 스냅샷 그대로 표시가 1차. 테스트: net/store 로직만 TDD(렌더링은 ADR-0008 §6 면제 — tsc·lint·빌드·스모크+수동 확인 게이트). 골든 신설 없음(20a 선례 — RQ-02 클라 경로는 GA-01 서버 경계가 커버) |
| 20c | **RQ-62 클라이언트 예측 + 이동 입력 전송** | RQ-62, RQ-20 클라 측, RQ-61/92, ADR-0003/0004/0008 | ✅ | `feat/RQ-62-client-prediction` 브랜치 | **완료(2026-07-24, PR #15)**. Red(`06b55f6` 12건 — GA-35 손실 패킷 시나리오)→Green(`307a7e9` — prediction.ts·스키마 4필드·seq 배선·입력 캡처·30Hz 전송 분리)→평가 PASS→**리뷰 REQUEST_CHANGES(blocker: seq를 수신 시점에 기록, 위치 반영은 다음 틱 — 패치 50ms/틱 33ms 위상 차로 불일치 스냅샷 → 러버밴딩. evaluator 프로브 맹점(일관 스냅샷만 검증)을 reviewer가 검출)**→재현 테스트(`0d380e0` — vx 부호=seq 홀짝 오라클, 수정 전 7/22 위반 실측)→수정(`7243f48` — pendingSeqs 맵, seq 기록을 위치 기록과 같은 동기 블록으로 + BUFFER_CAP=100)→델타 재평가 PASS(6회 안정·이전 프로브 무회귀·자기 맹점 기록)→재리뷰 APPROVE. 교훈: 재조정 검증은 서버가 실제로 내보내는 스냅샷 층위까지 — 손으로 만든 일관 입력만으로는 배선 결함을 못 잡는다. 후속(리뷰 minor, 타이밍 지정): ① 침묵 disconnect 시 sendMoveInput 지속 — **RQ-63 leave UI 시**(20b detach 이월과 병합) ② 클라 입력당 1스텝 vs 서버 latest-wins 모델 편차 — 정정 빈도 모니터링 후 필요 시 별도 RQ ③ initialPredictionState 원점 중복 — **RQ-31 스폰 로테이션 시** shared 헬퍼 통합. **착수(2026-07-24, 사용자 결정 — 20b 머지 후 선택지에서 채택).** Phase 0에서 GA 부재로 멈춤 → 사용자가 GA-34~36 승인(즉시 반영·재조정+재생·궤적 일치). 설계는 ADR-0003 확정분(입력 시퀀스 버퍼·서버가 처리 시퀀스 반환·미확인 입력 재생). **21a-2 판단 확정**: Player 스키마에 vx·vy·vz·lastProcessedInputSeq 추가 — ADR-0003 문면("처리된 마지막 시퀀스 번호를 스냅샷에 담아") 정합 구현이자 공중 상태 재생 복원 요건(RQ-20 FAIL 라운드의 7필드 왕복 결정론이 이 재사용을 위한 선행 조건이었음). 포인터 락·WASD 캡처는 렌더 계층 면제(20b 이원 게이트 선례). 부기: 20b trivial(PlayerMeshes 주석 과장)도 이 라운드 겸사 정정 |
| 20d | **RQ-63 보간 (타 플레이어 표시)** | RQ-63, RQ-62 경계, ADR-0003 | ✅ | `feat/RQ-63-interpolation` 브랜치 | **완료(2026-07-24, PR #16)**. Red(`d910229` 11건 — 매니저 계약·경계 3종 고정·GA-39 구조적 self 거부)→Green(`c587edf` — interpolation.ts·getPosition/copyPositionInto 이원 API(GA-38 참조 비교 무력화 방지)·시각 축 단일화·disconnect 정리 부기)→평가 PASS(실서버 2클라 관찰 — RQ-62 교훈 적용)→리뷰 APPROVE(major 1: 버퍼 무한 성장)→계약 추가(`5e53957`)+수정(`409d436` — pruneBuffer ×10 윈도우·역방향 스캔)→델타 재평가 PASS·델타 재리뷰 APPROVE 유지. **이월 2건 해소**: 20b detach·RQ-62 sendMoveInput 지속(onDisconnect 훅). **ADR-0011(선별 Red) 정책 개정 동반**(`3cd7218`, 사용자 결정). 후속(리뷰 minor, 후속 이슈 지정): ① interpolation.ts 계약 주석에 "엄밀 증가 가정·tie 미규정" 1줄 ② tdd.md Phase 2 본문의 test-after 경로 미반영 ③ ADR-0008 §4에 ADR-0011 역참조 포인터 — 셋 다 다음 하네스/FE 라운드 겸사. **착수(2026-07-24, 사용자 결정 — RQ-62 머지 후 선택지에서 채택).** Phase 0에서 GA 부재로 멈춤 → 사용자가 GA-37~39 승인(선형 보간+지연 버퍼·지터 연속성·자기/타인 경로 분리). 설계는 ADR-0003 확정분(지연 버퍼 한 스냅샷 간격 이상, 약 33~66ms 과거 시점). 부기(이월 정리, 스코프 명시): 침묵 disconnect 감지 시 전송 인터벌·구독 정리 — 20b 후속(detach 미보관)+RQ-62 minor ①(sendMoveInput 지속) 병합분, leave UI 없이 정리 가능한 범위만 |
| 21a | **RQ-20 이동 (4단계 착수 — 평지+점프, 순수 산술)** | RQ-20, RQ-92/61 결합, ADR-0004 | ✅ | `feat/RQ-20-movement` 브랜치 | **착수(2026-07-24)**. Phase 0에서 GA 부재로 멈춤 → 사용자가 GA-30~33 결정(속도 6m/s·배율·점프 1.0m·서버 권위). 범위: 평지 이동+점프 순수 산술(shared 틱 함수 — RQ-62 예측 재사용 대비). Rapier·사다리(RQ-21)·박스(RQ-22)는 맵 단계(8)로 — ADR-0004에 단계적 도입 노트 추가. **완료(2026-07-24, PR #11)**. 파이프라인 첫 **FAIL 라운드 실증**: Red(`27512d6`)→Green(`a888038`)→평가 FAIL(WeakMap 은닉 — JSON 왕복 시 수평 관성 소실·참조 의존, 프로브 실증)→계약 확장(`3ce2718`, MoveState 7필드 — evaluator·reviewer 독립 판정 "약화 아닌 확장" 일치)→수정(`5d254dd`)→재평가 PASS(FAIL 프로브 원본 재실행, 왕복 클론 비트 단위 일치)→리뷰 APPROVE(blocker 0). 교훈: 은닉 상태(WeakMap·클로저)는 스냅샷·롤백(RQ-62) 전제를 깬다 — 상태는 값으로 완전 표현. 후속: 21a-1, 21a-2 |
| 21a-1 | ADR-0004 이동·물리 재프레이밍 승격 (후속) | ADR-0004, RQ-20~22 | ⬜ | `harness/adr/0004-physics-collision.md` | **PR #11 리뷰 major — reviewer가 타이밍 지정(8단계 맵 착수 시).** ADR-0004 결정#1 "Rapier로 이동 계산"이 이번 인라인 노트로 "이동=순수 함수 정본, Rapier=충돌 계층"으로 재프레이밍됨(같은 PR 개정·사용자 결정이라 사일런트 아님). RQ-20(평지·충돌 0)에선 기능 차이 없으나 8단계 Rapier 통합 시 순수함수-정본 모델 vs KinematicCharacterController move-and-slide 재작업 리스크 — 인라인 노트 누적 대신 **새 ADR로 승격**해 충돌 해소 방식을 결정하고 착수할 것 |
| 21a-2 | Player 스키마 와이어 확장 판단 (후속) | RQ-62, RQ-20 | ⬜ | `src/shared/schema/GameState.ts` | **PR #11 평가 비차단 참고.** `MoveState`는 7필드로 직렬화 완전하나 브로드캐스트 `Player` 스키마는 x·y·z만이다. RQ-62 클라 예측이 공중 상태를 스냅샷에서 이어받으려면 vx·vy·vz가 와이어에 필요할 수 있음 — **RQ-62 착수 시** 확장 필요 여부를 먼저 판단(불요 판정도 기록) |
| 20 | 3. 네트워킹 | RQ-60~64 | ⬜ | `docs/req/07_Roadmap.md` §3 | ADR-0002/0003(전송·넷코드)·ADR-0005(히트판정, 승인) 기반. RQ-64 되감기 상한 **200ms**, 허용 RTT **150ms**로 확정(질문 28·29) |
| 21 | 4. 플레이어 이동 | RQ-20~22, RQ-92 | ⬜ | `docs/req/07_Roadmap.md` §4 | ADR-0004(물리/충돌, 승인) 기반. RQ-92 확정: 이동 6m/s·앉기 50%·천천히 걷기 70%·점프 1.0m·낙하데미지(3m 초과 1m당 10)·에어 스트레이프 미허용(질문 5~9) |
| 22a | **전투 코어 — RQ-12/13/14 (+RQ-17, RQ-90 일부)** | RQ-12/13/14/17/61, RQ-90, ADR-0005/0008/0011 | ✅ | `feat/RQ-12-14-combat-core` 브랜치 | **완료(2026-07-25, PR #17)**. Red(`e8c503a` 33건 — 단위 26·통합 7, GA-05~08 전용 파일)→Green(`780e34a` — combat.ts 순수 판정·combat-tuning·hp/kills 스키마·fire 핸들러. coder가 겹쳐 스폰 문제로 eyeHeightM 1.9 잠정값 채택)→평가 PASS(판정 수학 독립 프로브·eyeHeight 진단 실증)→리뷰 APPROVE(major 1: **시신 재사격 킬 파밍** — tw의 "스코프 밖" 문서화가 만든 그물 구멍을 reviewer가 검출)→재현 Red(`b69d4b4` — 되돌림 프로브로 진단값 kills=2 재현 확인)→수정(`33cc709` — applyDamage 전이 조건 1줄, GameRoom 0줄)→델타 재평가 PASS·델타 재리뷰 APPROVE 유지. 교훈(evaluator 맹점 기록): **구현자가 "스코프 밖"이라 문서화한 도달 가능 라이브 경로는 미검증으로 취급하고 상태 시퀀스로 프로브할 것.** 후속 이월: ① eyeHeightM=1.9 현실값 복원+GA-06 재확인 — **RQ-31 스폰 로테이션 시**(복원 조건 주석 명문화됨) ② applySpread 서버 배선+콘 반경 튜닝 — **밸런싱 단계** ③ 킬 이벤트 headshot 플래그 — **RQ-55 킬피드 시** ④ GA-06 벽 차폐 케이스 회귀 — **맵 단계** ⑤ 시신 잔존 경로는 RQ-15 리스폰이 소멸시킴. **착수(2026-07-24, 사용자 결정 — RQ-63 머지 후 선택지에서 채택).** 골든 GA-05~08 기존 매핑(스펙 정규화 시점 신설분 — GA 신설 불요). **사용자 결정 2건**: ① GA-06 given 일반화 v1.1(벽→조준 이탈 포함 — 맵 8단계 이월로, 벽 케이스는 맵 단계에서 회귀 추가) ② 탄퍼짐(RQ-90 랜덤 콘)은 **구조만**(시드 주입 RNG 경로+콘 산술 shared 구현·단위 검증, 기본 반경 0=정조준을 설정 파일에) — 콘 반경·저하 계수 튜닝값 결정은 밸런싱 단계로. 범위: 사격 메시지(방향+seq)→서버 hitscan(2단 히트박스 — 순수 산술 레이 교차, Rapier 불요)→데미지(바디25/헤드50)→HP·사망·킬 기록. rate-limit 150ms(ADR-0005 — 클라 연사 조작 차단). **히트박스 치수는 설정 파일 튜닝값**(기본: 표준 인체 비례 — ADR-0005 잔존 사항, 게임 감각 튜닝 대상). 제외: RQ-15 리스폰·RQ-16 스폰보호·RQ-18 낙하·RQ-64 랙보상·클라 사격 UI. Red-first 영역(ADR-0011 — shared 판정+서버 권위) |
| 22b | 클라 사격+마우스 룩 — 조준·발사·시점 (RQ-12 클라 측) | RQ-12 발사 이벤트, RQ-61, ADR-0005/0011, fe.md | ✅ | `feat/client-aim-fire` 브랜치 | **착수(2026-07-25, 사용자 결정 — 전투 코어 머지 후 선택지에서 채택). ADR-0011 선별 Red 첫 실전 적용** — 클라 모듈이라 test-writer 없이 coder 직행(test-after, 같은 PR 동반, tests/ 순증만). 범위: 마우스 룩(pitch/yaw, 포인터 락 연계)·1인칭 카메라(**높이는 shared combat-tuning eyeHeightM 재사용** — 서버 판정 레이 원점과 시각 정합, 값 복제 금지)·조준 벡터 산술·클릭 발사→'fire' 전송·로컬 쿨다운(WEAPON 상수 재사용)·WASD를 yaw 기준 회전해 월드 벡터 전송(서버 계약 무변경 — 예측도 동일 입력). 테스트: 조준·회전 순수 산술만 단위(test-after), 렌더·DOM 면제. 제외: RQ-10/11 탄약·재장전(서버 판정 — 다음 라운드), HUD(크로스헤어 정식 디자인은 DESIGN.md 후 — 임시 중앙 점만), 피격 효과(RQ-70/71), **발사 이벤트의 타임스탬프/시퀀스(ADR-0005 결정 1항 명시분) — RQ-64 랙보상 라운드로 유예**(그 필드의 유일한 소비자가 되감기 기준 시각이라 지금 실으면 아무도 읽지 않는 필드가 된다. RQ-64 착수 세션은 **없다는 전제**에서 시작해 클라·서버 양쪽에 함께 도입할 것 — ADR-0005 "결과" 절과 상호참조). **완료(2026-07-25, PR #18)**. ADR-0011 첫 test-after 라운드 — coder 직행(원 세션이 주간 한도로 중단돼 team-lead가 구현 마무리, 판정은 격리 유지). 구현(`2978b90`)→평가 PASS(three 카메라 실측 오차 3.3e-16, 변이 21/21 killed)→리뷰 APPROVE(major 2·minor 7)→대응(`35b2d5f`)→**평가가 그 대응의 무력함을 실증**(가드가 배선 두 줄을 복제만 해 변이 6/6 생존)→재설계(`908ab2a` — `cameraLook.applyLookToCamera` 공유 함수로 배선과 가드를 기계적으로 결속, W1/W2/W3 killed 전환)→문서 정정(`11aa7b7`)→델타 재평가 2차 PASS·델타 재리뷰 APPROVE 유지(리뷰 세션 독립 변이 10/10 검출 확인). 교훈: **가드 테스트가 대상 코드를 임포트하지 않으면 거짓 신호를 낸다** — 복제가 아니라 공유로 묶어야 한다. 후속 이월: ① 위치 인자 오용(W2p·W4)은 테스트로 못 닫힘 → `LookAngles` 객체 전달 래퍼로 **타입 차원 차단**(다음 FE 라운드) ② 호출부 삭제·우회(W6~W8)는 렌더 면제의 구조적 귀결 — **구멍이 아니라 사람 담당**(리뷰 정독 + 브라우저 스모크가 지정 게이트) ③ eyeHeightM 원점이 클라·서버 두 곳에서 각각 계산됨 — shared 헬퍼로 통합(**RQ-15/16 라운드에 입력 전달됨**, 22a 후속 ①과 병합) ④ 재연결 시 시야 리셋(RQ-91) ⑤ 로컬 쿨다운=서버 최소 간격 마진(밸런싱). **미이행 기록**: fe.md 스크린샷·실브라우저 수동 확인을 **사용자 결정으로 생략하고 머지**했다(감도 체감·포인터락 UX·크로스헤어 시인성 + 위 ②의 지정 게이트) — 다음 플레이테스트 때 확인 |
| 22c | **RQ-15 리스폰 + RQ-16 스폰 보호** (+잠정 스폰 로테이션·사망자 갭·eyeHeight 복원) | RQ-15/16, RQ-31 선택 규칙, RQ-14/61, ADR-0005/0008/0011 | ✅ | `feat/RQ-15-16-respawn-protection` 브랜치 | **⚠️ 이 행은 머지 시점에 사후 신설됐다**(착수 시 🔄 기록 누락 — 리뷰 major 1. 지표 M2를 읽을 때 이 행의 '착수' 기록은 사후 재구성임을 감안할 것). **완료(2026-07-25, PR #19)**. 범위: 리스폰 90틱·HP 100 초기화(GA-09)·스폰 보호 90틱/사격 시 즉시 해제/자연 만료(GA-10)·**최초 입장에도 보호**(RQ-16 '스폰' 문면 — 평가·리뷰 독립 확인)·잠정 스폰 로테이션 15지점 전역 순환(RQ-31 선택 규칙만, 좌표는 맵 단계가 교체)·**사망자 갭 해소**(시신의 사격·이동 무시)·**시신 통과**(사용자 결정 — hitscan 후보에서 사망자 제외)·**eyeHeight 1.9→1.7 복원**(22a 후속 ① 종료)·**`eyeOrigin` 공유 계약**(22b 후속 ③ 종료 — 서버 배선 완료). 파이프라인: Red(`0052838`·`56efe4d`)→구현(`47fb924`)→**평가 FAIL**(기존 골든 GA-06이 스폰 보호로 **공허화** — 통과 이유가 '미스'에서 '보호'로 뒤바뀜, 프로브로 실증)→공허화 해소+양성 대조군(`cc143bd`)→재평가 PASS→리뷰 APPROVE(major 3·minor 11)→대응 4커밋→델타 재평가·재리뷰 PASS/APPROVE 유지. **교훈**: 스펙을 더 충실히 이행하면 기존 테스트의 **전제**가 무너진다(11건). 단언은 그대로 두고 셋업만 적응시키되, 그때 **통과 이유가 바뀌지 않았는지**(공허화)를 반드시 프로브로 확인할 것. **Red-first 순서 역전 기록**(ADR-0011 문면 위반): 시신 통과는 구현(`1a32d45`)이 테스트(`6daa62e`)보다 먼저 커밋됐다 — 워크트리 공유로 인한 사고. tw가 구현을 일시 되돌려 3회 Red 확인, 평가가 독립 재현. 리뷰는 '수용하되 기록' 판정. 후속 이월: ① **사망 처리가 `handleFire`에만 있다** — 다른 피해원이 `diedAtTick`을 심지 않으면 영구 시신 → **RQ-18 낙하 데미지 착수 시** 필수 ② RQ-31 Safe Zone(반경 5m·사격 불가)과 RQ-16 해제 경로의 우선순위 → **맵 단계 스펙 질의** ③ 스폰 지점 최소 간격 8.60m < Safe Zone 지름 10m → **맵 단계 좌표 확정 시 제약으로** ④ 클라 카메라가 `eyeOrigin`을 호출하지 않는다(무할당 out-파라미터 변형 필요 — 프레임 예산) → 다음 FE 라운드 ⑤ raw `npx vitest run` 동시 실행 시 워커 크래시 관측(게이트 경로 `npm run check`는 영향 없음 — 분리 실행+시그니처 재시도) → 통합 파일이 늘어나는 라운드에서 재측정 ⑥ 변이 실험과 리뷰가 같은 워크트리 공유(프로세스) → 변이는 전용 체크아웃에서 |
| 22d | **RQ-10 탄창 + RQ-11 재장전** | RQ-10/11, RQ-90(150ms), ADR-0005/0008/0011 | ✅ | `feat/RQ-10-11-ammo-reload` 브랜치 | **착수(2026-07-25, 병렬 트랙 C — RQ-15/16 머지로 `GameRoom.ts`가 풀린 직후).** 골든 GA-03·GA-04 기존 매핑(신설 불요). 범위: 탄창 10발·예비 무한(RQ-10)·재장전 2초 잠금(RQ-11 — 요청 시 또는 탄창 0일 때, 잠금 중 사격 무시)·틱 기반 타이머(결정론). **제외**: 클라 탄약 HUD(RQ-53 — DESIGN.md 대기), 재장전 애니메이션·사운드, RQ-18/64. Red-first 영역(서버 판정 로직) **완료(2026-07-26, PR #21)**. Red(`f255034`)→**테스트 레이스 규명**(coder가 구현을 방어하지 않고 진단: 루프가 `send();sleep();` 순서라 첫 발만 rate-limit 미만 간격 → 정당하게 드롭 → 탄창 1발 잔존. 실측 로그 `RATE-LIMIT-DROP delta=31ms`, 대조 증거 GA-04(a)만 통과)→테스트 수정(`1e49214`)→구현 커밋(`241d9a7`)→평가 PASS(**140ms에서 실패**하는 음성 대조로 sleep 여유를 실측)→리뷰 APPROVE(**major 2**: ① `handleReload`에 `canAct` 가드 누락 — 시신이 건 재장전 잠금이 리스폰을 넘어 살아나 부활 후 ≈2초 사격 불가 ② 재장전 중 재요청이 2초 타이머를 재시작 — 무한 연장 가능)→재현 Red(`622310d`)→수정(`b17d09f`)→델타 재평가 PASS(되돌림 프로브)·델타 재리뷰 APPROVE 유지→minor 5·6(`a0dc481`·`ab83f8e`). **교훈**: 통합 테스트에서 `send(); await sleep();` 순서는 **첫 발만 간격이 없다** — rate-limit이 있는 도메인에서 소모 개수를 세는 테스트는 루프 진입 전에 간격을 확보할 것. 후속 이월: ① **리스폰 시 탄창·재장전 잠금 초기화 여부** — 스펙 침묵, 게임 감각 결정(밸런싱 단계) ② `reloadStartedAtMs` 주석 정정(minor 3 — 하네스 정비) ③ 음성 단언의 고정 sleep 의존(minor 4 — 여유 300ms, 140ms에서 실패 실측) ④ **`RELOAD_MS < RESPAWN_MS` 암묵 의존**(minor 5로 주석화 — 재장전을 3초 이상으로 튜닝하면 `respawnPlayer`가 `reloadStartedAtTick`도 지워야 한다) |
| 22 | 5. 전투 | RQ-10~18, RQ-70~71, RQ-90 | ⬜ | `docs/req/07_Roadmap.md` §5 | ADR-0005(히트판정, 승인) 기반. RQ-90 확정: 바디25/헤드50, 400RPM, 감쇠없음, 랜덤 콘 탄퍼짐(질문 1~4) |
| 23a | **RQ-40 글로벌 채팅 + RQ-95 금칙어 필터** | RQ-40/95, RQ-41(관전자 참여), RQ-61, ADR-0002/0008/0011, fe.md | ✅ | `feat/RQ-40-chat` 브랜치 | **착수(2026-07-25, 병렬 트랙 D — 우선순위 1위: 프로젝트 정체성('채팅이 중심')인데 완전 미구현이었다).** 골든 GA-12(도착 순서)·GA-24(필터) 기존 매핑. **50개 히스토리 복원·채팅 중 게임 입력 차단은 골든이 없어 RQ-40 EARS 문면을 근거로 진행**(20a-1·20a-2 선례). 범위: 채팅 메시지 송수신·**서버 도착 순서 보장**(GA-12)·최근 50개 보관 및 접속/재접속 시 복원·**금칙어 필터**(RQ-95/GA-24, 순수 shared 모듈)·**관전자도 채팅 참여**(RQ-40 문면 '플레이어 또는 관전자', RQ-41)·클라 최소 채팅 패널(입력·로그)과 **포커스 시 게임 입력 차단**(fe.md 입력 처리 규칙 — 개별 핸들러 분산 체크 금지, 최상단 게이트). **시각 디자인은 DESIGN.md 이후로 유예**(22b 임시 크로스헤어 선례 — 기능 최소). **제외**: RQ-42 스프레이·RQ-94 비트맵 100종(에셋·ADR-0007 필요), RQ-43 AFK(별도 라운드 — GA-13이 관전자 승격까지 요구), RQ-51~55 HUD 정식 **완료(2026-07-26, PR #22)**. Red(`67efe1f` 6파일)→Green(`66eb810`)→**격리 결함**(`autoDispose=false`+RQ-04로 룸 `chatHistory`가 it() 사이 누적, 단언은 정확 일치 → `beforeEach`로 서버 격리, `6d82fe8`)→평가 FAIL(**GA-12 공허**: (A)수신자 간 일치는 덮이나 (B)순서==도착순서가 동시성 구간에서 미단언 — 40ms 배칭+역순 변이가 3건 전부 통과)→단일 소켓 버스트 보강(`300bcc9`)→재평가 PASS→리뷰 APPROVE(**major 4**: M1 HUD가 netcode 직접 구독(일회성 이력 유실 위험)·M2 포커스가 언마운트에서 리셋 안 돼 **입력 영구 차단**·M3 클라 로그 무제한·M4 게이트 복제+배선 미강제)→대응(`8b3cba9`)+배선 가드(`027fd71`)→main 병합(`1fad1f4`, 충돌 2건 양쪽 유지)→**델타 재평가 FAIL 2**(동작 결함 아님 — **새 코드에 그물 부재**: `connection.ts` 채팅 구독을 통째로 지워도 330건 초록, 관전자 이력 전송을 지워도 통과)→보강(`acf2af0`)→재평가 PASS(변이 3종 독립 재현). **교훈**: 리뷰가 고친 코드에 테스트가 없으면 그 수정은 되돌아가도 아무도 모른다 — ADR-0011 §2의 '같은 PR 동반'은 렌더 면제와 별개다. 후속 이월: ① **m10 조건부** — 다자 발신 재정렬은 현재 `handleChat`이 완전 동기라 경로가 없으나, **채팅 경로에 큐·배칭·비동기를 도입하는 PR에서는 필수 검증 항목** ② m3 금칙어 매칭 단위(부분 문자열 과잉 필터) — 목록과 함께 **스펙 질의** ③ m7 HUD 중첩 live region — DESIGN.md 후 RQ-51~55에서 일괄 ④ **M4 배선 가드의 구조적 한계** — `PlayerControls.tsx`를 임포트하는 테스트가 0건이라 배선 우회는 못 잡는다(22b 후속 ②와 동종, 렌더 면제의 귀결) ⑤ 채팅 본문 길이 무제한(RQ-02 v1.2가 닉네임에 상한을 둔 것과 대비) — 스펙 질의 ⑥ **통합 워커 크래시 빈도**: 평가 5회 중 2회(40%) 1차 크래시·재시도 1회로 green, tw는 3연속 하드 실패 1회 관측. 동시 워크트리 세션 경합과 겹치는 경향은 있으나 **인과 미규명**(재시도 1회당 ≈22초) ⑦ 수동 스모크 2건 — '채팅 입력 중 접속 종료 → 재접속 후 이동 가능', '채팅 포커스 중 WASD·클릭 무효' |
| 23 | 6. 채팅 | RQ-40~43, RQ-94, RQ-95 | ⬜ | `docs/req/07_Roadmap.md` §6 | RQ-94(스프레이 AI 생성 100종, 질문 21)·RQ-95(기본 금칙어 필터만, 질문 22) 확정. ADR-0007(맵 에셋 파이프라인, 스프레이 로딩 전략 포함) 승인 |
| 24 | 7. HUD | RQ-50~55, RQ-91, RQ-93 | ⬜ | `docs/req/07_Roadmap.md` §7 | RQ-91(관전 카메라: 자유시점+1인칭 추적, 질문 10~13)·RQ-93(Top Scorer = 서버 가동 이후 누적, 질문 24·26) 확정 |
| 25 | 8. 맵 | RQ-30~32 | ⬜ | `docs/req/07_Roadmap.md` §8 | ADR-0007(맵 에셋 파이프라인, 승인) 기반. 확정: 60×60m 소형, 색조·재질만 Dust2 유사(레이아웃 독자설계), 스폰 14~16개 순환 로테이션, Safe Zone 반경5m·사격불가, 사다리2·박스클러스터3(질문 14~19) |
| 26 | 9. 최적화 | RQ-60(틱 예산), CLAUDE.md 프레임 예산 불변식 | ⬜ | `docs/req/07_Roadmap.md` §9 | `metrics-baseline.md` M8(틱 초과율)·M9(프레임 예산 준수율) 계측 인프라 구축과 병행. 목표 하한 사양(질문 37: 내장 GPU 30fps)이 M9 목표치 근거 |
| 27a | **RQ-80 Docker 배포 (10단계 착수)** | RQ-80, ADR-0009, ADR-0002 | ✅ | `feat/RQ-80-docker-deploy` 브랜치 | **착수(2026-07-25, 사용자 결정 — 병렬 트랙 B).** 트랙 A(RQ-15/16→RQ-10/11)와 **소스 파일 겹침 0**이라 별도 워크트리로 병렬 진행. Phase 0에서 GA 부재 → 사용자가 **GA-41~43 승인**(헬스체크·WS 업그레이드 프록시·재시작 복구). 범위: Dockerfile(멀티스테이지 — 클라 빌드+서버 번들), docker-compose(app+nginx, Redis는 RQ-81까지 유예), nginx.conf(HTTP/WS 동일 오리진 프록시·TLS 종단 자리만 열어둠 — RQ-80 문면), `scripts/smoke-deploy.sh`(GA-41~43 검증). **제외**: RQ-81 통계·Redis 영속(별도 라운드), TLS 실사용, 무중단 배포(RQ-80이 비목표로 명시). ADR-0011상 인프라라 test-after — 스모크 스크립트가 게이트. **구현 완료(2026-07-25) — 리뷰 대기.** 실제 Docker(Desktop 4.58.0)로 빌드·기동·스모크 전부 실행해 GA-41~43 통과 확인, `bash scripts/check.sh`도 통과(기존 게이트 무회귀). 산출·근거·검증 출력 전문: `_workspace/RQ-80/01_impl_report.md`. 체크박스는 격리 리뷰(review-gate) 승인 후 전환 — CLAUDE.md "검증 판정은 별도 세션". **리뷰 APPROVE(blocker 0/major 4/minor 10, `_workspace/review/feat-RQ-80-docker-deploy.md`) → major 4건·값싼 minor 반영 완료(2026-07-25).** GA-41~43의 `verify`(`scripts/smoke-deploy.sh`)는 테스트 파일이 아닌 **수동 실행 셸 스크립트**이며 CI 밖이다 — `nginx.conf`/`Dockerfile` 회귀는 `scripts/check.sh`가 잡지 못한다(상세: `harness/evals/README.md` 트랙 A 예외 절, 리뷰 minor 7). **델타 재리뷰(2026-07-25) — APPROVE 유지, major 4/4 실측 종결.** 신규 minor 5건 중 D1(중복 `Cache-Control` 헤더 — `expires`+`add_header` 병용이 원인, `add_header` 단독으로 정리)·D5(스모크의 마지막 `wait "$PERSIST_PID"`가 유일한 무상한 대기 — 폴링 기반 `wait_pid_bounded()` 헬퍼로 상한 확보)만 반영. **이월 3건(트리거 명시)**: D2 — 주석 처리된 TLS(443) 서버 블록이 80 블록 전체를 복제해 드리프트 위험 → **TLS를 실제로 켜는 시점**에 공통부 `include`화 판단. D3 — gzip·캐시 대상 확장자에 `.wasm`·`.glb` 없음 → **RQ-30/ADR-0007 맵 에셋(glb) 도입 시** 추가. D4 — `gzip_types application/javascript`가 현재 `nginx:1.27-alpine` 기준이라 향후 MIME 기본값이 바뀌면 어긋날 수 있음 → **nginx 베이스 이미지 업그레이드 시** 재확인. **완료(2026-07-25, PR #20)**. 리뷰 APPROVE(blocker 0/major 4/minor 10)→대응(`e33bd46`)→델타 재리뷰 APPROVE 유지(**major 4/4 실측으로 닫힘** — 리뷰어가 정적 자산을 고의 파괴해 major 3이 실효로 작동함을 증명)→D1·D5(`94cc727`). **GA-41~43은 실제 Docker 스택에서 통과**(빌드→기동→헬스체크 값대조→정적 자산→WS 상호관측→재시작 후 세션 소실(`LEFT code=1006`)+복구). **주의**: GA-41~43의 `verify`는 테스트 파일이 아니라 **수동 실행 스크립트**(`scripts/smoke-deploy.sh`)이며 **CI 밖**이다 — nginx.conf·Dockerfile 회귀는 정규 게이트가 잡지 못한다(ADR-0008 §7 3분 예산상 CI 편입 부적절). 후속 이월: ① 런타임 이미지 497MB — **최대 요인은 `uWebSockets.js` 132.8MB**(colyseus 전이 의존성, 이 프로젝트는 ws-transport 사용이라 미사용 코드). 클라 전용 패키지(~47MB)는 1/5 수준이라 dependencies 재분류만으로는 대부분 안 준다 → 별도 조사 필요 ② 주석 443 블록이 80 블록을 복제 — **TLS 실제 활성화 시** include로 공통부 추출 판단 ③ 캐시·압축 대상에 `.wasm`·`.glb` 없음 — **RQ-30/ADR-0007 맵 에셋 도입 시** ④ `gzip_types`가 현재 베이스 이미지 결합 — nginx 이미지 업그레이드 시 재확인 ⑤ Redis·RQ-81 통계 영속은 별도 라운드 |
| 27 | 10. Docker 배포 | RQ-80~81 | ⬜ | `docs/req/07_Roadmap.md` §10 | ADR-0009(배포, **승인**) 기반. 확정: 사내망/로컬 전용, HTTP/WS(TLS 불요), 무중단 목표 없음(질문 27·30~32). RQ-81 통계 키는 UUID(질문 25, ADR-0006) — SQLite 스키마 설계가 이 답변에 의존 |

> v1 컷라인: 질문 34 답변에 따라 **로드맵 10단계 전부**가 v1 범위다(최적화·
> Docker 배포까지 포함) — 별도로 잘라낼 단계 없음. 마감일 없음(질문 33).
