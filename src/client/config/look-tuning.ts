/**
 * 마우스 룩 감도(22b) — 스펙 미기재 구현 선택값. `harness/workflow/fe.md`
 * "키 바인딩 소스"와 같은 성격이다: 재조정 UI는 스펙에 없으므로 클라이언트
 * 설정 파일에 고정값으로 둔다(`src/client/input/keymap.ts` 선례).
 *
 * 단위는 라디안/픽셀 — 마우스 1px 이동당 회전각(`@client/input/aimMath`의
 * `accumulateLook`이 소비). 일반적인 FPS 감도 범위는 대략 0.001~0.004rad/px다.
 *
 * **0.002 → 0.001(원장 24f)** — 초안이 그 범위의 중간값을 "실측 플레이테스트로
 * 조정 가능한 슬롯"으로 두었고, 실제 플레이테스트에서 사용자가 **"너무 빠르다,
 * 절반으로"** 로 판정했다. 즉 이 값은 추측에서 실측으로 바뀐 것이다. 범위의
 * 하단이라 더 낮출 여지는 크지 않다 — 다음 조정 요구가 오면 슬롯이 아니라
 * **감도 설정 UI**가 필요하다는 신호이고, 그것은 스펙에 없으므로 RQ 신설이
 * 먼저다(스펙에 없는 기능을 추가하지 않는다).
 */
export const MOUSE_SENSITIVITY_RAD_PER_PX = 0.001
