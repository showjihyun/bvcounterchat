/**
 * RQ-73/ADR-0015 — 6파츠 캐릭터 모델의 파츠 치수·오프셋·걸음 스윙 진폭.
 * `harness/workflow/fe.md` "config" 레이어 규칙: 스펙 미기재 클라 전용
 * 튜닝 슬롯은 여기 둔다 — RQ-73은 파츠의 **정확한 치수**를 규정하지 않는다
 * (EARS 문면에 cm 단위 수치가 없다). `look-tuning.ts`(마우스 감도)와 같은
 * 성격: 코더 재량의 시각 선택값이지, 스펙 확정값이 아니다.
 *
 * **유일한 제약(스펙·ADR)은 「모든 파츠가 히트박스 안」이다** — 값 자체는
 * 코더 재량이지만, 이 파일의 모든 비율은 `bodyRadiusM`(정본:
 * `@shared/config/combat-tuning`)에 대한 **비율**로 정의해 히트박스 값이
 * 바뀌어도(예: RQ-90 재튜닝) 함께 스케일되게 한다(ADR-0010 정신 — 절대
 * 미터 값을 매직넘버로 박지 않는다).
 *
 * ⚠️ **`bodyRadiusM`은 두 자세(선·앉음)에서 완전히 동일하다**
 * (`combat-tuning.ts` "headRadiusM·bodyRadiusM·bodyBottomM은 DEFAULT_HITBOX와
 * 완전히 동일(불변)"). 즉 아래 수평(XZ) 치수는 자세와 무관하게 항상 같은
 * 여유를 갖는다 — 자세마다 다시 검산할 필요가 없다. 아래 각주는 **선 자세
 * 기준**(`bodyRadiusM = 0.3`) 수치로 여유(마진)를 적어 둔다.
 *
 * ## 정적 배치(스윙 없음) 여유 검산
 *
 * | 파츠 | 코너 수평거리(m) | 한도(`bodyRadiusM`) | 여유 |
 * |---|---|---|---|
 * | 몸통(torso) | √(0.135²+0.105²) ≈ 0.171 | 0.3 | 43% |
 * | 팔(arm) | √(0.195²+0.03²) ≈ 0.197 | 0.3 | 34% |
 * | 다리(leg) | √(0.1095²+0.042²) ≈ 0.117 | 0.3 | 61% |
 *
 * (팔·다리는 몸통 바로 옆에 붙인 실루엣이다 — ADR-0015 결정 2 "팔을 몸 옆에
 * 붙인 실루엣은 들어가고, 팔을 벌린 포즈는 들어가지 않는다"를 그대로 따른다.)
 *
 * ## 스윙(걷기 애니메이션) 여유 검산
 *
 * 스윙은 팔·다리를 **로컬 Z축으로만 평행이동**한다(회전이 아니다 — 회전이면
 * 피벗 주위로 파츠 끝이 그리는 호의 수평 투영을 삼각함수로 매 프레임 다시
 * 증명해야 하는데, 평행이동은 코너 좌표에 진폭을 더하기만 하면 되어 안전
 * 마진을 코드 리뷰로 직접 확인할 수 있다 — 판단 근거, `_workspace/RQ-73/
 * 02_coder_green.md` 참고). 최악 코너(z가 halfDepth+진폭까지 벌어지는 지점)
 * 기준:
 *
 * | 파츠 | 코너 수평거리(스윙 포함, m) | 한도 | 여유 |
 * |---|---|---|---|
 * | 다리 | √(0.1095²+(0.042+0.10)²) ≈ 0.179 | 0.3 | 40% |
 * | 팔 | √(0.195²+(0.03+0.05)²) ≈ 0.211 | 0.3 | 30% |
 *
 * 두 표 모두 여유가 30% 이상이라 부동소수점 오차·수동 검산 실수를 감안해도
 * 히트박스를 벗어나지 않는다.
 */
export const PLAYER_MODEL = {
  /** 다리(하반신)와 몸통(상반신)의 경계 높이 — `bodyHeightM`(=`bodyTopM -
   * bodyBottomM`)에 대한 비율. 0.5 = 정확히 절반. */
  HIP_HEIGHT_RATIO: 0.5,

  /** 몸통 절반 폭(X) — `bodyRadiusM`에 대한 비율. */
  TORSO_HALF_WIDTH_RATIO: 0.45,
  /** 몸통 절반 깊이(Z) — `bodyRadiusM`에 대한 비율. */
  TORSO_HALF_DEPTH_RATIO: 0.35,

  /** 팔 절반 폭(X, 두께) — `bodyRadiusM`에 대한 비율. */
  ARM_HALF_WIDTH_RATIO: 0.1,
  /** 팔 절반 깊이(Z, 두께) — `bodyRadiusM`에 대한 비율. */
  ARM_HALF_DEPTH_RATIO: 0.1,

  /** 다리 절반 폭(X, 두께) — `bodyRadiusM`에 대한 비율. */
  LEG_HALF_WIDTH_RATIO: 0.14,
  /** 다리 절반 깊이(Z, 두께) — `bodyRadiusM`에 대한 비율. */
  LEG_HALF_DEPTH_RATIO: 0.14,
  /** 다리 중심축(X) 오프셋 — 몸통 절반 폭(`torsoHalfWidthM`, 절대값이 아니라
   * 파생값)에 대한 비율. 팔 오프셋(`torsoHalfWidthM + armHalfWidthM`, 몸통
   * 바로 옆)보다 좁게 잡아 다리가 어깨보다 좁은 인체 비례를 흉내낸다. */
  LEG_OFFSET_RATIO: 0.5,
} as const

/**
 * 걸음 스윙(RQ-73 v2.6 "걸음 위상") — 팔·다리를 로컬 Z축으로 흔드는 진폭(m).
 * 위 파일 docblock "스윙 여유 검산"이 이 값들의 안전 마진을 고정한다 — 값을
 * 올리려면 그 검산도 함께 다시 해야 한다.
 */
export const GAIT_TUNING = {
  /** 한 걸음 주기(위상 0→1)에 해당하는 누적 접지 수평 이동 거리(m). 스펙
   * 미기재 — 자연스러운 보행 룩을 위한 시각 선택값(`AUDIO.FOOTSTEP_STRIDE_M`
   * 재사용은 부적절하다 — 그 값은 "발소리 1회당 거리"이지 "스윙 왕복 주기당
   * 거리"가 아니고, ADR-0015 결정 4가 두 값을 독립으로 두라고 못박았다). */
  CYCLE_DISTANCE_M: 1.2,
  /** 다리 스윙 진폭(m, 로컬 Z 평행이동). */
  LEG_SWING_AMPLITUDE_M: 0.1,
  /** 팔 스윙 진폭(m, 로컬 Z 평행이동) — 다리보다 작게 잡아 과장된 팔 흔들림을
   * 피한다. */
  ARM_SWING_AMPLITUDE_M: 0.05,
} as const
