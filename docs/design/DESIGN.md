# DESIGN — ChatStrike UI

> **진실 공급원 4번**(`CLAUDE.md`). UI(HUD·크로스헤어·킬 피드·입장 화면) 작업은
> 이 문서를 먼저 읽는다. 충돌 시 서열은 `requirements.md` → `adr/` →
> `CLAUDE.md` → **이 문서** 순이다 — 즉 **이 문서는 스펙이 정한 것을 바꾸지
> 못하고, 스펙이 비워 둔 시각 결정만 채운다.**

- 신설: 2026-08-05 (원장 24a, 사용자 결정 3건)
- 상태: **§1~§5 확정 · §6은 다음 라운드 입력**

## 0. 이 문서가 정하는 것과 정하지 않는 것

`requirements.md` §6(RQ-50~55)은 **무엇을 어디에**만 정한다 — 원본
`docs/req/05_UI_UX.md`가 위치 다섯 개("좌상단 MiniMap / 우상단 Top Scorer /
좌하단 Global Chat / 우하단 HP·Ammo·Kill / 중앙 Crosshair")뿐이기 때문이다.
**어떻게 보이는가**가 비어 있었고 이 문서가 그것을 채운다.

**정하지 않는 것**(다른 문서 소유):
- 요소의 **존재·위치·동작** — `requirements.md` RQ-50~55
- 성능 규칙(인스턴싱·링버퍼·렌더 루프 할당 금지) — `harness/workflow/fe.md`, ADR-0001
- 무엇이 서버 권위인가 — RQ-61/62. **HUD는 판정 결과(HP·킬·명중·헤드샷)를 계산하지
  않는다 — 서버가 보낸 값을 표시한다.** 예측 표시(크로스헤어 확산 §3.1, 미니맵 자기
  위치 §3.5)는 **RQ-62 예측값을 쓰되 서버 값과 어긋나면 서버가 이긴다.**
  ⚠️ 초안은 이 줄을 "HUD는 서버 값을 표시할 뿐 계산하지 않는다"는 절대문으로 적어
  **같은 문서의 §3.1·§3.5를 스스로 금지했다**(1차 리뷰 major). 그대로 두면 다음
  라운드가 그 문면을 근거로 예측 표시를 서버 값 대기로 바꿔 조준이 RTT만큼 밀린다.

## 1. 방향

**기능 우선 · 고대비.** 반투명 어두운 배경 위에 밝은 단색 텍스트.

**왜 이 방향인가**(사용자 결정 2026-08-05):
- 맵이 RQ-30에 따라 **사암색**(`#c2b49a` 하늘 / `#8a7a5c` 바닥 — §2.1)이다. HUD를 같은
  색역으로 맞추면 분위기는 좋으나 **시인성이 떨어지고 저HP 경고 같은 상태
  구분이 어려워진다** — 그래서 "맵과 톤 통일" 안은 기각됐다.
- 어두운 반투명 배경은 **밝은 사암색 하늘·짙은 사암색 바닥 어느 쪽 위에서도** 대비를
  확보한다 — **맵 전체가 한 색역이라 밝기로 가르는 편이 안전하다.**
  ⚠️ 초안은 이 줄을 “사암색·하늘색 어느 쪽 위에서도”라고 적었다(2차 리뷰 minor) —
  바닥을 `#c2b49a`로 잘못 안 상태에서 “그럼 하늘은 파랗겠지”로 쓴 문장이고, 이 게임에
  하늘색(sky blue) 영역은 없다. **§2.1 오기의 마지막 조각이다.**
- 명도 대비를 우선하면 **접근성이 자연히 따라온다** — 색만으로 정보를 전달하지
  않는다는 규칙(§4)을 지키기도 쉽다.

## 2. 토큰

⚠️ **이 값들은 `src/client/config/design-tokens.ts`가 정본이고 이 표는 미러다.**
어긋나면 코드가 이긴다 — ADR-0010(값 복제 금지)과 같은 원리. CSS는 그 상수에서
생성한 커스텀 프로퍼티를 쓴다.

### 2.1 색

대부분 **이미 코드에 있던 값을 정본으로 승격**한 것이다(새로 발명하지 않았다).

| 토큰 | 값 | 쓰임 | 출처 |
|---|---|---|---|
| `--bg-app` | `#1a1713` | 캔버스 밖 배경(입장 화면) | 기존 `index.css:17` |
| `--fg` | `#e8e0d4` | 기본 텍스트 | 기존 `index.css:18` |
| `--fg-strong` | `#f2f2f2` | 크로스헤어·강조 수치 | 기존 크로스헤어 색 |
| `--fg-muted` | `rgb(232 224 212 / 60%)` | 보조 텍스트(플레이스홀더 등) | 기존 `opacity: 0.6` |
| `--panel` | `rgb(0 0 0 / 35%)` | 읽기 전용 패널(채팅 로그·킬 피드) | 기존 `index.css:77` |
| `--panel-strong` | `rgb(0 0 0 / 45%)` | 입력 요소 배경 | 기존 `index.css:98` |
| `--border` | `rgb(255 255 255 / 20%)` | 패널·입력 테두리 | 기존 `index.css:99` |
| `--danger` | `#e07a7a` | 오류·저HP 경고 | 기존 `index.css:128` |
| `--accent` | `#7ab8e0` | 자신 관련 강조(미니맵 자기 표시) | **신규** — `--danger`의 보색 축 |
| `--shadow-text` | `0 0 2px rgb(0 0 0 / 80%)` | 배경 없는 텍스트의 가독성 | 기존 크로스헤어 `box-shadow` |

**3D 씬 팔레트** — 정본은 `design-tokens.ts`의 `SCENE`이고 **이 표는 미러다**
(원장 24c가 이관했다. 그 전에는 `GameScene.tsx`·`PlayerMeshes.tsx`가 리터럴을 직접
갖고 대조할 코드 상수가 없었다):

| 대상 | 값 | 정본 키 |
|---|---|---|
| 씬 배경(하늘) | `#c2b49a` | `SCENE.sky` |
| 바닥 평면 | `#8a7a5c` | `SCENE.ground` |
| 반구광 `groundColor` | `#8a7a5c` | `SCENE.groundLight` |
| 자신 | `#5b8dd6` | `SCENE.self` |
| 타인 | `#c65b5b` | `SCENE.other` |
| 벽(RQ-30) | `#b9ac91` | `SCENE.wall` |
| 박스(RQ-32) | `#6b5d45` | `SCENE.box` |
| 사다리(RQ-32) | `#4a4640` | `SCENE.ladder` |
| 고지대 플랫폼(RQ-33) | `#a89574` | `SCENE.platform` |

✅ **벽·박스·사다리는 원장 24f에서 렌더된다**(`src/client/scene/MapMeshes.tsx`).
초안이 적었던 "클라이언트에 렌더 메시가 없다"는 **더는 사실이 아니다** — 그때는
지오메트리가 `src/shared/sim/{walls,boxes,ladders}.ts`에 판정 전용으로만 있어
**보이지 않는 벽에 부딪히는** 상태였고, 사용자 플레이테스트가 그것을 잡아냈다.
좌표는 `PRODUCTION_GEOMETRY` 한 곳에서만 오고 렌더는 환산만 한다(ADR-0010).

세 색은 바닥(`SCENE.ground`)과 **명도로 갈라** 골랐다 — 벽은 밝게, 박스는 어둡게,
사다리는 색상 자체를 분리한다. 원장 24c가 경고한 "벽 색이 바닥 색과 같아지는 사고"를
막는 자리가 정확히 여기였다. ⚠️ 벽 **높이**(`SCENE.wallRenderHeightM`)는 색이 아니라
렌더 선택값이고 **스펙에 근거가 없다**(RQ-30은 플레이 면적만 규정).

✅ **원장 24g 처분(사용자 결정 2026-08-12, RQ-70·71 2/2 라운드)**: 옛 값 4m는
사다리 발 높이만 기준으로 삼았는데 카메라는 눈에 있다 — 사다리 꼭대기 시점(4 +
1.7 눈높이 = 5.7m, 점프 +1.0m = 6.7m)이 그려진 벽 윗면보다 높아 벽 너머가 보이고
그쪽으로 쏜 탄(판정은 무한 높이 기둥)이 벽 위 허공에 뜨는 괴리가 있었다. 유한 높이
판정으로 바꾸는 대신(ADR-0013 "벽=무한 기둥" 어휘·이동 충돌 계층 유지) 렌더 높이를
**6.7m**로 올려 정리했다 — 유도식(발 높이 4 + 눈높이 1.7 + 점프 1.0)은
`tests/unit/24f-map-render-geometry.test.ts`가 정본 상수 기준으로 잠근다.

**명중 피드백 데칼**(RQ-70 탄흔·RQ-71 피격 효과, ADR-0016) — 정본은
`design-tokens.ts`의 `DECAL`이고 이 표는 미러다. 탄흔·피격 효과는 같은
코드 경로(법선 정렬 원형 데칼)를 쓰고 색·크기만 다르다:

| 대상 | 값 | 정본 키 |
|---|---|---|
| 표면 오프셋(공용) | `0.01`m | `DECAL.offsetM` |
| 탄흔 반지름 | `0.05`m | `DECAL.bulletHoleRadiusM` |
| 탄흔 색 | `#1a1613` | `DECAL.bulletHoleColor` |
| 피격 효과 반지름 | `0.12`m | `DECAL.hitEffectRadiusM` |
| 피격 효과 색 | `#7a1414` | `DECAL.hitEffectColor` |

⚠️ 개수 상한(탄흔 64)·GPU 버퍼 용량(피격 효과)은 디자인 값이 아니다 —
정본은 각각 `EFFECTS.BULLET_HOLE_CAP`(`@shared/constants`)와
`HitDecals.tsx` 자신의 상수다.

⚠️ **초안은 이 표를 두 행 틀리게 적었다**(1차 리뷰 blocker) — 하늘색을 바닥색이라
쓰고, **렌더되지도 않는 벽·박스·사다리에 색을 배정**했다. 원인은 씬 색에 **코드
정본이 없어서** 문서만 값을 갖게 된 것이다(원장 24c). 이 문서가 같은 PR에서
"정본은 코드, 표는 미러"를 선언해 놓고 그 코드를 잘못 읽었다.

**원장 26r과의 관계 — 부분만 닫힌다.** 26r은 RQ-30의 "사암색 팔레트, 건조한
지중해풍"이 자동 검증 불가라 디자인 리뷰의 몫인데 그 문서가 없다고 적었다. 이 표는
**현재 렌더되는 것**(하늘·바닥·플레이어)의 팔레트를 기록해 그 판단 근거를 만든다.
**맵 지오메트리 색은 원장 24f에서 정해져 이 표에 들어왔다** —
26r은 그때 완전히 닫힌다.

**사격 연습용 과녁**(RQ-34, 원장 24cw) — 정본은 `design-tokens.ts`의 `TARGET`이고
이 표는 미러다. **RQ-34가 정하는 것은 개수(벽마다 하나)·형태(동심원)·중심 높이
(눈높이)뿐**이고, 아래 값(링 반지름·색·표면 오프셋)은 스펙에 근거가 없는 디자인
선택값이다(RQ-30/32/33이 좌표·치수를 문면에 두지 않는 것과 같은 관례):

| 대상 | 값 | 정본 키 |
|---|---|---|
| 링 반지름(바깥→중심 원) | `0.5 / 0.35 / 0.2 / 0.08`m | `TARGET.ringRadiiM` |
| 링 색(바깥→중심 원) | `#f2f2f2`(`--fg-strong`) / `#e07a7a`(`--danger`) 교대 | `TARGET.ringColors` |
| 표면 오프셋 | `0.005`m | `TARGET.offsetM` |
| 원 세그먼트 수 | `32` | `TARGET.segments` |

⚠️ **판정에 참여하지 않는다**(RQ-34 핵심 결정) — 총알은 여전히 벽에 명중하고
탄흔(RQ-70)이 그 자리에 남는다. 히트마커(RQ-57)도 뜨지 않는다 — 서버에게 과녁
명중은 벽 명중이다(GA-122 "벽 명중엔 표시하지 않는다"가 그대로 적용된다). 배치
산술(안쪽면 판정·중심 좌표·회전)은 `@client/scene/targetLayout`이 하고
`tests/unit/rq-34-target-layout.test.ts`가 값으로 고정한다.

⚠️ **표면 오프셋은 `DECAL.offsetM`(탄흔·피격 효과, `0.01`m)과 값이 같지
않다 — 층 규칙이다**(PR #90 리뷰 blocker B1): `0 < TARGET.offsetM <
DECAL.offsetM`이어야 같은 벽면에서 탄흔이 과녁 **위에** 그려진다. 과거 두
값이 우연히 같아 두 평면이 겹쳤고, 어느 쪽이 이기는지가 컴포넌트 마운트
순서라는 문서화되지 않은 우연에 맡겨져 있었다.

### 2.2 타이포

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--font` | `system-ui, -apple-system, 'Segoe UI', sans-serif` | 전역(기존) |
| `--size-sm` | `0.8rem` | 채팅 로그·킬 피드·보조 |
| `--size-md` | `0.85rem` | 입력창·본문 |
| `--size-lg` | `1.5rem` | HP·탄약 수치, 입장 화면 제목 |
| `--weight-strong` | `600` | 닉네임·수치 |

**2단계 + 강조 하나**로 끝낸다. 게임 HUD는 읽는 문서가 아니라 **곁눈질로 확인하는
계기판**이라 위계가 얕을수록 낫다.

### 2.3 간격

**4px 배수 스케일.** 기존 CSS가 이미 `0.25rem`(4px)·`0.5rem`(8px)·`1rem`(16px)을
쓰고 있어 그것을 규칙으로 승격한다.

| 토큰 | 값 |
|---|---|
| `--space-1` | `0.25rem` (4px) |
| `--space-2` | `0.5rem` (8px) |
| `--space-3` | `1rem` (16px) |
| `--space-4` | `1.5rem` (24px) |
| `--radius` | `4px` |

**화면 가장자리 여백은 `--space-3`(16px)로 통일한다** — 기존 `bottom: 1rem;
left: 1rem`이 이미 그 값이다.

⚠️ **예외 하나(원장 24m, 사용자 요구)**: 채팅 패널의 `bottom`만 여백 +
`CHAT.bottomOffsetPx`(100px) = **116px**다. `left`는 여백 그대로 16px이라
사분면(좌하단, RQ-52)은 유지된다. 오프셋의 정본은 `design-tokens.ts`이고,
값을 키우면 낮은 뷰포트에서 사분면을 벗어날 여지가 생긴다(경계 조건은 원장 24p).

⚠️ **컴포넌트 치수는 이 스케일이 아니다.** 채팅 패널 너비(`22rem`)·로그 최대
높이(`8rem`)·입장 폼 너비(`16rem`)는 "그 내용이 읽히는 크기"에서 온 값이라
4px 배수를 따르지 않는다 — 스케일은 **여백과 안쪽 여백**에만 적용한다. 그
구분을 두지 않으면 모든 치수를 스케일에 맞추려다 내용이 잘린다.

## 3. 요소별 스펙 (RQ-50~55)

모든 HUD 요소는 `.hud`(`pointer-events: none`) 안에 있다. **입력을 받는 요소만
개별적으로 `pointer-events: auto`를 되살린다** — 조준·사격이 캔버스로 가야 한다.

### 3.1 크로스헤어 (RQ-54, 중앙)

**형태**: 중앙 간격을 둔 십자 4선(원장 24e에서 22b의 임시 점을 교체했다) —
선이라야 **탄퍼짐 확장을 표현할 수 있다**.

| 속성 | 값 |
|---|---|
| 선 길이 | 6px |
| 선 굵기 | 2px |
| 색 | `--fg-strong` + `--shadow-text` |
| 기본 간격(중심↔선 안쪽) | 4px |

✅ **원장 24e에서 정식 십자로 교체됐다** — 22b의 임시 점과 그 지름 상수
`CROSSHAIR.dotPx`는 **제거됐다**. 구현은 `src/client/hud/Crosshair.tsx`(4선)와
`@client/hud/crosshairSpread`(간격 계산)다.

⚠️ 24a 1차 리뷰 major가 잡은 사고가 이 자리였다 — 토큰화하면서 선 굵기(2px)를
임시 점의 지름(4px)에 물려 **조준점이 절반이 됐다**. 굵기와 지름은 **다른 양**이고,
자동 게이트가 전량 초록인 채 통과했다(ADR-0008 §6 — 렌더 계층에는 그물이 없다).

**탄퍼짐 확장** — RQ-90의 3단계와 **같은 배율을 쓴다**(새 수치를 만들지 않는다):

| 상태 | 콘 배율 | 간격 |
|---|---|---|
| 정지·앉기 | ×1 | 4px |
| 이동 | ×2 | 8px |
| 공중 | ×4 | 16px |

즉 **간격 = 기본 간격 × 콘 배율**이다. 배율의 정본은
`src/shared/config/combat-tuning.ts`의 `DEFAULT_SPREAD`이고 HUD는 그것을 읽는다 —
**값을 복제하지 않는다**(ADR-0010).

⚠️ **크로스헤어는 예측 표시다.** 실제 편차는 서버가 시드로 정하고(RQ-90/22v)
클라는 그 시드를 알 수 없다 — **간격은 "이만큼 퍼질 수 있다"는 안내이지
탄착점이 아니다.** 이 구분을 코드 주석에도 남긴다.

### 3.2 HP · 탄약 · 킬 (RQ-53, 우하단)

한 줄 배치: `HP  100   ┃   10 / 10   ┃   K 3`

| 요소 | 크기 | 색 |
|---|---|---|
| HP 수치 | `--size-lg` / `--weight-strong` | `--fg-strong`, **HP ≤ 25면 `--danger`** |
| 탄약 `현재/탄창` | `--size-lg` / `--weight-strong` | `--fg-strong`, **현재 0이면 `--danger`** |
| 킬 수 | `--size-md` | `--fg` |
| 라벨(HP·K) | `--size-sm` | `--fg-muted` |

**저HP 임계 25는 유도값이다** — `WEAPON.DAMAGE_BODY`(25)에서 왔다. 즉 **"바디
한 발 더 맞으면 죽는다"**가 경고의 뜻이고, 임의의 백분율이 아니다.

⚠️ **색만으로 알리지 않는다** — 저HP·탄약 0에서 수치 옆에 점(`•`)을 함께 띄운다.

### 3.3 채팅 (RQ-52, 좌하단)

기존 구현을 토큰으로 옮기는 수준이고 구조는 유지한다.

| 속성 | 값 |
|---|---|
| 자리 | 좌하단 — `left` 16px(여백) · `bottom` **116px**(여백 + `CHAT.bottomOffsetPx` 100px, 원장 24m) |
| 너비 | `22rem`, `max-width: calc(100vw - var(--edge-inset) * 2)` |
| 로그 최대 높이 | `8rem`, 넘치면 스크롤 |
| 로그 배경 | `--panel`, `--radius` |
| 입력창 배경·테두리 | `--panel-strong` / `--border` |
| 닉네임 | `--weight-strong` |

**컨테이너는 `pointer-events: none`이고 입력창만 `auto`다**(원장 24f) — 그러지 않으면
로그 면적까지 좌하단 플레이 클릭을 삼켜 포인터 락이 요청조차 되지 않는다.

⚠️ **예외 하나(원장 24m)**: 로그는 **포인터 락이 풀린 동안에만** `auto`가 된다
(`.hud__chat--interactive`). 채팅 이력을 스크롤하려면 포인터가 필요한데(RQ-40이
50개를 보관한다) 락 중에는 커서 자체가 없기 때문이다. **대가를 알고 치른 것**이다 —
락이 풀린 동안에는 로그 면적만큼 클릭 삼킴이 남는다(처분은 원장 24n).

포커스 중 게임 입력 차단은 이미 구현돼
있다(원장 23a — 최상단 게이트, 개별 핸들러 분산 체크 금지).

### 3.4 킬 피드 (RQ-55, 우상단 아래)

RQ-55가 **5초 노출 · 최대 5줄**을 이미 정했다. 시각만 정한다.

| 속성 | 값 |
|---|---|
| 행 형식 | `가해자 → 피해자` |
| 크기 | `--size-sm` |
| 배경 | `--panel`, 행마다 `--radius` |
| 행 간격 | `--space-1` |
| 자신이 관여한 행 | 닉네임에 `--accent` |
| **헤드샷 구분** | 화살표를 `→`에서 **`⇒`**로 바꾸고 `--danger` 적용 |

⚠️ **헤드샷 구분도 색만으로 하지 않는다** — 화살표 모양이 함께 바뀐다. RQ-55가
"구분 가능하게"만 요구하고 수단은 정하지 않았다. `headshot` 플래그는 서버가
보낸다(ADR-0005) — **클라가 판정하지 않는다**.

### 3.5 미니맵 (RQ-50, 좌상단)

| 속성 | 값 |
|---|---|
| 크기 | 160 × 160px |
| 축척 | **맵 60m → 160px = 2.67px/m** (`WORLD.SIZE_M`에서 유도) |
| 배경 | `--panel`, `--radius` |
| 테두리 | `--border` |
| 자기 표시 | `--accent` 삼각형, **꼭짓점이 바라보는 방향** |
| 세이프존 | `--border` 원, 반경 `WORLD.SAFE_ZONE_RADIUS_M`(4m) × 축척 ≈ 11px |

**타인은 표시하지 않는다.** RQ-50 문면이 "자신의 위치와 방향"만 요구하고,
타인 위치는 **전술 정보라 스펙에 없는 기능을 만드는 것**이 된다(CLAUDE.md 금지 1항).

**구분 기준**(1차 리뷰 minor — 초안의 배제 논거가 세이프존 원을 걸러내지 못했다):
**정적 맵 정보**(세이프존 경계 — 플레이하면 알게 되고 시간에 따라 변하지 않는다)는
"미니맵"이라는 요구의 일부로 본다. **동적 전술 정보**(타인 위치·체력·무기)는 아니다.
맵 지오메트리(벽·박스·사다리)를 그리는 것도 정적 정보라 이 기준상 허용이다 —
그 렌더 메시는 원장 24f에서 씬에 들어왔다(§2.1). 다만 **미니맵에 그것을 투영하는
것**은 아직 하지 않았다.

### 3.6 히트마커 (RQ-57, 중앙 — 크로스헤어 위)

⚠️ **이 절은 원장 24cv(2/4 라운드)까지의 시각 부분만 정한다** — RQ-57의
"확인음"은 오디오 라운드(3/4)가 §7(신설 예정)에서 정한다.

**형태**: 크로스헤어(§3.1)와 같은 중심점 위에 겹치는 X 표시(대각선 2개) —
크로스헤어의 십자와 45° 회전 관계라 겹쳐도 서로 구분된다.

| 속성 | 값 |
|---|---|
| 대각선 길이 | 14px |
| 대각선 굵기 | 3px |
| 색 | `--hit-marker-color`(= `--danger` 재사용) |

**뜨는 조건**: 자신이 쏜 총알이 **다른 플레이어에** 명중했을 때만 —
벽 명중(GA-122)·남이 남을 맞힌 명중에는 뜨지 않는다. 판정은
`@client/effects/hitFeedback`의 `applyHitReaction`(순수)이 한다, 이
문서는 그리기만 정한다.

**지속 시간은 디자인 값이 아니다** — 정본은
`@shared/config/effects-tuning`의 `EFFECTS_TUNING.HIT_MARKER_DURATION_MS`
(ADR-0016 결정 3, "지속 시간은 스펙이 규정하지 않은 구현 세부").

### 3.7 피격 방향 (RQ-58, 화면 전체 — 가장자리)

**형태**: 화면 가장자리에서 안쪽으로 번지는 단색 그라데이션. 네 가장자리
(위·아래·왼쪽·오른쪽) 중 하나만 물든다 — 정밀 각도·좌표·화살표는
그리지 않는다(사용자 결정, 8방위 화살표 기각).

| 속성 | 값 |
|---|---|
| 번지는 폭 | **해당 축의 화면 변** 대비 35% (위/아래는 높이, 좌/우는 너비 — PR #89 리뷰 minor m1) |
| 색 | `--hit-direction-color`(= `--danger` 재사용) |
| 최대 불투명도 | 0.45 |

**어느 가장자리가 물드는가**: `@client/hud/hitDirectionEdge`의
`deriveHitDirectionEdge`가 이벤트의 **표면 법선**과 **피격자 자신의**
카메라 yaw만으로 정한다 — 사수의 좌표(스냅샷)는 이 계산 어디에도
들어가지 않는다(ADR-0016 결정 1의 재현 금지, GA-123). 좌우는 법선의
카메라 오른쪽 성분 부호로, 전후는 카메라 정면 성분 부호로 가른다 —
절댓값이 더 큰 축을 우선한다. 전후→상하 매핑(정면에서 맞으면 위쪽,
후방에서 맞으면 아래쪽)은 스펙 근거가 없는 렌더 선택값이다.

**지속 시간은 디자인 값이 아니다** — 정본은
`EFFECTS_TUNING.HIT_DIRECTION_DURATION_MS`.

⚠️ **정보 누출을 감수한 표시다**(ADR-0016 결정 1, 사용자 결정
2026-08-13) — 방향을 주면 사수의 대략적 위치가 샌다. 이 문서의 §1
("색만으로 정보를 전달하지 않는다")과는 별개 축이다 — 색은 하나뿐이고
가장자리 **위치**로 방향을 전달하므로 §5 접근성 원칙과 충돌하지 않는다.

### 3.8 상위 스코어러 (RQ-51, 우상단)

| 속성 | 값 |
|---|---|
| 표시 | 상위 3명, `순위 닉네임 킬수` |
| 크기 | `--size-sm` |
| 배경 | `--panel`, `--radius` |
| 자신 | `--accent` |

**"상위 3명"은 이 문서의 결정이다** — RQ-51은 "상위 스코어러"라고만 적고 인원을
정하지 않았다. 정원이 10명(RQ-03)이라 3명이면 상위 30%다. ⚠️ 이 수가 스펙 값이
되어야 한다고 판단되면 **RQ-51 개정이 먼저다**(이 문서는 스펙을 못 바꾼다).

## 4. 상태

| 상태 | 표현 | 근거 |
|---|---|---|
| **세이프존 안** | 크로스헤어를 `--fg-muted`로 낮추고 탄약 수치 옆에 `⃠` | RQ-31이 사격을 막는데 **클라가 그것을 모른다**(원장 25t) |
| **재장전 중** | 탄약 자리에 진행 막대(2초, `--accent`) | `WEAPON.RELOAD_MS` = 2000 |
| **사망·리스폰 대기** | 화면 가장자리 `--danger` 비네트 + 중앙 카운트다운 | `PLAYER.RESPAWN_MS` = 3000 |
| **스폰 보호 중** | 자기 표시 주위 `--accent` 링 | **서버 플래그**(아래) |
| **저HP** | §3.2 | `DAMAGE_BODY` = 25 |

**고정 길이 상태(재장전·리스폰)만 게임 상수에서 온다 — 애니메이션 길이를 따로
정하지 않는다.**

⚠️ **조기 해제가 가능한 상태는 클라 타이머로 그리지 않는다**(1차 리뷰 major):

- **스폰 보호** — RQ-16이 "보호 중인 플레이어가 **사격하면 즉시 보호를 해제**"를
  요구한다. 클라가 `SPAWN_PROTECTION_MS`(3000) 타이머로 링을 그리면 **사격으로
  이미 풀린 뒤에도 링이 남아 HUD가 서버와 다른 것을 말한다**. 서버가 보호 플래그를
  스냅샷에 실어야 한다 — 현재 `Player` 스키마에 **없으므로**(`nickname·x/y/z·
  vx/vy/vz·grounded·lastProcessedInputSeq·hp·kills`가 전부) 필드 추가는 그 표시를
  구현하는 라운드의 일이다.
- **세이프존 안** — 사격을 거부하는 주체가 서버이므로(RQ-31) 표시의 정본도 서버다.
  클라가 `@shared/sim/spawn` + 예측 위치로 **선표시**하는 것은 허용하되 서버 값이
  오면 서버가 이긴다. 이 플래그도 현재 스키마에 없다.

§3.4가 헤드샷에 대해 적은 원칙("`headshot` 플래그는 **서버가 보낸다** — 클라가
판정하지 않는다")이 이 두 상태에도 그대로 적용된다.

⚠️ **이 두 플래그를 실제로 배선하는 라운드는 와이어 요구를 RQ 문면에 넣는다** —
RQ-55가 "서버의 킬 이벤트 페이로드가 `headshot` 플래그를 포함해야 한다"로 처리한
선례를 따른다. **이 문서는 서열 4번이라 와이어 계약의 최종 귀속처가 아니다** — 이
문서만 보고 스키마에 필드를 더하면 **스펙 근거 없는 와이어 변경**이 되고 CI의
스펙-코드 동행 경고가 그 자리에서 운다(2차 리뷰 minor).

## 5. 접근성

- **색만으로 정보를 전달하지 않는다** — 저HP·헤드샷·세이프존 전부 형태나 기호를 겸한다.
- 명도 대비는 어두운 반투명 배경 위 밝은 텍스트로 확보한다.
- 채팅 로그는 **하나의** live region이다 — 중첩 금지(원장 23a 이월 ③, 리뷰 minor m7:
  "HUD 중첩 live region — DESIGN.md 후 RQ-51~55에서 일괄").

## 6. 미결 — 다음 라운드 입력

| 항목 | 왜 지금 안 정하나 |
|---|---|
| 폰트 파일 도입 | 시스템 폰트로 시작한다. 웹폰트는 로딩·라이선스 결정이 따로 필요하다 |
| 사운드 | 스펙에 없다 |
| RQ-91 관전 카메라 UI | 그 RQ 착수 라운드에서 함께 정한다 |
| 애니메이션·트랜지션 | 렌더 루프 할당 금지(ADR-0001)와 함께 봐야 한다 |
| 반응형·모바일 | RQ 비요구사항(§11)에 **모바일 제외**가 명시돼 있다 |
